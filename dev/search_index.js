var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/types/#Types-API-Reference","page":"Types","text":"This page documents the core data types in ERGM.jl.","title":"Types API Reference"},{"category":"section","location":"api/types/#Abstract-Term-Types","page":"Types","text":"","title":"Abstract Term Types"},{"category":"section","location":"api/types/#AbstractERGMTerm","page":"Types","text":"","title":"AbstractERGMTerm"},{"category":"section","location":"api/types/#StructuralTerm","page":"Types","text":"","title":"StructuralTerm"},{"category":"section","location":"api/types/#NodalTerm","page":"Types","text":"","title":"NodalTerm"},{"category":"section","location":"api/types/#DyadicTerm","page":"Types","text":"","title":"DyadicTerm"},{"category":"section","location":"api/types/#ConstraintTerm","page":"Types","text":"","title":"ConstraintTerm"},{"category":"section","location":"api/types/#Term-Collections","page":"Types","text":"","title":"Term Collections"},{"category":"section","location":"api/types/#TermSet","page":"Types","text":"","title":"TermSet"},{"category":"section","location":"api/types/#Model-Specification","page":"Types","text":"","title":"Model Specification"},{"category":"section","location":"api/types/#ERGMFormula","page":"Types","text":"","title":"ERGMFormula"},{"category":"section","location":"api/types/#ERGMModel","page":"Types","text":"","title":"ERGMModel"},{"category":"section","location":"api/types/#Results","page":"Types","text":"","title":"Results"},{"category":"section","location":"api/types/#ERGMResult","page":"Types","text":"","title":"ERGMResult"},{"category":"section","location":"api/types/#MCMLEConvergence","page":"Types","text":"","title":"MCMLEConvergence"},{"category":"section","location":"api/types/#MCMCDiagnostics","page":"Types","text":"","title":"MCMCDiagnostics"},{"category":"section","location":"api/types/#Interface-Functions","page":"Types","text":"","title":"Interface Functions"},{"category":"section","location":"api/types/#compute","page":"Types","text":"","title":"compute"},{"category":"section","location":"api/types/#change_stat","page":"Types","text":"","title":"change_stat"},{"category":"section","location":"api/types/#name","page":"Types","text":"","title":"name"},{"category":"section","location":"api/types/#Batch-Operations","page":"Types","text":"","title":"Batch Operations"},{"category":"section","location":"api/types/#Result-Accessors","page":"Types","text":"The full ecosystem StatsAPI surface (coef, stderror, vcov, confint, loglikelihood, nobs, dof, aic, bic, coeftable) is defined on ERGMResult; Networks.check_statsapi(fit; strict=true) passes.","title":"Result Accessors"},{"category":"type","location":"api/types/#ERGM.AbstractERGMTerm","page":"Types","text":"AbstractERGMTerm\n\nAbstract base type for all ERGM terms.\n\nAll terms must implement:\n\ncompute(term, net) -> Float64: Compute the term statistic for the network\nchange_stat(term, net, i, j) -> Float64: The add-direction change statistic g(y⁺ᵢⱼ) − g(y⁻ᵢⱼ), independent of the dyad's current state\nname(term) -> String: Return the term name\n\nand may declare the term-trait protocol (required_vertex_attributes, requires_directed, is_dyad_dependent, ...; see src/terms/traits.jl).\n\nExample\n\nusing ERGM\nstruct Density <: AbstractERGMTerm end          # a third-party term\nERGM.name(::Density) = \"density\"\nERGM.compute(::Density, net) = ne(net) / (nv(net) * (nv(net) - 1) / 2)\nERGM.change_stat(::Density, net, i, j) = 1 / (nv(net) * (nv(net) - 1) / 2)\nERGM.is_dyad_dependent(::Density) = false\nnet = load_dataset(:florentine_marriage)\ncompute(Density(), net)                       # 0.1667\nDensity() isa AbstractERGMTerm                # true\n\n\n\n\n\n","title":"ERGM.AbstractERGMTerm"},{"category":"type","location":"api/types/#ERGM.StructuralTerm","page":"Types","text":"StructuralTerm <: AbstractERGMTerm\n\nTerms based purely on network structure (edges, triangles, etc.).\n\n\n\n\n\n","title":"ERGM.StructuralTerm"},{"category":"type","location":"api/types/#ERGM.NodalTerm","page":"Types","text":"NodalTerm <: AbstractERGMTerm\n\nTerms based on vertex attributes (nodefactor, nodecov, etc.).\n\n\n\n\n\n","title":"ERGM.NodalTerm"},{"category":"type","location":"api/types/#ERGM.DyadicTerm","page":"Types","text":"DyadicTerm <: AbstractERGMTerm\n\nTerms based on dyad-level attributes or combinations of vertex attributes.\n\n\n\n\n\n","title":"ERGM.DyadicTerm"},{"category":"type","location":"api/types/#ERGM.ConstraintTerm","page":"Types","text":"ConstraintTerm <: AbstractERGMTerm\n\nTerms that represent constraints rather than model terms.\n\n\n\n\n\n","title":"ERGM.ConstraintTerm"},{"category":"type","location":"api/types/#ERGM.TermSet","page":"Types","text":"TermSet\n\nA collection of ERGM terms.\n\nTerms are stored as a tuple so that compute_all/change_stat_all compile to statically dispatched calls per term instead of dynamic dispatch through an abstractly-typed vector (which would dominate the MCMC inner loop). TermSet(::AbstractVector) accepts a vector of terms (nested vectors are spliced, as fit_ergm does).\n\nExample\n\nusing ERGM\nts = TermSet([Edges(), Triangle()])\nlength(ts)                         # 2\nts.names                           # [\"edges\", \"triangle\"]\ncompute_all(ts, load_dataset(:florentine_marriage))   # [20.0, 3.0]\n\n\n\n\n\n","title":"ERGM.TermSet"},{"category":"type","location":"api/types/#ERGM.ERGMFormula","page":"Types","text":"ERGMFormula(terms; constraints=ConstraintTerm[])\n\nRepresents an ERGM model specification: the right-hand side of R ergm's net ~ edges + triangle + nodematch(\"attr\").\n\nterms is a TermSet or any vector of terms (the shapes fit_ergm accepts, including [Edges(), Degree(0:2)]).\n\nConstraints are not implemented. ConstraintTerm is exported as a reserved abstract type, but ERGM.jl fits unconstrained models only: passing a non-empty constraints= vector throws an ArgumentError rather than storing constraints that no sampler or estimator would honour (statnet's constraints= — edges, degrees, blockdiag, observed, … — has no counterpart yet).\n\nFields\n\nterms::TermSet: Model terms\nconstraints::Vector{ConstraintTerm}: always empty (see above)\n\nExample\n\nusing ERGM\nformula = ERGMFormula([Edges(), Triangle()])\nlength(formula.terms)    # 2\n\n\n\n\n\n","title":"ERGM.ERGMFormula"},{"category":"type","location":"api/types/#ERGM.ERGMModel","page":"Types","text":"ERGMModel{T,D}\nERGMModel(formula::ERGMFormula, net::Network{T,D}; reference=:bernoulli)\n\nAn ERGM model specification with observed network. T is the network's vertex-index type and D its directedness (Graphs.is_directed(model) == D, known at the type level, so every hot loop reading model.network is statically typed).\n\nConstruction validates the formula against the network — every attribute-based term's vertex attribute must exist on the network, and intrinsically directed terms (e.g. Mutual) are rejected on undirected networks — throwing an ArgumentError otherwise. Attribute-based nodal terms are then materialized: their attribute values are snapshotted into dense typed vectors (see src/terms/materialize.jl) so that change statistics in the estimation and sampling hot loops avoid the untyped attribute storage. Materialized terms keep the original term names and semantics.\n\nOne-mode networks only. A two-mode (bipartite-flagged) network — created with network(n; bipartite=k), or a BipartiteNetwork — is refused with an ArgumentError: the bipartite ERGM terms of statnet (b1degree, b2degree, b1factor, b1nodematch, …) and the two-mode proposal kernel are not implemented, and fitting the one-mode terms would silently count the structurally impossible within-mode dyads as observations. See the README's \"Not implemented\" section.\n\nNo self-loops. A network that contains a self-loop (has_edge(net, v, v); only possible with network(n; loops=true)) is refused with an ArgumentError too: the term statistics would count the loop, but the pseudo-likelihood, nobs, the MH proposal and every simulation range over the n(n−1) (or n(n−1)/2) off-diagonal dyads only, so a loop would inflate the observed statistics against a model that can never reproduce it (R ergm warns \"This network contains loops\" and fits regardless). A loops=true network with no loop present is accepted and modelled as the loop-free network it is — the diagonal is structurally absent.\n\nshow(model) prints the network's size and directedness and the formula (`ERGMModel{Int64,false}: 16 vertices, 20 edges (undirected); terms: edges\n\nnodecov.wealth`), never the materialized attribute vectors.\n\nFields\n\nformula::ERGMFormula: Model formula (with materialized terms)\nnetwork::Network{T,D}: Observed network\nreference::Symbol: Reference measure (:bernoulli by default)\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nmodel = ERGMModel(ERGMFormula([Edges(), Triangle()]), net)\nis_directed(model)                  # false\nmodel.formula.terms.names           # [\"edges\", \"triangle\"]\n\n\n\n\n\n","title":"ERGM.ERGMModel"},{"category":"type","location":"api/types/#ERGM.ERGMResult","page":"Types","text":"ERGMResult{T,D}\n\nResults from fitting an ERGM. The type parameters are those of the fitted ERGMModel (vertex-index type and directedness).\n\nFields\n\nmodel::ERGMModel{T,D}: The fitted model\ncoefficients::Vector{Float64}: Estimated coefficients\nstd_errors::Vector{Float64}: Standard errors\nz_values::Vector{Float64}: Z-statistics\np_values::Vector{Float64}: Two-sided p-values\nvcov::Matrix{Float64}: Estimated covariance matrix of the coefficients\nloglik::Float64: Log-likelihood (or pseudo-log-likelihood)\naic::Float64: AIC\nbic::Float64: BIC\nmethod::Symbol: Estimation method (:mple or :mcmle)\nconverged::Bool: Convergence status\nmcmc_samples::Union{Nothing, Matrix{Float64}}: Statistics sampled at the final coefficient values (for MCMLE)\nse_type::Symbol: How the standard errors were obtained (:hessian for inverse observed information, :bootstrap for parametric bootstrap, :mcmc for the inverse Fisher information estimated from MCMC samples)\nmissing_method::Symbol: How masked (unobserved) dyads were treated — :none if the network had none, :available_case if they were dropped from the pseudo-likelihood (MPLE), :mle if they were integrated out of the likelihood by the constrained chain of the missing-data MCMLE (missing=:mle), :condition_on_face if they were held fixed at their stored face value throughout MCMC (missing=:condition_on_face). See src/missing.jl.\nvcov_fisher::Matrix{Float64}: For an MCMLE fit, the inverse Fisher information Σ̂⁻¹ from the final MCMC sample alone — vcov is this plus the Monte-Carlo component V·Σ_mc·V (see mcmle). For an MPLE fit it is vcov itself.\nmcmc_se::Vector{Float64}: The Monte-Carlo standard error of each coefficient, sqrt(diag(V·Σ_mc·V)) — the part of std_errors that comes from the finite MCMC sample rather than from the data (R's \"MCMC %\"). Zeros for an MPLE fit. Accessor: mcmc_se.\nmcmc_convergence::Union{Nothing, MCMLEConvergence}: For an MCMLE fit, the convergence report recomputed on the final sample at the returned coefficients — a NamedTuple (iterations, step_length, t_ratios, hotelling_p, n_eff) — so an unconverged fit can say how unconverged it is (converged is the loop's verdict; the warning quotes these numbers). nothing for an MPLE fit.\nchain_lengths::Vector{Int}: For an MCMLE fit, the number of rows of mcmc_samples each of the n_chains independent chains contributed, in order (the sample is their concatenation), so mcmc_diagnostics can work chain by chain instead of across the seams. Empty for MPLE.\nboot_replicates::Union{Nothing, Matrix{Float64}}: For se=:bootstrap, the n_boot × p matrix of refitted coefficients the covariance was taken over; a replicate on which the MPLE did not exist (a boundary statistic or a separated design in the simulated network) is a row of NaNs and was excluded. nothing otherwise.\n\nThe full ecosystem StatsAPI surface is available on a result: coef, stderror, vcov, confint, loglikelihood, nobs, dof, aic, bic and coeftable (a Networks.CoefficientTable), plus the result-metadata protocol (Networks.fit_metadata).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nfit = fit_ergm(net, [Edges(), NodeCov(:wealth)])\ncoef(fit)             # 2-vector\nconfint(fit)          # 2×2 matrix of normal-theory 95% limits\ncoeftable(fit)        # the R-style table `show(fit)` prints\n\n\n\n\n\n","title":"ERGM.ERGMResult"},{"category":"type","location":"api/types/#ERGM.MCMLEConvergence","page":"Types","text":"MCMLEConvergence\n\nThe convergence report an MCMLE fit carries in fit.mcmc_convergence (nothing for MPLE): a NamedTuple with\n\niterations::Int: MCMLE iterations run (≤ maxiter);\nstep_length::Float64: the last Hummel step length γ (1.0 at convergence);\nt_ratios::Vector{Float64}: per-statistic convergence t-ratios |g_obs − ḡ| / sd(g) on the final sample at the returned coefficients;\nhotelling_p::Float64: p-value of the Hotelling T² test on that sample;\nn_eff::Float64: the effective sample size behind it (Geyer, summed over chains).\n\nSee mcmc_convergence, which computes the last three.\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = mcmle(ERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), net);\n            n_samples=300, bridge_rungs=0, rng=Xoshiro(1))\nfit.mcmc_convergence isa ERGM.MCMLEConvergence   # true\nfit.mcmc_convergence.step_length                 # 1.0 at convergence\n\n\n\n\n\n","title":"ERGM.MCMLEConvergence"},{"category":"type","location":"api/types/#ERGM.MCMCDiagnostics","page":"Types","text":"MCMCDiagnostics\n\nWhat mcmc_diagnostics returns: one row of chain diagnostics per model statistic, computed chain by chain when the fit ran n_chains > 1 (the sample is the chains' concatenation; chain_lengths says where the seams are) and printed as a table by show. Fields:\n\nterm_names::Vector{String}: statistic names\nautocorrelation::Vector{Float64}: lag-1 autocorrelation (within chains, length-weighted over chains)\neffective_sample_size::Vector{Float64}: ESS from the lag-1 autocorrelation, Σ_c L_c (1−ρ_c)/(1+ρ_c) — the most optimistic column\ness_geyer::Vector{Float64}: Geyer initial-sequence ESS summed over chains — all lags, the preferred column, and the same estimator the MCMLE's own convergence test uses (fit.mcmc_convergence.n_eff)\ngeweke_z::Vector{Float64}, geweke_p::Vector{Float64}: Geweke stationarity z-scores (first 10 % vs last 50 % of a chain) — with several chains the z of largest magnitude, and its two-sided p-value (the smallest); a small p flags a chain that had not reached stationarity\nn_samples::Int, chain_lengths::Vector{Int}: total draws and the per-chain counts\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = mcmle(ERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), net);\n            n_samples=400, n_chains=2, bridge_rungs=0, rng=Xoshiro(1))\nd = mcmc_diagnostics(fit)\nd isa MCMCDiagnostics              # true\nd.chain_lengths                    # [200, 200]\nprintln(d)                         # the per-term table\n\n\n\n\n\n","title":"ERGM.MCMCDiagnostics"},{"category":"function","location":"api/types/#Networks.compute","page":"Types","text":"compute(term::AbstractERGMTerm, net) -> Float64\n\nCompute the term statistic g(y) for the given network — the statnet summary(net ~ term) value. compute is the shared Networks.jl statistic generic (Networks.compute); ERGM adds one method per term type.\n\nA masked (missing) dyad is read at its stored face value: the term layer knows nothing about the mask (see supports_missing(::AbstractERGMTerm)), the estimators do.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(Edges(), net)            # 20.0\ncompute(Triangle(), net)         # 3.0\ncompute(NodeCov(:wealth), net)   # 2168.0\n\n\n\n\n\n","title":"Networks.compute"},{"category":"function","location":"api/types/#ERGM.change_stat","page":"Types","text":"change_stat(term::AbstractERGMTerm, net, i::Int, j::Int) -> Float64\n\nCompute the add-direction change statistic for dyad (i,j): g(y⁺ᵢⱼ) − g(y⁻ᵢⱼ), i.e. the statistic with edge (i,j) present minus the statistic with it absent, holding all other dyads at their current values.\n\nThe result must not depend on whether edge (i,j) currently exists — this is the convention required by both the MPLE design matrix and the Metropolis–Hastings sampler (which negates it for removal proposals).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nchange_stat(Edges(), net, 1, 2)          # 1.0 — always, present or absent\nchange_stat(Triangle(), net, 2, 9)       # 0.0 — 2 and 9 share no neighbour\nchange_stat(Triangle(), net, 6, 7)       # 1.0 — 2 is tied to both 6 and 7\nhas_edge(net, 2, 9)                      # true; the values above do not depend on it\n\n\n\n\n\n","title":"ERGM.change_stat"},{"category":"function","location":"api/types/#Networks.name","page":"Types","text":"name(term::AbstractERGMTerm) -> String\n\nThe R ergm-style coefficient name of the term (\"edges\", \"nodematch.sex\", \"gwesp.fixed.0.5\", ...), as printed in the coefficient table. name is the shared Networks.jl statistic generic.\n\nExample\n\nusing ERGM\nname(Edges())            # \"edges\"\nname(GWESP(0.5))         # \"gwesp.fixed.0.5\"\nname(NodeMatch(:sex))    # \"nodematch.sex\"\n\n\n\n\n\nname(term::AbstractERGMTerm, net) -> String\n\nThe coefficient name term carries in a model built on net — the label R ergm prints for that term on that network. For most terms this is name(term); the geometrically weighted shared-partner terms (GWESP, GWDSP) are the exception, because R's label depends on the network's directedness (gwesp.fixed.0.5 on an undirected network, gwesp.OTP.fixed.0.5 on a directed one). ERGMModel and summary_stats name their statistics with this method, so fit.model.formula.terms.names matches names(coef(fit)) of a statnet fit of the same formula on either kind of network.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nname(GWESP(0.5), net)                # \"gwesp.fixed.0.5\"\ndnet = network(3; directed=true)\nname(GWESP(0.5), dnet)               # \"gwesp.OTP.fixed.0.5\"\nname(Edges(), dnet)                  # \"edges\"\n\n\n\n\n\n","title":"Networks.name"},{"category":"function","location":"api/types/#Networks.compute_all","page":"Types","text":"compute_all(ts::TermSet, net) -> Vector{Float64}\n\nCompute all term statistics for the network — the vector g(y) of the model's sufficient statistics, in term order. compute_all is the shared Networks.jl statistic generic.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute_all(TermSet([Edges(), NodeCov(:wealth)]), net)   # [20.0, 2168.0]\n\n\n\n\n\n","title":"Networks.compute_all"},{"category":"function","location":"api/types/#ERGM.change_stat_all","page":"Types","text":"change_stat_all(ts::TermSet, net, i::Int, j::Int) -> Vector{Float64}\n\nCompute add-direction change statistics for all terms for dyad (i,j) — one row of the MPLE design matrix.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nchange_stat_all(TermSet([Edges(), NodeCov(:wealth)]), net, 1, 2)   # [1.0, 46.0]\n\n\n\n\n\n","title":"ERGM.change_stat_all"},{"category":"function","location":"api/types/#ERGM.summary_stats","page":"Types","text":"summary_stats(net, terms; missing=:error) -> NamedTuple\n\nCompute the observed value of every term on net without fitting anything — R ergm's summary(net ~ edges + triangle). Returns a NamedTuple keyed by the terms' names as they appear in a model on net (see name(term, net): a directed network's GWESP(0.5) is gwesp.OTP.fixed.0.5, as in R); terms is a vector of terms, in the same shapes fit_ergm accepts. A multi-degree count term (Degree(0:2)) expands into one entry per degree, as it does in a fit.\n\nMasked dyads (set_missing_dyad!) are refused by default — a statistic is a number computed from every dyad, and an unobserved dyad has no value to contribute (the ecosystem missing-data contract, Networks.require_observed). missing=:face is the explicit opt-in: every masked dyad is read at its stored face value (a masked tie counts as a tie). This is a descriptive statistic, so the opt-in is legitimate — but note that R's summary() treats an NA dyad as absent, so to reproduce R's number remove the masked ties first (the provenanced masked-flomarriage fixture does exactly that). missing_policies(summary_stats) == (:error, :face). The protocol-level compute/compute_all are raw evaluations with no policy of their own (they are what the estimators, which handle the mask themselves, call).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nsummary_stats(net, [Edges(), Triangle()])   # (edges = 20.0, triangle = 3.0)\nsummary_stats(net, [Degree(0:1)])           # (degree0 = 1.0, degree1 = 4.0)\nmasked = copy(net); set_missing_dyad!(masked, 1, 9)   # a masked tie\ntry summary_stats(masked, [Edges()]) catch e; e isa ArgumentError end   # true\nsummary_stats(masked, [Edges()]; missing=:face)       # (edges = 20.0,)\n\n\n\n\n\n","title":"ERGM.summary_stats"},{"category":"method","location":"api/types/#StatsAPI.coef-Tuple{ERGMResult}","page":"Types","text":"coef(result::ERGMResult) -> Vector{Float64}\n\nEstimated coefficients of the fitted model (a method of StatsAPI.coef), in the order of result.model.formula.terms.names.\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges(), NodeCov(:wealth)])\nround.(coef(fit); digits=3)      # [-2.595, 0.011]\n\n\n\n\n\n","title":"StatsAPI.coef"},{"category":"method","location":"api/types/#StatsAPI.stderror-Tuple{ERGMResult}","page":"Types","text":"stderror(result::ERGMResult) -> Vector{Float64}\n\nStandard errors of the coefficient estimates (a method of StatsAPI.stderror); their type is recorded in result.se_type (:hessian, :bootstrap, or :mcmc — the last includes the Monte-Carlo component mcmc_se).\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges(), NodeCov(:wealth)])\nround.(stderror(fit); digits=3)      # [0.536, 0.005]\n\n\n\n\n\n","title":"StatsAPI.stderror"},{"category":"method","location":"api/types/#StatsAPI.vcov-Tuple{ERGMResult}","page":"Types","text":"vcov(result::ERGMResult) -> Matrix{Float64}\n\nVariance-covariance matrix of the coefficient estimates (a method of StatsAPI.vcov); sqrt.(diag(vcov(fit))) == stderror(fit).\n\nExample\n\nusing ERGM, LinearAlgebra\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges(), NodeCov(:wealth)])\nsize(vcov(fit))                                   # (2, 2)\nsqrt.(diag(vcov(fit))) ≈ stderror(fit)            # true\n\n\n\n\n\n","title":"StatsAPI.vcov"},{"category":"method","location":"api/types/#StatsAPI.confint-Tuple{ERGMResult}","page":"Types","text":"confint(result::ERGMResult; level=0.95) -> Matrix{Float64}\n\nNormal-theory (Wald) confidence limits θ̂ ± z_{(1+level)/2} · se, one row per coefficient with the lower limit in column 1 and the upper in column 2 (a method of StatsAPI.confint). The standard errors are the ones the fit reports — result.se_type says whether they are inverse-Hessian, parametric-bootstrap or MCMC-based — so for an MPLE fit of a dyad-dependent model the intervals inherit the anticonservative pseudo-likelihood SEs (see mple).\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges(), NodeCov(:wealth)])\nci = confint(fit)               # 2×2\nall(ci[:, 1] .< coef(fit) .< ci[:, 2])   # true\nconfint(fit; level=0.9)         # narrower\n\n\n\n\n\n","title":"StatsAPI.confint"},{"category":"method","location":"api/types/#StatsAPI.coeftable-Tuple{ERGMResult}","page":"Types","text":"coeftable(result::ERGMResult) -> Networks.CoefficientTable\n\nThe R-style coefficient table (Estimate, Std.Error, z value, Pr(>|z|)) as an inspectable Networks.CoefficientTable — exactly the table show(result) prints, built from the same vectors (a method of StatsAPI.coeftable). Rows can be read by index or by term name.\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges(), NodeCov(:wealth)])\ntbl = coeftable(fit)\ntbl[\"edges\"].estimate == coef(fit)[1]     # true\ntbl[2].p_value == fit.p_values[2]         # true\n\n\n\n\n\n","title":"StatsAPI.coeftable"},{"category":"section","location":"api/terms/#Terms-API-Reference","page":"Terms","text":"This page documents all ERGM terms available in ERGM.jl.","title":"Terms API Reference"},{"category":"section","location":"api/terms/#Structural-Terms","page":"Terms","text":"Terms based purely on network structure.","title":"Structural Terms"},{"category":"section","location":"api/terms/#Nodal-Attribute-Terms","page":"Terms","text":"Terms incorporating vertex-level attributes.","title":"Nodal Attribute Terms"},{"category":"section","location":"api/terms/#Dyadic-Terms","page":"Terms","text":"Terms based on dyad-level covariates.","title":"Dyadic Terms"},{"category":"section","location":"api/terms/#Term-Traits","page":"Terms","text":"The public protocol by which a term — ERGM.jl's own or a third party's — declares what it needs and where it belongs. ERGM.jl acts on the declarations when an ERGMModel is constructed: a term whose declared vertex/edge attributes are absent from the network, or whose direction requirement the network does not meet, is rejected with an ArgumentError instead of silently contributing an all-zero design column.\n\nTwo further traits complete the protocol:\n\nis_dyad_dependent — whether the change statistic reads other dyads (conservative default true); it drives the pseudo-likelihood caveat and the MCMLE bridge reference.\nNetworks.supports_missing — whether the term's statistic honours the missing-dyad mask, i.e. is invariant to the face value of a dyad marked with set_missing_dyad! (default false, the honest answer for every built-in term: they count masked dyads at face value, and the principled missing-data treatment lives in the estimator — see mple).\n\nA third-party term declares them by adding methods:\n\nusing ERGM\nusing Networks\n\nstruct RichClub <: AbstractERGMTerm\n    attr::Symbol\nend\n\nERGM.name(t::RichClub) = \"richclub.$(t.attr)\"\n# ... plus compute(t, net) and change_stat(t, net, i, j)\n\nERGM.required_vertex_attributes(t::RichClub) = (t.attr,)\nERGM.requires_directed(::RichClub)           = true\nERGM.is_dyad_dependent(::RichClub)           = true\nNetworks.supports_missing(::RichClub)        = false\n\nERGMUserterms.validate_term exercises every one of these declarations against a network; ERGMUserterms.jl/examples/MyTermPackage is a complete package template built on them.\n\nnote: Pre-0.2 private names\nThe traits were internal before v0.2 (ERGM._vertex_attribute, ERGM._requires_directed, ERGM._requires_undirected). The private names remain as aliases of the same generics, so downstream methods declared against them (TERGM.jl's ERGM._requires_directed(::Delrecip) = true) keep working, but new code should use the public names.","title":"Term Traits"},{"category":"type","location":"api/terms/#ERGM.Edges","page":"Terms","text":"Edges <: StructuralTerm\n\nThe number of edges in the network (statnet edges, coefficient name \"edges\"). The most basic ERGM term, analogous to an intercept: alone it is the Bernoulli model, whose MLE is the logit of the density.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(Edges(), net)                        # 20.0\nround(coef(fit_ergm(net, Edges()))[1]; digits=4)   # -1.6094 == log(20/100)\n\n\n\n\n\n","title":"ERGM.Edges"},{"category":"type","location":"api/terms/#ERGM.Mutual","page":"Terms","text":"Mutual <: StructuralTerm\n\nThe number of mutual (reciprocated) dyads (statnet mutual). Directed networks only: requires_directed(Mutual()) == true, so a model with it on an undirected network is refused.\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 1); add_edge!(net, 2, 3)\ncompute(Mutual(), net)              # 1.0\nchange_stat(Mutual(), net, 3, 2)    # 1.0 — 2→3 exists, so 3→2 would reciprocate it\n\n\n\n\n\n","title":"ERGM.Mutual"},{"category":"type","location":"api/terms/#ERGM.Triangle","page":"Terms","text":"Triangle <: StructuralTerm\n\nThe number of triangles in the network.\n\nFor undirected networks this is the usual triangle count. For directed networks it follows the statnet triangle definition: the number of transitive triples plus the number of cyclic triples (ttriple + ctriple). Dyad-dependent: an MPLE fit with it prints the pseudo-likelihood caveat; prefer method=:mcmle. GWESP is the usual well-behaved alternative.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(Triangle(), net)              # 3.0\nchange_stat(Triangle(), net, 6, 7)    # 1.0 — Barbadori and Guadagni share Medici\n\n\n\n\n\n","title":"ERGM.Triangle"},{"category":"type","location":"api/terms/#ERGM.Kstar","page":"Terms","text":"Kstar <: StructuralTerm\nKstar(k)\n\nThe number of k-stars in the network (statnet's kstar(k), coefficient name \"kstar<k>\"). A k-star is a vertex together with k of its neighbours, so the statistic is Σᵥ C(deg(v), k).\n\nUndirected networks only, as in R ergm (kstar \"may not be used with networks with directed==TRUE\"). On a directed network the model is refused at construction with an ArgumentError, and so are compute/change_stat; use OStar / IStar (statnet's ostar/istar) instead.\n\nwarning: Changed in 0.2\nBefore 0.2 a directed network silently received out-stars under the label kstar<k>.\n\nFields\n\nk::Int: Star size (number of spokes), at least 2\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(Kstar(2), net)     # 47.0 — Σᵥ C(deg(v), 2)\nname(Kstar(3))             # \"kstar3\"\n\n\n\n\n\n","title":"ERGM.Kstar"},{"category":"type","location":"api/terms/#ERGM.OStar","page":"Terms","text":"OStar <: StructuralTerm\nOStar(k)\n\nThe number of out-k-stars in a directed network (statnet's ostar(k), coefficient name \"ostar<k>\"): a vertex together with k of its out-neighbours, so the statistic is Σᵥ C(outdeg(v), k). Directed networks only — on an undirected network use Kstar.\n\nAdding the arc i→j only changes i's out-degree, so the change statistic is C(outdeg⁻(i)+1, k) − C(outdeg⁻(i), k) with outdeg⁻ the out-degree in the baseline state without the arc — an O(1) computation.\n\nFields\n\nk::Int: Star size (number of out-spokes), at least 2\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(OStar(2), net)     # 1.0 — only vertex 1 has out-degree 2\ncompute(IStar(2), net)     # 1.0 — only vertex 3 has in-degree 2\nname(OStar(2))             # \"ostar2\"\n\n\n\n\n\n","title":"ERGM.OStar"},{"category":"type","location":"api/terms/#ERGM.IStar","page":"Terms","text":"IStar <: StructuralTerm\nIStar(k)\n\nThe number of in-k-stars in a directed network (statnet's istar(k), coefficient name \"istar<k>\"): a vertex together with k of its in-neighbours, so the statistic is Σᵥ C(indeg(v), k). Directed networks only — on an undirected network use Kstar.\n\nAdding the arc i→j only changes j's in-degree, so the change statistic is C(indeg⁻(j)+1, k) − C(indeg⁻(j), k) with indeg⁻ the in-degree in the baseline state without the arc — an O(1) computation.\n\nFields\n\nk::Int: Star size (number of in-spokes), at least 2\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(IStar(2), net)     # 1.0 — vertex 3 receives two arcs\ncompute(OStar(2), net)     # 0.0\nname(IStar(3))             # \"istar3\"\n\n\n\n\n\n","title":"ERGM.IStar"},{"category":"type","location":"api/terms/#ERGM.TwoPath","page":"Terms","text":"TwoPath <: StructuralTerm\n\nThe number of two-paths in the network.\n\nFor directed networks: pairs of edges (h → v, v → k) with h ≠ k (statnet's twopath/m2star). For undirected networks: the number of 2-stars, Σᵥ C(deg(v), 2) — the same statistic as Kstar(2).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(TwoPath(), net) == compute(Kstar(2), net)   # true (undirected)\ncompute(TwoPath(), net)                              # 47.0\n\n\n\n\n\n","title":"ERGM.TwoPath"},{"category":"type","location":"api/terms/#ERGM.GWESP","page":"Terms","text":"GWESP <: StructuralTerm\nGWESP(decay=0.5; type=:OTP)\n\nGeometrically Weighted Edgewise Shared Partners with fixed decay (statnet's gwesp(decay, fixed=TRUE)).\n\nFor undirected networks, shared partners of an edge (i,j) are the common neighbors of i and j; type is ignored.\n\nFor directed networks, type selects the shared-partner definition for each directed edge i→j, matching statnet's dgwesp types (the shared partner k is always distinct from i and j):\n\n:OTP — outgoing two-path, k with i→k→j (statnet's default for directed gwesp; also the default here)\n:ITP — incoming two-path, k with j→k→i\n:OSP — outgoing shared partner, k with i→k and j→k\n:ISP — incoming shared partner, k with k→i and k→j\n:union — k adjacent to both i and j in either direction. This is not a statnet type; it preserves this package's historical (pre-0.2) directed GWESP behavior and is named \"gwesp.union.fixed.<decay>\" so it can never be confused with statnet's OTP-based \"gwesp.fixed.<decay>\".\n\nThe decay must be non-negative; GWESP(0.0) is statnet's gwesp(0, fixed=TRUE) — the number of edges with at least one shared partner. Coefficient names follow R's labels exactly: an integer-valued decay prints without a decimal point (\"gwesp.fixed.0\", \"gwesp.fixed.1\"), a fractional one as is (\"gwesp.fixed.0.5\").\n\nThe label depends on the network's directedness, as in R. On an undirected network the coefficient is \"gwesp.fixed.<decay>\" (whatever type, which is ignored there). On a directed network R ergm 4.12.0 names every type — the default included — \"gwesp.<type>.fixed.<decay>\" (gwesp.OTP.fixed.0.5), and so does ERGM.jl wherever the network is known: ERGMModel/fit_ergm (fit.model.formula.terms.names), summary_stats and the two-argument name(term, net). The one-argument name(term), which cannot see a network, returns the undirected label for :OTP and \"gwesp.<type>.fixed.<decay>\" for the other types; :union is always \"gwesp.union.fixed.<decay>\".\n\nFields\n\ndecay::Float64: Decay parameter, ≥ 0 (higher = less downweighting)\ntype::Symbol: Directed shared-partner type (see above)\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(GWESP(0.5), net)   # 8.3934... (statnet: summary(flomarriage ~ gwesp(0.5, fixed=TRUE)))\ncompute(GWESP(0.0), net)   # 8.0 — edges with ≥ 1 shared partner\nname(GWESP(0.0))           # \"gwesp.fixed.0\"\nname(GWESP(0.5), net)      # \"gwesp.fixed.0.5\" — undirected label\ndnet = network(4; directed=true); add_edge!(dnet, 1, 2); add_edge!(dnet, 2, 3)\nname(GWESP(0.5), dnet)     # \"gwesp.OTP.fixed.0.5\" — R's directed label\n\n\n\n\n\n","title":"ERGM.GWESP"},{"category":"type","location":"api/terms/#ERGM.GWDSP","page":"Terms","text":"GWDSP <: StructuralTerm\nGWDSP(decay=0.5; type=:OTP)\n\nGeometrically Weighted Dyadwise Shared Partners with fixed decay (statnet's gwdsp(decay, fixed=TRUE)): the shared-partner analogue of GWESP summed over all dyads — tied or not — instead of over edges only.\n\nFor undirected networks the statistic sums eᵅ(1 − (1 − e⁻ᵅ)^dsp(i,j)) over all unordered dyads {i,j}, where dsp(i,j) is the number of common neighbors of i and j; type is ignored.\n\nFor directed networks, type selects the shared-partner definition, matching statnet's dgwdsp types (the shared partner k is always distinct from i and j). As in statnet's C implementation (ddsp), every directed type is summed over all ordered dyads (i, j), i ≠ j — including the symmetric :OSP/:ISP counts, so each unordered pair {i, j} contributes twice (R ergm 4.12.0 on samplike: gwdsp.OSP.fixed.0.5 = 316.085, gwdsp.ISP.fixed.0.5 = 229.136; pinned by the provenanced fixture):\n\n:OTP — outgoing two-path, k with i→k→j (statnet's default for directed networks; also the default here). For dyadwise shared partners :OTP and :ITP yield the same statistic.\n:ITP — incoming two-path, k with j→k→i\n:OSP — outgoing shared partner, k with i→k and j→k (symmetric in i and j, hence counted for both (i, j) and (j, i))\n:ISP — incoming shared partner, k with k→i and k→j (likewise)\n:union — k adjacent to both i and j in either direction, summed over unordered dyads. This is not a statnet type; it mirrors GWESP's :union and is named \"gwdsp.union.fixed.<decay>\".\n\nThe decay must be non-negative; GWDSP(0.0) is statnet's gwdsp(0, fixed=TRUE) — the number of dyads with at least one shared partner. Coefficient names follow R's labels (\"gwdsp.fixed.0\", \"gwdsp.fixed.0.5\"), and — exactly as for GWESP — the label is direction-aware wherever the network is known: \"gwdsp.fixed.<decay>\" on an undirected network, \"gwdsp.<type>.fixed.<decay>\" (gwdsp.OTP.fixed.0.5 for the default) on a directed one, in ERGMModel, summary_stats and name(term, net).\n\nFields\n\ndecay::Float64: Decay parameter, ≥ 0 (higher = less downweighting)\ntype::Symbol: Directed shared-partner type (see above)\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(GWDSP(0.0), net)   # 43.0 — dyads with ≥ 1 shared partner\nname(GWDSP(0.0))           # \"gwdsp.fixed.0\"\n\n\n\n\n\n","title":"ERGM.GWDSP"},{"category":"type","location":"api/terms/#ERGM.GWDegree","page":"Terms","text":"GWDegree <: StructuralTerm\nGWDegree(decay=0.5)\n\nGeometrically Weighted Degree distribution with fixed decay (statnet's gwdegree(decay, fixed=TRUE), coefficient name \"gwdeg.fixed.<decay>\" — R's label, with an integer-valued decay printed without a decimal point).\n\nUndirected networks only, as in R ergm (gwdegree \"may not be used with networks with directed==TRUE\"). On a directed network the model is refused at construction with an ArgumentError, and so are compute/change_stat; use GWODegree / GWIDegree instead.\n\nThe decay must be non-negative; GWDegree(0.0) is statnet's gwdegree(0, fixed=TRUE) — the number of non-isolated vertices.\n\nwarning: Changed in 0.2\nBefore 0.2 a directed network silently received the out-degree statistic under the label gwdegree.fixed.<decay>, and the undirected coefficient was labelled gwdegree.fixed.<decay> where R writes gwdeg.fixed.<decay>.\n\nFields\n\ndecay::Float64: Decay parameter, ≥ 0\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(GWDegree(0.0), net)   # 15.0 — every family but Pucci has a tie\nname(GWDegree(0.5))           # \"gwdeg.fixed.0.5\"\nname(GWDegree(1.0))           # \"gwdeg.fixed.1\"\n\n\n\n\n\n","title":"ERGM.GWDegree"},{"category":"type","location":"api/terms/#ERGM.GWIDegree","page":"Terms","text":"GWIDegree <: StructuralTerm\nGWIDegree(decay=0.5)\n\nGeometrically Weighted In-Degree distribution with fixed decay (statnet's gwidegree(decay, fixed=TRUE)). Only defined for directed networks; the coefficient is named \"gwideg.fixed.<decay>\" as in statnet (an integer-valued decay prints without a decimal point). The decay must be non-negative; GWIDegree(0.0) counts the vertices with in-degree ≥ 1.\n\nFields\n\ndecay::Float64: Decay parameter, ≥ 0\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(GWIDegree(0.0), net)   # 2.0 — vertices 2 and 3 receive arcs\nname(GWIDegree(0.5))           # \"gwideg.fixed.0.5\"\n\n\n\n\n\n","title":"ERGM.GWIDegree"},{"category":"type","location":"api/terms/#ERGM.GWODegree","page":"Terms","text":"GWODegree <: StructuralTerm\nGWODegree(decay=0.5)\n\nGeometrically Weighted Out-Degree distribution with fixed decay (statnet's gwodegree(decay, fixed=TRUE)). Only defined for directed networks; the coefficient is named \"gwodeg.fixed.<decay>\" as in statnet (an integer-valued decay prints without a decimal point). The decay must be non-negative; GWODegree(0.0) counts the vertices with out-degree ≥ 1.\n\nFields\n\ndecay::Float64: Decay parameter, ≥ 0\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(GWODegree(0.0), net)   # 2.0 — vertices 1 and 2 send arcs\nname(GWODegree(1.0))           # \"gwodeg.fixed.1\"\n\n\n\n\n\n","title":"ERGM.GWODegree"},{"category":"type","location":"api/terms/#ERGM.Degree","page":"Terms","text":"Degree <: StructuralTerm\nDegree(d)\nDegree(ds)\n\nThe number of vertices with degree exactly d (statnet's degree(d), coefficient name \"degree<d>\"). Only defined for undirected networks, as in R ergm — use IDegree / ODegree for directed networks (the model is refused at construction otherwise, and so are compute/change_stat).\n\nAs in statnet, a vector (or range) of degrees is one term that expands into one statistic per degree when the model is built: Degree(0:2) in a term list becomes degree0, degree1, degree2 in the fit, exactly like a multi-level NodeFactor. The unexpanded multi-degree term is a specification, not a statistic — compute(Degree(0:2), net) throws; use summary_stats or build the model.\n\nfit_ergm(net, [Edges(), Degree(0:2)])   # coefficients edges, degree0, degree1, degree2\n\nFields\n\ndegrees::Vector{Int}: The degree(s) counted\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(Degree(0), net)                    # 1.0 — Pucci is isolated\nsummary_stats(net, [Degree(0:2)])          # (degree0 = 1.0, degree1 = 4.0, degree2 = 2.0)\nname(Degree(1))                            # \"degree1\"\nDegree(0:2) == Degree([0, 1, 2])           # true\n\n\n\n\n\n","title":"ERGM.Degree"},{"category":"type","location":"api/terms/#ERGM.IDegree","page":"Terms","text":"IDegree <: StructuralTerm\nIDegree(d)\nIDegree(ds)\n\nThe number of vertices with in-degree exactly d (statnet's idegree(d), coefficient name \"idegree<d>\"). Only defined for directed networks.\n\nAs in statnet, a vector (or range) of degrees is one term that expands into one statistic per degree when the model is built: IDegree(0:2) becomes idegree0, idegree1, idegree2 (see Degree); the unexpanded multi-degree term is not a statistic and compute refuses it.\n\nFields\n\ndegrees::Vector{Int}: The in-degree(s) counted\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(IDegree(0), net)                   # 1.0 — vertex 1 receives nothing\nsummary_stats(net, [IDegree(0:2)])         # (idegree0 = 1.0, idegree1 = 1.0, idegree2 = 1.0)\n\n\n\n\n\n","title":"ERGM.IDegree"},{"category":"type","location":"api/terms/#ERGM.ODegree","page":"Terms","text":"ODegree <: StructuralTerm\nODegree(d)\nODegree(ds)\n\nThe number of vertices with out-degree exactly d (statnet's odegree(d), coefficient name \"odegree<d>\"). Only defined for directed networks.\n\nAs in statnet, a vector (or range) of degrees is one term that expands into one statistic per degree when the model is built: ODegree(0:2) becomes odegree0, odegree1, odegree2 (see Degree); the unexpanded multi-degree term is not a statistic and compute refuses it.\n\nFields\n\ndegrees::Vector{Int}: The out-degree(s) counted\n\nExample\n\nusing ERGM\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 1, 3); add_edge!(net, 2, 3)\ncompute(ODegree(2), net)                   # 1.0 — vertex 1 sends two arcs\nsummary_stats(net, [ODegree(0:2)])         # (odegree0 = 1.0, odegree1 = 1.0, odegree2 = 1.0)\n\n\n\n\n\n","title":"ERGM.ODegree"},{"category":"type","location":"api/terms/#ERGM.NodeFactor","page":"Terms","text":"NodeFactor <: NodalTerm\nNodeFactor(attr; levels=nothing, base=1, level=nothing)\n\nMain effect of a categorical vertex attribute (R ergm's nodefactor): for each included attribute level, the number of times a vertex with that level appears as an endpoint of an edge. As in statnet, an edge within the level counts twice, and one statistic is produced per included level (named \"nodefactor.<attr>.<level>\"); the levels are resolved from the network at model construction, where the term expands into its per-level statistics.\n\nWhich levels are included follows statnet:\n\nBy default the first level is dropped (base=1): the levels are the sorted distinct attribute values, and the lowest acts as the reference category. Including all levels would make the term collinear with Edges.\nbase gives the indices (into the sorted levels) to drop; base=0 keeps every level.\nlevels gives the included attribute values explicitly, in order, overriding base.\nlevel=x constructs a single-level term directly (one statistic, no expansion), equivalent to levels=[x].\n\nwarning: Changed in 0.2\nNodeFactor(attr) previously produced a single statistic counting the endpoint appearances of every vertex with the attribute — collinear with Edges by construction. It now matches statnet: one statistic per attribute level, with the first (sorted) level dropped as the reference. Pass base=0 to keep all levels.\n\nBefore expansion (e.g. compute on a term that has not been through model construction), a multi-level term evaluates to the sum of its per-level statistics.\n\nFields\n\nattr::Symbol: Vertex attribute name\nlevel::Any: Single level counted (nothing for a multi-level term)\nlevels::Union{Nothing, Vector{Any}}: Explicit included levels\nbase::Vector{Int}: Sorted-level indices dropped (default [1])\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nrich = Dict(v => (w > 40 ? \"rich\" : \"poor\") for (v, w) in get_vertex_attribute(net, :wealth))\nset_vertex_attribute!(net, :class, rich)\nfit = fit_ergm(net, [Edges(), NodeFactor(:class)])\nfit.model.formula.terms.names      # [\"edges\", \"nodefactor.class.rich\"] — \"poor\" is the reference\ncompute(NodeFactor(:class; level=\"rich\"), net)   # 20.0 endpoint appearances\n\n\n\n\n\n","title":"ERGM.NodeFactor"},{"category":"type","location":"api/terms/#ERGM.NodeCov","page":"Terms","text":"NodeCov <: NodalTerm\nNodeCov(attr; transform=:none)\n\nMain effect of a continuous vertex attribute (R ergm's nodecov): the sum of the attribute values of both endpoints over the edges, named \"nodecov.<attr>\".\n\nThe attribute must be set on every vertex: as in statnet (which refuses NA attribute values), ERGMModel throws an ArgumentError naming the term, the attribute and the vertices without a value, and a direct compute/change_stat on incomplete data throws rather than zero-filling.\n\nFields\n\nattr::Symbol: Vertex attribute name\ntransform::Symbol: Optional transform of each vertex's value before summing — :none (default), :log (log(x + 1)) or :sqrt. Any other symbol is an ArgumentError (it used to fall back silently to :none).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(NodeCov(:wealth), net)             # 2168.0\nname(NodeCov(:wealth; transform=:log))     # \"nodecov.wealth\"\n\n\n\n\n\n","title":"ERGM.NodeCov"},{"category":"type","location":"api/terms/#ERGM.NodeMatch","page":"Terms","text":"NodeMatch <: NodalTerm\nNodeMatch(attr; diff=false, level=nothing)\n\nHomophily effect: the number of edges whose endpoints have matching values of the vertex attribute attr (R ergm's nodematch).\n\nWith diff=false (uniform homophily, the default) this is a single statistic counting all matched edges, named \"nodematch.<attr>\".\n\nWith diff=true (differential homophily, matching R's nodematch(attr, diff=TRUE)) there is one statistic per attribute level, each counting only the edges matched on that level and named \"nodematch.<attr>.<level>\". Since this package's term system is one-statistic-per-term (like NodeFactor), construct one NodeMatch per level:\n\nlevels = sort(unique(values(get_vertex_attribute(net, :group))))\nterms = [NodeMatch(:group; diff=true, level=l) for l in levels]\n\nwarning: Changed in 0.2\ndiff=true previously counted mismatched edges — a different model from R's nodematch(diff=TRUE) under the same keyword. That statistic is now available as NodeMismatch.\n\nFields\n\nattr::Symbol: Vertex attribute name\ndiff::Bool: Differential (per-level) homophily as in R\nlevel::Any: The attribute level counted (required when diff=true)\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nrich = Dict(v => (w > 40 ? \"rich\" : \"poor\") for (v, w) in get_vertex_attribute(net, :wealth))\nset_vertex_attribute!(net, :class, rich)\ncompute(NodeMatch(:class), net)                          # 10.0 within-class ties\nlevels = sort(unique(values(rich)))                      # [\"poor\", \"rich\"]\nterms = [NodeMatch(:class; diff=true, level=l) for l in levels]\nsum(compute(t, net) for t in terms)                      # 10.0\n\n\n\n\n\n","title":"ERGM.NodeMatch"},{"category":"type","location":"api/terms/#ERGM.NodeMismatch","page":"Terms","text":"NodeMismatch <: NodalTerm\nNodeMismatch(attr)\n\nHeterophily effect: the number of edges whose endpoints have different (non-missing) values of the vertex attribute attr. Named \"nodemismatch.<attr>\".\n\nThis is the statistic that NodeMatch(attr; diff=true) computed before 0.2; it has no R ergm equivalent under the nodematch name (R's diff=TRUE is per-level homophily — see NodeMatch).\n\nFields\n\nattr::Symbol: Vertex attribute name\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nrich = Dict(v => (w > 40 ? \"rich\" : \"poor\") for (v, w) in get_vertex_attribute(net, :wealth))\nset_vertex_attribute!(net, :class, rich)\ncompute(NodeMismatch(:class), net) + compute(NodeMatch(:class), net) == 20.0   # true\n\n\n\n\n\n","title":"ERGM.NodeMismatch"},{"category":"type","location":"api/terms/#ERGM.NodeMix","page":"Terms","text":"NodeMix <: NodalTerm\nNodeMix(attr; levels=nothing, levels2=-1)\nNodeMix(attr, l1, l2)\n\nMixing-matrix cell counts for a categorical vertex attribute (R ergm's nodemix): one statistic per mixing cell, counting the edges whose endpoint levels fall in that cell, named \"mix.<attr>.<l1>.<l2>\".\n\nThe cells follow statnet's level ordering. With sorted levels u₁ < u₂ < …:\n\nundirected networks use the unordered cells (uᵢ, uⱼ) with i ≤ j, in column-major order: (u₁,u₁), (u₁,u₂), (u₂,u₂), (u₁,u₃), …\ndirected networks use all ordered (tail-level, head-level) cells in column-major order: (u₁,u₁), (u₂,u₁), …, (u₁,u₂), …\n\nand statnet's reference handling: by default the first cell is dropped (levels2=-1), acting as the reference category. levels2 selects cells by index into the ordered cell list — all-negative values drop those cells, all-positive values keep exactly those cells (in the given order), and levels2=0 keeps every cell. levels restricts (and orders) the attribute levels used to build the cells.\n\nLike NodeFactor, the cells are resolved from the network at model construction, where the term expands into its per-cell statistics; NodeMix(attr, l1, l2) constructs a single-cell term directly. Before expansion, a multi-cell term evaluates to the sum of its selected cells.\n\nFields\n\nattr::Symbol: Vertex attribute name\nl1::Any, l2::Any: The cell's levels (nothing for a multi-cell term); for directed networks l1 is the tail (sender) level and l2 the head (receiver) level\nlevels::Union{Nothing, Vector{Any}}: Levels used to build the cells\nlevels2::Vector{Int}: Cell selection (see above)\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nrich = Dict(v => (w > 40 ? \"rich\" : \"poor\") for (v, w) in get_vertex_attribute(net, :wealth))\nset_vertex_attribute!(net, :class, rich)\nfit = fit_ergm(net, [Edges(), NodeMix(:class)])\nfit.model.formula.terms.names   # [\"edges\", \"mix.class.poor.rich\", \"mix.class.rich.rich\"]\ncompute(NodeMix(:class, \"poor\", \"rich\"), net)   # 10.0 cross-class ties\n\n\n\n\n\n","title":"ERGM.NodeMix"},{"category":"type","location":"api/terms/#ERGM.AbsDiff","page":"Terms","text":"AbsDiff <: NodalTerm\nAbsDiff(attr; pow=1)\n\nAbsolute difference in a continuous attribute between edge endpoints (R ergm's absdiff): Σ_edges |xᵢ − xⱼ|^pow, named \"absdiff.<attr>\" for pow = 1 and \"absdiff<pow>.<attr>\" otherwise (absdiff2.wealth, absdiff0.5.wealth — R's labels, so two powers of one attribute in a formula print distinct names; pinned by the provenanced fixture).\n\nThe attribute must be set on every vertex: as in statnet, ERGMModel throws an ArgumentError naming the vertices without a value, and a direct compute/change_stat on incomplete data throws rather than zero-filling.\n\nFields\n\nattr::Symbol: Vertex attribute name\npow::Float64: Power to raise the difference to (default 1; any Real is accepted and stored as a Float64, so R's absdiff(\"wealth\", pow=2) is AbsDiff(:wealth; pow=2))\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\ncompute(AbsDiff(:wealth), net)     # 1146.0\ncompute(AbsDiff(:wealth; pow=2), net)   # 91570.0 (R: summary(flomarriage ~ absdiff(\"wealth\", pow=2)))\nname(AbsDiff(:wealth; pow=2))      # \"absdiff2.wealth\"\n\n\n\n\n\n","title":"ERGM.AbsDiff"},{"category":"type","location":"api/terms/#ERGM.EdgeCov","page":"Terms","text":"EdgeCov <: DyadicTerm\n\nDyad-level covariate effect (statnet edgecov): the sum of the covariate over the edges present. Built from an n×n matrix (EdgeCov(M; name=\"edgecov\"); undirected networks read the (min, max) entry) or from an edge attribute (EdgeCov(net, :attr)). Dyad-independent.\n\nThe matrix must be n×n for the network it is used on: ERGMModel/ fit_ergm, compute and change_stat all throw an ArgumentError naming the sizes otherwise (a matrix built for another network is the commonest edgecov slip, and a BoundsError would not say so).\n\nFields\n\ncovariate::Matrix{Float64}: n×n matrix of dyad covariates\nname_str::String: Name for this term\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nw = get_vertex_attribute(net, :wealth)\nM = [Float64(abs(w[i] - w[j])) for i in 1:16, j in 1:16]      # wealth gap per dyad\nterm = EdgeCov(M; name=\"edgecov.wealthgap\")\ncompute(term, net) == compute(AbsDiff(:wealth), net)          # true\nname(term)                                                    # \"edgecov.wealthgap\"\n\n\n\n\n\n","title":"ERGM.EdgeCov"},{"category":"function","location":"api/terms/#ERGM.required_vertex_attributes","page":"Terms","text":"required_vertex_attributes(term::AbstractERGMTerm) -> Tuple{Vararg{Symbol}}\n\nThe vertex attributes the term needs on the network, as a tuple of symbols (empty for terms that read none — the default).\n\nA term whose statistic reads a vertex attribute must declare it. ERGM.jl checks the declaration against list_vertex_attributes(net) when an ERGMModel is built and throws an ArgumentError if the attribute is absent, because the alternative is silent nonsense: get_vertex_attribute returns an empty Dict for an unknown attribute, so an undeclared attribute-based term would quietly evaluate to an all-zero design column and a meaningless coefficient.\n\nDeclared attributes are also what makes a term eligible for attribute materialization (dense typed snapshots; ERGM's own nodal terms only).\n\nExample\n\nstruct Homophily <: AbstractERGMTerm\n    attr::Symbol\nend\nERGM.required_vertex_attributes(t::Homophily) = (t.attr,)\n\nSee also required_edge_attributes, requires_directed, is_dyad_dependent.\n\n\n\n\n\n","title":"ERGM.required_vertex_attributes"},{"category":"function","location":"api/terms/#ERGM.required_edge_attributes","page":"Terms","text":"required_edge_attributes(term::AbstractERGMTerm) -> Tuple{Vararg{Symbol}}\n\nThe edge (dyad) attributes the term needs on the network, as a tuple of symbols (empty by default). Validated against list_edge_attributes(net) at model construction exactly like required_vertex_attributes.\n\nDeclare an edge attribute only when its absence is an error. A term that falls back to a default weight for edges without the attribute (as ERGMUserterms.WeightedEdges does) genuinely does not require it and must not declare it — otherwise a perfectly meaningful model would be rejected.\n\nNone of ERGM.jl's built-in terms declare one: EdgeCov snapshots the dyadic covariate into a matrix at construction time, so by the time the model is built the attribute is no longer needed.\n\nExample\n\nusing ERGM\nrequired_edge_attributes(EdgeCov(zeros(3, 3)))        # ()\nstruct Weighted <: AbstractERGMTerm end\nERGM.required_edge_attributes(::Weighted) = (:weight,)\nrequired_edge_attributes(Weighted())                   # (:weight,)\n\n\n\n\n\n","title":"ERGM.required_edge_attributes"},{"category":"function","location":"api/terms/#ERGM.requires_directed","page":"Terms","text":"requires_directed(term::AbstractERGMTerm) -> Bool\n\nWhether the term is only defined for directed networks (false by default). Model construction rejects such a term on an undirected network with an ArgumentError, as R ergm does: there its statistic would be a structural zero or a silent duplicate of another term, not a model.\n\nBuilt-in terms declaring true: Mutual, IStar, OStar, IDegree, ODegree, GWIDegree, GWODegree. TERGM.jl declares it for Delrecip.\n\nSee also requires_undirected.\n\nExample\n\nusing ERGM\nrequires_directed(Mutual())     # true\nrequires_directed(Edges())      # false\ntry\n    ERGMModel(ERGMFormula([Mutual()]), load_dataset(:florentine_marriage))\ncatch e\n    e isa ArgumentError           # true — refused on an undirected network\nend\n\n\n\n\n\n","title":"ERGM.requires_directed"},{"category":"function","location":"api/terms/#ERGM.requires_undirected","page":"Terms","text":"requires_undirected(term::AbstractERGMTerm) -> Bool\n\nWhether the term is only defined for undirected networks (false by default). Model construction rejects such a term on a directed network with an ArgumentError, pointing at the in-/out- variant to use instead.\n\nBuilt-in terms declaring true, exactly the terms R ergm refuses on a directed network: Degree (use IDegree/ODegree), Kstar (use IStar/OStar) and GWDegree (use GWIDegree/GWODegree). These three also refuse a directed network in compute/change_stat themselves, so a caller bypassing ERGMModel (a variant lifting a term to a layer or a panel) cannot silently receive the out-degree statistic under the undirected label, as happened before 0.2.\n\nSee also requires_directed.\n\nExample\n\nusing ERGM\nrequires_undirected(Kstar(2))     # true\nrequires_undirected(OStar(2))     # false — the directed counterpart\n\n\n\n\n\n","title":"ERGM.requires_undirected"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"Prepare a binary network, choose terms, fit a model, and check simulated networks against the observation. The examples distinguish a quick pseudo-likelihood fit from MCMC-MLE, whose convergence and simulation diagnostics are part of interpreting the result.\n\nnote: Before you begin\nEstimation supports loop-free, one-mode binary networks. Curved and bipartite ERGMs are not implemented. MPLE is a conditional approximation for dyad-dependent terms; use MCMC-MLE and inspect its convergence diagnostics when that is the intended estimator. Missing dyads require the estimator-specific policies described in the estimation guide.","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>\n\nRun the blocks below in order in that environment. They build on variables from earlier steps; stochastic examples use seeded random number generators where shown.","title":"Installation"},{"category":"section","location":"getting_started/#Basic-Workflow","page":"Getting Started","text":"The typical ERGM.jl workflow consists of four steps:\n\nCreate a network - Prepare your network data\nDefine model terms - Choose which network statistics to include\nFit the model - Estimate coefficients via MPLE or MCMLE\nAssess the fit - Simulate networks and run goodness-of-fit tests","title":"Basic Workflow"},{"category":"section","location":"getting_started/#Step-1:-Create-a-Network","page":"Getting Started","text":"Networks are represented using the Network type from Networks.jl:\n\nusing Networks, ERGM\nusing Random\n\n# Create an undirected network with 10 nodes\nnet = network(10; directed=false)\n\n# Add edges\nadd_edge!(net, 1, 2)\nadd_edge!(net, 1, 3)\nadd_edge!(net, 2, 3)\nadd_edge!(net, 3, 4)\nadd_edge!(net, 4, 5)\n\nprintln(\"Nodes: \", nv(net))   # 10\nprintln(\"Edges: \", ne(net))   # 5","title":"Step 1: Create a Network"},{"category":"section","location":"getting_started/#Setting-Vertex-Attributes","page":"Getting Started","text":"Vertex attributes are used by nodal terms like NodeMatch and NodeCov:\n\n# Categorical attribute\nset_vertex_attribute!(net, :gender, Dict(\n    1 => \"M\", 2 => \"F\", 3 => \"M\", 4 => \"F\", 5 => \"M\",\n    6 => \"F\", 7 => \"M\", 8 => \"F\", 9 => \"M\", 10 => \"F\"\n))\n\n# Continuous attribute\nset_vertex_attribute!(net, :age, Dict(\n    1 => 25.0, 2 => 30.0, 3 => 28.0, 4 => 35.0, 5 => 22.0,\n    6 => 40.0, 7 => 33.0, 8 => 27.0, 9 => 31.0, 10 => 29.0\n))","title":"Setting Vertex Attributes"},{"category":"section","location":"getting_started/#Directed-Networks","page":"Getting Started","text":"# Create a directed network\ndnet = network(5; directed=true)\nadd_edge!(dnet, 1, 2)\nadd_edge!(dnet, 2, 1)  # Reciprocated tie\nadd_edge!(dnet, 1, 3)","title":"Directed Networks"},{"category":"section","location":"getting_started/#Step-2:-Define-Model-Terms","page":"Getting Started","text":"Terms capture different structural mechanisms that may shape the observed network:\n\n# Basic structural model\nterms = [\n    Edges(),        # Baseline density (like an intercept)\n    Triangle(),     # Triadic closure tendency\n]","title":"Step 2: Define Model Terms"},{"category":"section","location":"getting_started/#Exploring-Available-Terms","page":"Getting Started","text":"ERGM.jl provides terms organized by type:\n\nCategory Terms Description\nStructural Edges, Mutual, Triangle, Kstar (undirected), OStar/IStar (directed), TwoPath Network topology\nDegree counts Degree, IDegree, ODegree Vertices with a given (in-/out-)degree\nGeometrically Weighted GWESP, GWDSP, GWDegree, GWIDegree, GWODegree Downweighted structural terms\nNodal NodeFactor, NodeCov, NodeMatch, NodeMismatch, NodeMix, AbsDiff Vertex attribute effects\nDyadic EdgeCov Dyad-level covariate effects","title":"Exploring Available Terms"},{"category":"section","location":"getting_started/#Example:-Comprehensive-Model","page":"Getting Started","text":"# Structural + attribute model\nterms = [\n    Edges(),                           # Baseline density\n    Triangle(),                        # Triadic closure\n    NodeMatch(:gender),                # Gender homophily\n    NodeCov(:age),                     # Age effect on tie formation\n    AbsDiff(:age),                     # Age similarity effect\n]","title":"Example: Comprehensive Model"},{"category":"section","location":"getting_started/#Step-3:-Fit-the-Model","page":"Getting Started","text":"Use ergm (or fit_ergm) to estimate the model:\n\nresult = ergm(net, terms; method=:mple)","title":"Step 3: Fit the Model"},{"category":"section","location":"getting_started/#Estimation-Methods","page":"Getting Started","text":"Method Description Use Case\n:mple Maximum pseudo-likelihood Exact objective for dyad-independent models; conditional approximation otherwise\n:mcmle Monte Carlo maximum likelihood Fits the likelihood with MCMC error; requires convergence and mixing checks","title":"Estimation Methods"},{"category":"section","location":"getting_started/#MPLE","page":"Getting Started","text":"# Fit the product of dyad-conditional probabilities\nresult = ergm(net, terms; method=:mple, verbose=true)","title":"MPLE"},{"category":"section","location":"getting_started/#MCMLE","page":"Getting Started","text":"# Iterative MCMC-based estimation. (On this 10-node toy network a model with\n# `Triangle()` is degenerate — the sampler collapses and `mcmle` says so\n# loudly — so the MCMLE example uses the dyad-independent homophily model;\n# see the Complete Example below and the estimation guide for real data.)\nresult = ergm(net, [Edges(), NodeMatch(:gender)];\n    method = :mcmle,\n    n_samples = 1000,\n    maxiter = 20,\n    rng = Xoshiro(42),\n    verbose = true\n)","title":"MCMLE"},{"category":"section","location":"getting_started/#MCMLE-Parameters","page":"Getting Started","text":"Parameter Description Default\nn_samples MCMC samples per iteration (over all chains) 1000\nn_chains Independent chains per iteration (parallel with threads; same result at any thread count) 1\nburnin Burn-in steps per chain 20 * n_dyads\ninterval Thinning interval max(100, n_dyads ÷ 10)\nmaxiter Maximum MCMLE (Newton-Raphson) iterations 20\nbridge_rungs Path-sampling rungs for the log-likelihood; 0 skips it (loglik/AIC/BIC NaN, everything else identical) 16\nconv_threshold, hotelling_alpha Convergence tests (per-statistic t-ratios, Hotelling T²) 0.1, 0.05\nrng The AbstractRNG every draw flows from Random.default_rng()\n\n(There is no tol: convergence is a statistical test, not a tolerance. A fit that exhausts maxiter is returned with converged == false, a warning quoting the last max t-ratio and Hotelling p-value, a caveat line in show, and the numbers in result.mcmc_convergence; continue it with init=coef(result). The standard errors of an MCMLE fit include the Monte-Carlo error of the estimate — show prints the share as \"MCMC %\", mcmc_se(result) returns it.)","title":"MCMLE Parameters"},{"category":"section","location":"getting_started/#Step-4:-Interpret-Results","page":"Getting Started","text":"The result object contains coefficient estimates and test statistics:\n\n# Print formatted summary table\nprintln(result)\n\nresult is the MCMLE fit of [Edges(), NodeMatch(:gender)] from the block above, and this is what it prints (the shared ecosystem coefficient table, followed by R's \"MCMC %\" line for an MCMLE fit):\n\nERGM Results\n============\nMethod: mcmle\nLog-likelihood: -14.9621\nAIC: 33.92, BIC: 37.54\nConverged: true\n\nCoefficients:\n                  Estimate  Std.Error  z value  Pr(>|z|)\nedges              -1.6582     0.5220  -3.1769    0.0015 **\nnodematch.gender   -1.2862     1.1446  -1.1238    0.2611\n---\nSignif. codes: 0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nMCMC % of the standard error (100·(se − se_fisher)/se): edges 0, nodematch.gender 0\n\nAn MPLE fit of a model with a dyad-dependent term (Triangle, GWESP, …) prints the same table followed by a warning that the naive pseudo-likelihood standard errors are suspect — refit with method=:mcmle or use se=:bootstrap (see the estimation guide for the exact text).","title":"Step 4: Interpret Results"},{"category":"section","location":"getting_started/#Accessing-Results-Programmatically","page":"Getting Started","text":"# Coefficient vector\ncoef(result)\n\n# Standard errors\nstderror(result)\n\n# Variance-covariance matrix\nvcov(result)\n\n# Normal-theory confidence limits (one row per coefficient)\nconfint(result)\n\n# The coefficient table itself, inspectable by index or term name\ntbl = coeftable(result)\ntbl[\"edges\"].p_value","title":"Accessing Results Programmatically"},{"category":"section","location":"getting_started/#Interpreting-Coefficients","page":"Getting Started","text":"Coefficients are log-odds ratios for the conditional probability of an edge:\n\nCoefficient Interpretation\nθ > 0 Term increases edge probability\nθ < 0 Term decreases edge probability\nθ = 0 No effect\nexp(θ) Odds ratio for one-unit change in statistic\n\nExample interpretations:\n\nedges = -2.3 → Low baseline density (exp(-2.3) ≈ 0.10 odds for each potential edge)\ntriangle = 0.9 → Each shared partner increases odds of a tie by 146% (exp(0.9) ≈ 2.46)\nnodematch.gender = 0.5 → Same-gender ties are 65% more likely (exp(0.5) ≈ 1.65)","title":"Interpreting Coefficients"},{"category":"section","location":"getting_started/#Complete-Example","page":"Getting Started","text":"using Networks, ERGM\nusing Random\nusing Statistics\n\nRandom.seed!(42)\n\n# Create a small social network\nnet = network(15; directed=false)\n\n# Add edges forming a clustered structure\nfor (i, j) in [(1,2), (1,3), (2,3), (2,4), (3,4),    # Cluster 1\n                (5,6), (5,7), (6,7), (6,8), (7,8),    # Cluster 2\n                (9,10), (10,11), (11,12),              # Cluster 3\n                (4,5), (8,9)]                           # Bridge ties\n    add_edge!(net, i, j)\nend\n\n# Set vertex attributes\nset_vertex_attribute!(net, :group, Dict(\n    i => (i <= 4 ? \"A\" : i <= 8 ? \"B\" : \"C\") for i in 1:15\n))\n\n# Define and fit model. Closure is modelled with GWESP, not a raw\n# Triangle count — see the note under this block.\nterms = [\n    Edges(),\n    GWESP(0.5),\n    NodeMatch(:group),\n]\n\nresult = ergm(net, terms; method=:mple)\nprintln(result)\n\n# Simulate from fitted model: the simulated edge count sits near the\n# observed 15 (inspect the mean), one diagnostic of model adequacy\nsim_nets = simulate_ergm(result; n_sim=50, rng=Xoshiro(42))\nprintln(\"Mean edges in simulations: \",\n    mean(ne(s) for s in sim_nets))\n\n# Goodness of fit\ngof_result = gof(result; n_sim=50, stats=[:degree, :esp], rng=Xoshiro(43))\ndeg = only(s for s in gof_result.statistics if s.name == \"degree\")\nprintln(\"Degree GOF p-values: \", deg.p_values)\n\nwarning: Why not `Triangle()`? A degenerate fit, made visible\nReplace GWESP(0.5) with Triangle() above and the MPLE still \"converges\" — but its simulations have ≈ 67 edges on average, against 15 observed (of 105 dyads): the fitted model puts its mass near the complete graph, the textbook ERGM degeneracy of a raw triangle count. Always look at the simulated edge count after a fit; the estimation guide's Handling Degeneracy section explains how geometrically weighted terms can reduce this risk. They do not guarantee a nondegenerate fit.bad = ergm(net, [Edges(), Triangle(), NodeMatch(:group)]; method=:mple)\nmean(ne(s) for s in simulate_ergm(bad; n_sim=50, rng=Xoshiro(1)))   # ≈ 67, not 15","title":"Complete Example"},{"category":"section","location":"getting_started/#Network-Simulation","page":"Getting Started","text":"Simulate networks from a fitted model to assess whether the model reproduces key features of the observed network:\n\n# Simulate 100 networks (burn-in and thinning default to the dyad-scaled\n# rule 20 × n_dyads / max(100, n_dyads ÷ 10); pass burnin=/interval= to override)\nsim_nets = simulate_ergm(result; n_sim=100, rng=Xoshiro(44))\n\n# Compare observed vs. simulated edge counts\nobs_edges = ne(net)\nsim_edges = [ne(s) for s in sim_nets]\nprintln(\"Observed edges: \", obs_edges)\nprintln(\"Simulated edges (mean ± sd): \",\n    round(mean(sim_edges), digits=1), \" ± \",\n    round(std(sim_edges), digits=1))","title":"Network Simulation"},{"category":"section","location":"getting_started/#Goodness-of-Fit","page":"Getting Started","text":"Compare observed network properties to the distribution of properties across simulated networks:\n\ngof_result = gof(result; n_sim=100, stats=[:degree, :esp, :distance], rng=Xoshiro(45))\n\n# Degree distribution GOF (one GOFStatistic panel per statistic)\ndeg_gof = only(s for s in gof_result.statistics if s.name == \"degree\")\nprintln(\"Observed degree distribution: \", deg_gof.observed)\nprintln(\"Simulated mean: \", round.(vec(mean(deg_gof.simulated; dims=1)), digits=1))","title":"Goodness of Fit"},{"category":"section","location":"getting_started/#Best-Practices","page":"Getting Started","text":"Start simple: Begin with Edges() only, then add terms incrementally\nUse MPLE for exploration: Switch to MCMLE for final results\nCheck convergence: Verify result.converged == true\nAssess fit: Use gof() to compare simulated and observed statistics; agreement does not establish that the model is correct\nAvoid degeneracy: Use geometrically weighted terms (GWESP, GWDegree) instead of raw Triangle and Kstar for larger networks\nSet random seeds: For reproducibility in MCMLE and simulation","title":"Best Practices"},{"category":"section","location":"getting_started/#Next-Steps","page":"Getting Started","text":"Learn about all available Model Terms\nUnderstand Model Estimation in detail\nExplore Network Simulation from fitted models\nRun Goodness-of-Fit diagnostics","title":"Next Steps"},{"category":"section","location":"guide/diagnostics/#Goodness-of-Fit-and-Diagnostics","page":"Goodness of Fit","text":"ERGM.jl provides tools for assessing how well a fitted model reproduces key properties of the observed network. Goodness-of-fit (GOF) diagnostics compare observed network statistics to their distributions under the fitted model, while MCMC diagnostics assess convergence of the MCMLE estimation.","title":"Goodness of Fit and Diagnostics"},{"category":"section","location":"guide/diagnostics/#Why-Diagnostics?","page":"Goodness of Fit","text":"Even a model that converges successfully may not capture important features of the observed network. GOF diagnostics help answer:\n\nDoes the model reproduce the observed degree distribution?\nDoes it capture the right amount of local clustering (shared partners)?\nDoes it get the geodesic distance distribution right?\nAre there systematic departures that suggest missing terms?","title":"Why Diagnostics?"},{"category":"section","location":"guide/diagnostics/#Goodness-of-Fit-Assessment","page":"Goodness of Fit","text":"","title":"Goodness-of-Fit Assessment"},{"category":"section","location":"guide/diagnostics/#Running-GOF","page":"Goodness of Fit","text":"using Networks, ERGM\nusing Random\nusing Statistics\n\nRandom.seed!(42)\n\n# Example network: Florentine marriage ties with a categorical attribute\nnet = load_dataset(:florentine_marriage)\nset_vertex_attribute!(net, :gender,\n    Dict(v => (isodd(v) ? \"F\" : \"M\") for v in 1:nv(net)))\n\n# Fit a model\nterms = [Edges(), GWESP(0.5), NodeMatch(:gender)]\nresult = ergm(net, terms; method=:mple)\n\n# Run goodness-of-fit with 100 simulated networks\ngof_result = gof(result; n_sim=100, stats=[:degree, :esp, :distance])\n\ngof also accepts rng (all simulation randomness flows from it — same seed, same result), burnin/interval (MCMC controls; they default to the dyad-scaled rule every sampler shares, 20 × n_dyads and max(100, n_dyads ÷ 10) — see the simulation guide), and n_chains (the simulations are split over independent chains run in parallel, seeded deterministically from rng, so results are thread-count-independent).","title":"Running GOF"},{"category":"section","location":"guide/diagnostics/#Available-Statistics","page":"Goodness of Fit","text":"The gof function compares these network statistics:\n\nStatistic Description What it Captures\n:degree Degree distribution Overall connectivity patterns\n:idegree / :odegree In-/out-degree distributions Direction-specific connectivity (directed networks)\n:esp Edgewise shared partner distribution Local clustering / triadic closure\n:distance Geodesic distance distribution, plus an \"Inf\" level for unreachable pairs Global network structure / reachability\n\nFor directed networks, requesting :degree produces separate idegree and odegree panels (as R ergm's GOF does), and the :esp panel uses the esp_type shared-partner definition (default :OTP, statnet's directed default — same types as GWESP).","title":"Available Statistics"},{"category":"section","location":"guide/diagnostics/#GOF-Results","page":"Goodness of Fit","text":"gof returns a Networks.GOFResult — the goodness-of-fit container shared by every model package in the ecosystem. Its statistics field holds one GOFStatistic per panel:\n\nField Type Description\nname String Panel name (\"degree\", \"esp\", ...)\nlabels Vector{String} Level labels (degree 0, 1, 2, ...)\nobserved Vector{Float64} Observed distribution\nsimulated Matrix{Float64} One row per simulated network\np_values Vector{Float64} Two-sided Monte-Carlo p-values per level\n\np-values use the shared (1 + k)/(N + 1) Monte-Carlo estimator (Networks.mc_pvalue), so they are never exactly zero. println(gof_result) renders observed value, simulation envelope, and p-value per level.","title":"GOF Results"},{"category":"section","location":"guide/diagnostics/#Interpreting-GOF-Results","page":"Goodness of Fit","text":"using Statistics\n\n# Degree distribution panel\ndeg_gof = only(s for s in gof_result.statistics if s.name == \"degree\")\n\nprintln(\"Degree distribution GOF:\")\nfor d in 0:min(10, length(deg_gof.observed)-1)\n    obs = deg_gof.observed[d+1]\n    sim_mean = round(mean(deg_gof.simulated[:, d+1]), digits=1)\n    sim_sd = round(std(deg_gof.simulated[:, d+1]), digits=1)\n    p = round(deg_gof.p_values[d+1], digits=3)\n    println(\"  Degree $d: obs=$obs, sim=$sim_mean ± $sim_sd, p=$p\")\nend\n\nInterpreting p-values:\n\np close to 0.5: Model reproduces this feature well\np close to 0.0 or 1.0: Systematic deviation — model under/over-predicts\np < 0.05 or p > 0.95: Significant departure from the model","title":"Interpreting GOF Results"},{"category":"section","location":"guide/diagnostics/#Degree-Distribution","page":"Goodness of Fit","text":"The degree GOF compares the count of nodes with each degree value:\n\ndeg_gof = only(s for s in gof_result.statistics if s.name == \"degree\")\n\n# Good fit: observed values fall within simulated range\nfor d in 0:length(deg_gof.observed)-1\n    sim_col = deg_gof.simulated[:, d+1]\n    within_range = abs(deg_gof.observed[d+1] - mean(sim_col)) < 2 * std(sim_col)\n    status = within_range ? \"OK\" : \"POOR\"\n    println(\"Degree $d: $status\")\nend","title":"Degree Distribution"},{"category":"section","location":"guide/diagnostics/#Edgewise-Shared-Partners","page":"Goodness of Fit","text":"The ESP GOF compares how many edges have 0, 1, 2, ... shared partners:\n\nesp_gof = only(s for s in gof_result.statistics if s.name == \"esp\")\n\nprintln(\"ESP distribution GOF:\")\nfor e in 0:length(esp_gof.observed)-1\n    println(\"  ESP $e: obs=$(esp_gof.observed[e+1]), \",\n            \"sim=$(round(mean(esp_gof.simulated[:, e+1]), digits=1))\")\nend","title":"Edgewise Shared Partners"},{"category":"section","location":"guide/diagnostics/#Geodesic-Distance","page":"Goodness of Fit","text":"The distance GOF compares the distribution of shortest-path lengths over the network's dyads — unordered pairs on an undirected network, ordered pairs (out-distances) on a directed one, exactly R's gof(..., GOF = ~distance) — with one level per finite distance and a final \"Inf\" level counting the pairs that cannot reach each other at all. That last row is the reachability comparison, the one thing this panel adds over the degree and ESP panels. On flomarriage the observed panel is [20, 35, 32, 15, 3, …, Inf: 15] (the 15 pairs involving the isolated Pucci family), R's obs.dist; the provenanced fixture pins it.\n\ndist_gof = only(s for s in gof_result.statistics if s.name == \"distance\")\n\nprintln(\"Geodesic distance GOF:\")\nfor (d, label) in enumerate(dist_gof.labels)\n    println(\"  Distance $label: obs=$(dist_gof.observed[d]), \",\n            \"sim=$(round(mean(dist_gof.simulated[:, d]), digits=1))\")\nend","title":"Geodesic Distance"},{"category":"section","location":"guide/diagnostics/#Diagnosing-Poor-Fit","page":"Goodness of Fit","text":"","title":"Diagnosing Poor Fit"},{"category":"section","location":"guide/diagnostics/#What-to-Do-When-GOF-Fails","page":"Goodness of Fit","text":"Poor GOF on Likely Missing Suggested Terms\nDegree distribution Degree heterogeneity control GWDegree, NodeCov, NodeFactor\nESP distribution Triadic closure GWESP, Triangle\nDistance distribution Global connectivity Edges (adjust), consider network constraints","title":"What to Do When GOF Fails"},{"category":"section","location":"guide/diagnostics/#Iterative-Model-Building","page":"Goodness of Fit","text":"# Model 1: Edges only\nr1 = ergm(net, [Edges()])\ng1 = gof(r1; n_sim=50)\n\n# Model 2: Add triadic closure\nr2 = ergm(net, [Edges(), GWESP(0.5)])\ng2 = gof(r2; n_sim=50)\n\n# Model 3: Add attribute effects\nr3 = ergm(net, [Edges(), GWESP(0.5), NodeMatch(:gender)])\ng3 = gof(r3; n_sim=50)\n\n# Compare degree GOF across models\nfor (i, g) in enumerate([g1, g2, g3])\n    deg = only(s for s in g.statistics if s.name == \"degree\")\n    println(\"Model $i degree GOF mean p-value: $(round(mean(deg.p_values), digits=3))\")\nend","title":"Iterative Model Building"},{"category":"section","location":"guide/diagnostics/#MCMLE-Non-convergence","page":"Goodness of Fit","text":"Before the MCMC diagnostics, check the estimation itself converged. An MCMLE fit that exhausts maxiter warns at fit time (quoting the last max t-ratio, Hotelling p-value and step length), prints the caveat under Converged: false when shown, lists it in Networks.approximations(result), and stores the numbers in result.mcmc_convergence:\n\nresult = ergm(net, terms; method=:mcmle, n_samples=1000, rng=Xoshiro(1))\nif !result.converged\n    c = result.mcmc_convergence          # (iterations, step_length, t_ratios, hotelling_p, n_eff)\n    println(\"max t-ratio \", maximum(c.t_ratios), \", Hotelling p \", c.hotelling_p,\n            \", ESS \", c.n_eff, \" after \", c.iterations, \" iterations\")\nend\n\nThe same numbers are available for any sample through the public ERGM.mcmc_convergence(samples, targets) (per-statistic t-ratios, the Geyer effective sample size, the Hotelling T² p-value and the verdict). A fit that converged has step_length == 1.0 and every t-ratio below conv_threshold. To continue an unconverged fit with a larger budget pass init=coef(result).\n\nThe standard errors of a converged MCMLE fit already include the Monte-Carlo error of the estimate; show prints R's \"MCMC %\" column — round(100·(se − se_fisher)/se), the share of the standard error the Monte-Carlo term adds — as MCMC % of the standard error, and the Monte-Carlo part itself is mcmc_se(result); see the estimation guide.","title":"MCMLE Non-convergence"},{"category":"section","location":"guide/diagnostics/#MCMC-Diagnostics","page":"Goodness of Fit","text":"For models fit with MCMLE, MCMC diagnostics assess whether the MCMC sampler has converged and mixed well.","title":"MCMC Diagnostics"},{"category":"section","location":"guide/diagnostics/#Running-MCMC-Diagnostics","page":"Goodness of Fit","text":"# Fit with MCMLE\nresult = ergm(net, terms; method=:mcmle, verbose=true)\n\n# Get MCMC diagnostics\ndiag = mcmc_diagnostics(result)\n\nmcmc_diagnostics requires a fit with MCMC samples. Calling it on an MPLE fit throws an ArgumentError (MPLE draws no MCMC sample, so there is nothing to diagnose) — refit with method=:mcmle first.","title":"Running MCMC Diagnostics"},{"category":"section","location":"guide/diagnostics/#Understanding-MCMC-Diagnostics","page":"Goodness of Fit","text":"The result is an MCMCDiagnostics whose show prints one row per term — println(diag) is R's mcmc.diagnostics(fit) table:\n\nMCMC diagnostics: 800 draws in 4 chains of 200, 200, 200, 200\n                  lag-1 AC  ESS (lag-1)  ESS (Geyer)  Geweke z  Geweke p\nedges                0.431        318.3        342.3     -0.98     0.329\ngwesp.fixed.0.5      0.278        452.0        436.2      1.03     0.301\nPrefer ESS (Geyer): it accounts for autocorrelation at every lag. A small Geweke p flags a chain that had not reached stationarity.\n\nIts fields:\n\nField Description Ideal Value\nterm_names Names of model terms —\nautocorrelation Lag-1 autocorrelation per term (within chains) Close to 0\neffective_sample_size ESS from the lag-1 autocorrelation (most optimistic estimate) > 100\ness_geyer Geyer initial-sequence ESS, using all lags (preferred) > 100\ngeweke_z, geweke_p Geweke stationarity z-scores (first 10% vs last 50% of a chain) and two-sided p-values geweke_p not small\nn_samples, chain_lengths Total MCMC samples and the per-chain counts —\n\nWith n_chains > 1 every quantity is computed within each chain and combined — the two ESS columns summed over chains (the Geyer column is then exactly the n_eff of the MCMLE's own convergence test, result.mcmc_convergence.n_eff), the Geweke test reported for the chain of largest |z| — never across the seams of the concatenated sample.\n\nPrefer ess_geyer over the lag-1 effective_sample_size: the initial-sequence estimator accounts for autocorrelation at all lags. A small geweke_p flags a chain that had not reached stationarity — increase burnin.","title":"Understanding MCMC Diagnostics"},{"category":"section","location":"guide/diagnostics/#Interpreting-Results","page":"Goodness of Fit","text":"if !isnothing(result.mcmc_samples)\n    diag = mcmc_diagnostics(result)\n    println(diag)                                   # the per-term table\n    low = diag.term_names[diag.ess_geyer .< 100]    # terms whose chain mixes poorly\n    isempty(low) || println(\"Low ESS: \", join(low, \", \"))\nelse\n    println(\"No MCMC samples — model was fit with MPLE\")\nend","title":"Interpreting Results"},{"category":"section","location":"guide/diagnostics/#Effective-Sample-Size","page":"Goodness of Fit","text":"The lag-1 effective sample size (ESS) accounts for first-order autocorrelation, chain by chain:\n\ntextESS_j = sum_c L_c cdot frac1 - rho_cj1 + rho_cj\n\nwhere rho_cj is the lag-1 autocorrelation of term j in chain c of length L_c. The Geyer column replaces (1-rho)(1+rho) by the initial-monotone-sequence estimate over all lags.\n\nESS Assessment\n> 200 Good mixing\n100–200 Acceptable\n< 100 Poor mixing — increase samples or interval","title":"Effective Sample Size"},{"category":"section","location":"guide/diagnostics/#Improving-MCMC-Mixing","page":"Goodness of Fit","text":"If diagnostics indicate poor mixing:\n\n<!– skip-check –>\n\n# Increase samples and thinning\nresult = ergm(net, terms;\n    method = :mcmle,\n    n_samples = 5000,      # More samples\n    burnin = 5000,          # Longer burn-in\n    interval = 500,         # More thinning\n    verbose = true\n)","title":"Improving MCMC Mixing"},{"category":"section","location":"guide/diagnostics/#Complete-Diagnostic-Workflow","page":"Goodness of Fit","text":"using Networks, ERGM\nusing Random\n\nRandom.seed!(42)\n\n# Fit model\nterms = [Edges(), GWESP(0.5), GWDegree(0.5), NodeMatch(:gender)]\nresult = ergm(net, terms; method=:mple)\n\n# 1. Check convergence\nprintln(\"Converged: \", result.converged)\n\n# 2. Run GOF\ngof_result = gof(result; n_sim=100, stats=[:degree, :esp, :distance])\n\n# 3. Summarize GOF\nfor stat in gof_result.statistics\n    mean_p = mean(stat.p_values)\n    println(\"$(stat.name) GOF (mean p): $(round(mean_p, digits=3))\")\nend\n\n# 4. MCMC diagnostics (if MCMLE)\nif !isnothing(result.mcmc_samples)\n    println(mcmc_diagnostics(result))\nend","title":"Complete Diagnostic Workflow"},{"category":"section","location":"guide/diagnostics/#Computational-Notes","page":"Goodness of Fit","text":"GOF computation time scales linearly with n_sim\nThe simulations are split over n_chains chains; each chain's burn-in and thinning default to the dyad-scaled rule (20 × n_dyads, max(100, n_dyads ÷ 10)), so the cost of gof grows with the number of dyads\nDistance computation uses Graphs.jl's gdistances and can be slow for very large networks\nFor quick diagnostics, use n_sim=50; for publication, use n_sim=200+","title":"Computational Notes"},{"category":"section","location":"guide/diagnostics/#Best-Practices","page":"Goodness of Fit","text":"Always run GOF: A converged model can still fit poorly\nCheck all three statistics: Degree, ESP, and distance capture different aspects\nUse enough simulations: At least 50 for exploration, 100+ for final assessment\nIterate on model specification: Use GOF results to guide term selection\nCompare models: Run GOF on multiple specifications to find the best fit\nCheck MCMC convergence: For MCMLE, verify adequate ESS before trusting results\nSet random seeds: For reproducible diagnostic results","title":"Best Practices"},{"category":"section","location":"guide/terms/#Model-Terms","page":"Model Terms","text":"ERGM terms are the building blocks of an ERGM. Each term computes a network statistic that captures a specific structural mechanism. All terms implement a common interface and can be freely combined in models.","title":"Model Terms"},{"category":"section","location":"guide/terms/#Term-Interface","page":"Model Terms","text":"All terms implement three methods:\n\n<!– skip-check –>\n\ncompute(term, net) -> Float64\nchange_stat(term, net, i, j) -> Float64\nname(term) -> String\n\nThe compute function calculates the full statistic value for the network. The change_stat function computes the add-direction change statistic g(y^+_ij) - g(y^-_ij): the statistic with edge (i j) present minus the statistic with it absent, holding the rest of the network fixed. Its value does not depend on whether the edge currently exists — this state-independent quantity is what the MPLE design matrix and the Metropolis–Hastings sampler both require (the sampler negates it for removal proposals).","title":"Term Interface"},{"category":"section","location":"guide/terms/#Term-Categories","page":"Model Terms","text":"ERGM.jl organizes terms into three categories:\n\nType Description Examples\nStructuralTerm Network topology only Edges, Triangle, GWESP, GWDSP, Degree\nNodalTerm Vertex attribute effects NodeFactor, NodeMatch, NodeMix, NodeCov\nDyadicTerm Dyad-level covariates EdgeCov\n\nwarning: Not implemented — what an R `ergm` user will not find\nERGM.jl implements the terms listed in this guide. What an R ergm user will not find, and what happens instead:Absent term families — sender/receiver attribute terms nodeicov/nodeocov/nodeifactor/nodeofactor; the directed triadic terms ttriple/ctriple/transitiveties/cyclicalties/asymmetric; the esp/dsp/nsp count terms; isolates (only as Degree(0)), balance, cycle, smalldiff, nodematch(keep=) have no counterpart (there is no term to call, so nothing is silently mis-fit). Use IStar/OStar for directed stars, Triangle (= ttriple + ctriple on a directed network) for triadic closure, GWESP/GWDSP for shared-partner effects, and NodeFactor/NodeCov for a one-mode attribute main effect.\nCurved terms — gwesp(fixed=FALSE), gwdegree(fixed=FALSE), gwdsp(fixed=FALSE) with the decay estimated. Every geometrically weighted term here takes a fixed decay (GWESP(0.5) is gwesp(0.5, fixed=TRUE)); there is no curved-family estimation.\nBipartite terms and two-mode networks — b1degree, b2degree, b1factor, b2factor, b1nodematch, b1star, …. ERGMModel (hence fit_ergm/ergm) refuses a network created with bipartite=k or a BipartiteNetwork with an ArgumentError naming the bipartite gap, rather than counting the impossible within-mode dyads as observations.\nconstraints= — edges, degrees, blockdiag, observed, …: a non-empty constraints= on ERGMFormula throws an ArgumentError.\noffset() terms — no counterpart; every term is estimated.\nnodecov/nodefactor/… with an NA value — refused at model construction with an ArgumentError naming the vertices without a value (statnet: \"Attribute has missing data, which is not currently supported by ergm\"). There is no zero-fill.\nSimulation and GOF under missing data — a simulated network has a value at every dyad, so simulate_ergm/gof on a masked network only offer the warned missing=:condition_on_face (masked dyads frozen at face value) and refuse missing=:mle. Estimation under missing data is implemented: mcmle(...; missing=:mle) is R's missing-data MLE, and MPLE drops masked dyads.","title":"Term Categories"},{"category":"section","location":"guide/terms/#Structural-Terms","page":"Model Terms","text":"These capture patterns in the network topology without reference to node attributes. The examples below assume the packages are loaded and a small example network exists:\n\nusing Networks, ERGM\n\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2)\nset_edge_attribute!(net, :distance, 1, 2, 1.5)","title":"Structural Terms"},{"category":"section","location":"guide/terms/#Edges","page":"Model Terms","text":"The most basic ERGM term — the total number of edges in the network. Analogous to an intercept in regression.\n\nEdges()\n\nInterpretation: Controls baseline network density. A negative coefficient (common) indicates sparse networks are more likely than dense ones.","title":"Edges"},{"category":"section","location":"guide/terms/#Mutual","page":"Model Terms","text":"The number of reciprocated dyads (directed networks only):\n\nMutual()\n\nInterpretation: A positive coefficient indicates a tendency toward reciprocity — if i sends a tie to j, then j is more likely to send a tie to i.","title":"Mutual"},{"category":"section","location":"guide/terms/#Triangle","page":"Model Terms","text":"The number of triangles in the network:\n\nTriangle()\n\nVisual representation:\n\n  k\n / \\\ni — j   ← all three edges present\n\nInterpretation: A positive coefficient indicates triadic closure — \"friends of friends become friends.\"\n\nwarning: Degeneracy\nThe Triangle term can cause model degeneracy in larger networks (the model places nearly all probability on either empty or complete networks). Use GWESP instead for robust triangle effects.","title":"Triangle"},{"category":"section","location":"guide/terms/#K-Star,-OStar,-IStar","page":"Model Terms","text":"The number of k-stars in the network — a central node connected to k others, sum_v binomd_vk (statnet's kstar(k)). Undirected networks only, as in R ergm; a directed network gets the out-star and in-star terms OStar(k) / IStar(k) (statnet's ostar(k) / istar(k), sum_v binomd^out_vk and sum_v binomd^in_vk):\n\nKstar(2)   # Two-stars, \"kstar2\" — undirected networks only\nKstar(3)   # Three-stars\n\nOStar(2)   # Out-two-stars, \"ostar2\" — directed networks only\nIStar(2)   # In-two-stars,  \"istar2\" — directed networks only\n\nERGMModel refuses Kstar on a directed network with an ArgumentError naming OStar/IStar (R: \"Term may not be used with networks with directed==TRUE\"), and refuses OStar/IStar on an undirected one.\n\nwarning: Changed in 0.2\nBefore 0.2 a directed network silently received out-stars under the label kstar<k>.\n\nInterpretation: Controls the degree distribution. Kstar(2) captures variance in degree; Kstar(3) captures skewness. OStar(2) is activity spread (out-degree variance), IStar(2) popularity spread.\n\nwarning: Degeneracy\nLike Triangle, raw k-star terms can cause degeneracy. Use GWDegree (or GWODegree/GWIDegree) for a more stable alternative.","title":"K-Star, OStar, IStar"},{"category":"section","location":"guide/terms/#TwoPath","page":"Model Terms","text":"The number of two-paths. For directed networks this counts pairs of edges h to v, v to k with h neq k (statnet's twopath); for undirected networks it counts 2-stars, sum_v binomd_v2:\n\nTwoPath()\n\nInterpretation: Captures the tendency for directed paths — nodes that receive ties also tend to send ties (to other nodes).","title":"TwoPath"},{"category":"section","location":"guide/terms/#GWESP-(Geometrically-Weighted-Edgewise-Shared-Partners)","page":"Model Terms","text":"A geometrically downweighted count of shared partners across edges:\n\n# Default decay = 0.5\nGWESP()\n\n# Custom decay parameter\nGWESP(0.8)\n\n# decay = 0 — statnet's gwesp(0, fixed=TRUE), the most common applied\n# specification: the number of edges with at least one shared partner\nGWESP(0.0)\n\nFormula: For each edge with ESP (edgewise shared partner) count s:\n\ntextGWESP = sum_textedges e^alpha left(1 - (1 - e^-alpha)^sright)\n\nParameter α (decay, ≥ 0): Controls how quickly additional shared partners are downweighted. Higher α = less downweighting; at α = 0 every edge with s ge 1 contributes exactly 1.\n\nCoefficient names follow R's labels exactly: an integer-valued decay is printed without a decimal point, so GWESP(0.0) is \"gwesp.fixed.0\" and GWESP(1.0) is \"gwesp.fixed.1\" (R's gwesp(1, fixed=TRUE)), while GWESP(0.5) is \"gwesp.fixed.0.5\". A coefficient can therefore be compared with a statnet fit by name.\n\nname(GWESP(0.0))   # \"gwesp.fixed.0\"\nname(GWESP(1.0))   # \"gwesp.fixed.1\"\nname(GWESP(0.5))   # \"gwesp.fixed.0.5\"\n\nDirected networks: type selects the shared-partner definition for each directed edge i to j, matching statnet's dgwesp types:\n\nGWESP(0.5)               # :OTP — outgoing two-path i→k→j (statnet's directed default)\nGWESP(0.5; type=:ITP)    # incoming two-path j→k→i\nGWESP(0.5; type=:OSP)    # outgoing shared partner i→k←... i.e. i→k and j→k\nGWESP(0.5; type=:ISP)    # incoming shared partner k→i and k→j\nGWESP(0.5; type=:union)  # either-direction adjacency (pre-0.2 behavior,\n                         # named \"gwesp.union.fixed.<decay>\"; not a statnet type)\n\nThe coefficient names follow statnet, including the fact that R's label depends on the network's directedness: on an undirected network the coefficient is \"gwesp.fixed.<decay>\" (type is ignored, in the label as in the statistic); on a directed network R ergm 4.12.0 names every type — the :OTP default included — \"gwesp.<type>.fixed.<decay>\", so a directed fit prints gwesp.OTP.fixed.0.5, gwesp.ITP.fixed.0.5, … exactly as coef() of the statnet fit does. ERGMModel/fit_ergm, summary_stats and the two-argument name(term, net) all resolve the label against the network; the one-argument name(GWESP(0.5)), which cannot see a network, gives the undirected label for :OTP. The non-statnet :union is always \"gwesp.union.fixed.<decay>\".\n\nflo = load_dataset(:florentine_marriage)    # undirected\nname(GWESP(0.5), flo)                       # \"gwesp.fixed.0.5\"\nname(GWESP(0.5), net)                       # \"gwesp.OTP.fixed.0.5\" — `net` above is directed: R's label\nERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), net).formula.terms.names   # [\"edges\", \"gwesp.OTP.fixed.0.5\"]\nsummary_stats(net, [GWESP(0.5)])            # (var\"gwesp.OTP.fixed.0.5\" = 0.0,)\n\nwarning: Changed in 0.2\nDirected GWESP previously counted either-direction shared partners while carrying statnet's OTP name. The default is now genuinely :OTP; use type=:union to reproduce the old statistic.\n\nDecay Behavior\nSmall (0.1–0.3) Strong downweighting — first shared partner matters most\nMedium (0.5) Moderate downweighting\nLarge (0.8–1.0) Weak downweighting — approaches raw triangle count\n\nInterpretation: A positive coefficient indicates triadic closure, with diminishing returns for each additional shared partner. Preferred over Triangle for avoiding degeneracy.","title":"GWESP (Geometrically Weighted Edgewise Shared Partners)"},{"category":"section","location":"guide/terms/#GWDegree-(Geometrically-Weighted-Degree)","page":"Model Terms","text":"A geometrically downweighted summary of the degree distribution (statnet's gwdegree(decay, fixed=TRUE), coefficient name \"gwdeg.fixed.<decay>\" — R's label):\n\n# Default decay = 0.5\nGWDegree()\n\n# Custom decay\nGWDegree(0.8)\n\n# decay = 0: the number of non-isolated vertices\nGWDegree(0.0)\n\nname(GWDegree(0.5))   # \"gwdeg.fixed.0.5\"\nname(GWDegree(1.0))   # \"gwdeg.fixed.1\"\n\nFormula: For each vertex with degree d:\n\ntextGWDegree = sum_textvertices e^alpha left(1 - (1 - e^-alpha)^dright)\n\nInterpretation: A positive coefficient indicates a preference for more even degree distributions (anti-preferential attachment). Preferred over Kstar for stability.\n\nGWDegree is defined for undirected networks only, as in R ergm. On a directed network ERGMModel refuses it with an ArgumentError naming the directed variants, which model the in- and out-degree distributions separately and match statnet's gwidegree/gwodegree (both with fixed=TRUE):\n\nGWIDegree(0.5)   # geometrically weighted in-degree, \"gwideg.fixed.0.5\"\nGWODegree(0.5)   # geometrically weighted out-degree, \"gwodeg.fixed.0.5\"\nGWIDegree(0.0)   # decay 0: the number of vertices with in-degree ≥ 1\n\nBoth are defined only for directed networks.\n\nwarning: Changed in 0.2\nBefore 0.2 a directed network silently received the out-degree statistic under the undirected label, and the undirected coefficient was labelled gwdegree.fixed.<decay> where R writes gwdeg.fixed.<decay>.","title":"GWDegree (Geometrically Weighted Degree)"},{"category":"section","location":"guide/terms/#GWDSP-(Geometrically-Weighted-Dyadwise-Shared-Partners)","page":"Model Terms","text":"The shared-partner analogue of GWESP summed over all dyads — tied or not — instead of over edges only (statnet's gwdsp(decay, fixed=TRUE)):\n\nGWDSP()                  # default decay = 0.5\nGWDSP(0.8)               # custom decay\nGWDSP(0.0)               # decay 0: the number of dyads with ≥ 1 shared partner\nGWDSP(0.5; type=:OSP)    # directed shared-partner type, as for GWESP\n\nFor directed networks type selects the shared-partner definition (:OTP outgoing two-path — the default, :ITP, :OSP, :ISP, or the non-statnet :union), with statnet's dgwdsp conventions: every type is summed over all ordered dyads (i, j), i ≠ j — the symmetric :OSP/:ISP counts included, so each unordered pair contributes twice (R's ddsp C code; on samplike, gwdsp.OSP.fixed.0.5 = 316.085 and gwdsp.ISP.fixed.0.5 = 229.136, pinned by the provenanced fixture). For dyadwise shared partners :OTP and :ITP yield the same statistic. The labels are direction-aware exactly as for GWESP: \"gwdsp.fixed.<decay>\" on an undirected network, \"gwdsp.OTP.fixed.<decay>\" etc. on a directed one.\n\nnote: Changed in 0.2 (round 2)\nGWDSP(d; type=:OSP) and type=:ISP on a directed network used to sum over unordered dyads and came out at exactly half of statnet's dgwdsp — with a consistently halved change statistic, so MPLE/MCMLE coefficients for these types were off by a factor of 2 relative to R. Both now follow R.\n\nInterpretation: GWESP captures closure among connected dyads; GWDSP captures the prevalence of two-paths regardless of whether the dyad is tied. Including both separates \"friends of friends\" (open two-paths) from actual triadic closure.","title":"GWDSP (Geometrically Weighted Dyadwise Shared Partners)"},{"category":"section","location":"guide/terms/#Degree,-IDegree,-ODegree","page":"Model Terms","text":"The number of vertices with a given (in-/out-)degree — statnet's degree(d), idegree(d), odegree(d):\n\nDegree(1)      # undirected networks only: count of degree-1 vertices\nIDegree(0)     # directed only: count of in-degree-0 vertices\nODegree(2)     # directed only: count of out-degree-2 vertices\n\nAs in statnet, Degree is only defined for undirected networks — use IDegree/ODegree on directed networks. A vector or range of degrees is one term that expands into one statistic per degree when the model is built (exactly like a multi-level NodeFactor), so the statnet formula ~ edges + degree(0:2) becomes:\n\nflo = load_dataset(:florentine_marriage)\nfit = fit_ergm(flo, [Edges(), Degree(0:2)])\nfit.model.formula.terms.names          # [\"edges\", \"degree0\", \"degree1\", \"degree2\"]\n\nsummary_stats(flo, [Edges(), Degree(0:2)])\n# (edges = 20.0, degree0 = 1.0, degree1 = 4.0, degree2 = 2.0)\n\nThe unexpanded Degree(0:2) is a specification, not a statistic: compute(Degree(0:2), net) throws an ArgumentError saying so, and name(Degree(0:2)) is the placeholder \"degree(0,1,2)\".\n\nInterpretation: Degree-count terms model specific features of the degree distribution exactly (e.g. Degree(0) for the number of isolates). For a smooth overall degree effect prefer the geometrically weighted terms.","title":"Degree, IDegree, ODegree"},{"category":"section","location":"guide/terms/#Nodal-Attribute-Terms","page":"Model Terms","text":"These incorporate vertex-level attributes for modeling homophily and covariate effects.","title":"Nodal Attribute Terms"},{"category":"section","location":"guide/terms/#NodeFactor","page":"Model Terms","text":"Main effect of a categorical vertex attribute (R ergm's nodefactor): for each included attribute level, the number of times a vertex with that level appears as an endpoint of an edge (edges within the level count twice).\n\n# One statistic per level, first (sorted) level dropped as the reference\n# — exactly like R's nodefactor(\"gender\")\nNodeFactor(:gender)\n\n# Keep all levels (statnet's base=0 / levels=TRUE)\nNodeFactor(:gender; base=0)\n\n# Choose the included levels explicitly, in order\nNodeFactor(:gender; levels=[\"F\"])\n\n# A single-level term directly (no expansion)\nNodeFactor(:gender; level=\"F\")\n\nThe levels are resolved from the network at model construction, where the multi-level term expands into per-level statistics named \"nodefactor.<attr>.<level>\".\n\nwarning: Changed in 0.2\nNodeFactor(attr) previously produced a single statistic counting the endpoint appearances of every vertex with the attribute — which is collinear with Edges by construction (every edge has two endpoints), so models with both were unidentified. It now matches statnet: one statistic per attribute level, with the first (sorted) level dropped as the reference category. Pass base=0 to keep all levels.\n\nInterpretation: A positive coefficient for level \"F\" means that female actors form more ties than actors in the reference category.","title":"NodeFactor"},{"category":"section","location":"guide/terms/#NodeCov","page":"Model Terms","text":"Main effect of a continuous vertex attribute. Sums the attribute values of both endpoints for each edge.\n\nThe attribute must be set on every vertex — for this and every other attribute-based term. As in statnet (which refuses NA attribute values), ERGMModel throws an ArgumentError naming the term, the attribute, how many vertices lack a value and which ones; before 0.2 those vertices were silently zero-filled into the design.\n\n# Basic continuous effect\nNodeCov(:age)\n\n# With transform\nNodeCov(:age; transform=:log)    # log(value + 1)\nNodeCov(:age; transform=:sqrt)   # sqrt(value)\n\nInterpretation: A positive coefficient means nodes with higher attribute values are more likely to form ties.","title":"NodeCov"},{"category":"section","location":"guide/terms/#NodeMatch","page":"Model Terms","text":"Homophily effect — tendency for edges between vertices with matching attribute values:\n\n# Uniform homophily: one statistic counting all matched edges\nNodeMatch(:gender)\n\n# Differential homophily (R's nodematch(diff=TRUE)): one statistic per\n# level, each counting only that level's matched edges. The term system is\n# one-statistic-per-term, so construct one term per level:\n[NodeMatch(:gender; diff=true, level=l) for l in (\"F\", \"M\")]\n\nStatistic names follow R ergm: \"nodematch.gender\" and \"nodematch.gender.F\" / \"nodematch.gender.M\".\n\nInterpretation:\n\nNodeMatch > 0: Homophily — actors prefer same-type partners\nNodeMatch < 0: Heterophily — actors prefer different-type partners\nWith diff=true, each level's coefficient measures within-level homophily separately\n\nwarning: Changed in 0.2\nNodeMatch(attr; diff=true) previously counted mismatched edges — a different model from R's nodematch(diff=TRUE) under the same keyword. diff=true now means differential homophily as in R (and requires level); the mismatch count is available as NodeMismatch.","title":"NodeMatch"},{"category":"section","location":"guide/terms/#NodeMismatch","page":"Model Terms","text":"Heterophily effect — the number of edges whose endpoints have different attribute values (this package's pre-0.2 NodeMatch(...; diff=true) statistic; it has no R nodematch equivalent):\n\nNodeMismatch(:gender)\n\nInterpretation: A positive coefficient means actors prefer different-type partners.","title":"NodeMismatch"},{"category":"section","location":"guide/terms/#NodeMix","page":"Model Terms","text":"Mixing-matrix cell counts for a categorical vertex attribute (R ergm's nodemix): one statistic per mixing cell, counting the edges whose endpoint levels fall in that cell.\n\n# One statistic per mixing cell, first cell dropped as the reference\n# (statnet's levels2=-1 default)\nNodeMix(:gender)\n\n# Keep every cell\nNodeMix(:gender; levels2=0)\n\n# Select cells by index into the statnet-ordered cell list\nNodeMix(:gender; levels2=[2, 3])\n\n# A single cell directly (tail level, head level for directed networks)\nNodeMix(:gender, \"F\", \"M\")\n\nCells follow statnet's ordering over the sorted levels — unordered pairs (u_i, u_j), i ≤ j, in column-major order for undirected networks, all ordered (tail, head) pairs for directed ones — and the statistics are named \"mix.<attr>.<l1>.<l2>\". Like NodeFactor, the multi-cell term expands into per-cell statistics at model construction.\n\nInterpretation: Each coefficient measures the propensity of ties in that particular level combination relative to the reference cell — a full mixing structure, generalizing NodeMatch/NodeMismatch.","title":"NodeMix"},{"category":"section","location":"guide/terms/#AbsDiff","page":"Model Terms","text":"Absolute difference in a continuous attribute between edge endpoints:\n\n# Linear absolute difference\nAbsDiff(:age)\n\n# Powered absolute difference\nAbsDiff(:age; pow=2.0)   # |age_i - age_j|²\n\nInterpretation:\n\nAbsDiff < 0: Actors with similar attribute values are more likely to form ties\nAbsDiff > 0: Actors with different attribute values are more likely to form ties","title":"AbsDiff"},{"category":"section","location":"guide/terms/#Dyadic-Terms","page":"Model Terms","text":"These incorporate dyad-level covariates.","title":"Dyadic Terms"},{"category":"section","location":"guide/terms/#EdgeCov","page":"Model Terms","text":"Dyad-level covariate effect. Takes an n times n matrix where entry (ij) is the covariate value for that dyad.\n\n# From a covariate matrix\ndistance_matrix = [0.0 1.5 3.0; 1.5 0.0 2.0; 3.0 2.0 0.0]\nEdgeCov(distance_matrix; name=\"distance\")\n\n# From an edge attribute stored in the network\nEdgeCov(net, :distance)\n\nInterpretation: A negative coefficient on distance means that geographically close nodes are more likely to form ties.","title":"EdgeCov"},{"category":"section","location":"guide/terms/#Using-Terms-in-Practice","page":"Model Terms","text":"","title":"Using Terms in Practice"},{"category":"section","location":"guide/terms/#Building-a-Model","page":"Model Terms","text":"using Networks, ERGM\n\n# Set attributes\nset_vertex_attribute!(net, :gender, Dict(1=>\"M\", 2=>\"F\", 3=>\"M\"))\nset_vertex_attribute!(net, :age, Dict(1=>25.0, 2=>30.0, 3=>28.0))\n\n# Build comprehensive model\nterms = [\n    # Structural\n    Edges(),\n    GWESP(0.5),\n\n    # Attribute effects\n    NodeMatch(:gender),\n    NodeCov(:age),\n    AbsDiff(:age),\n]","title":"Building a Model"},{"category":"section","location":"guide/terms/#Computing-Statistics-Manually","page":"Model Terms","text":"# Compute individual term\nedges_term = Edges()\nprintln(\"Edge count: \", compute(edges_term, net))\n\n# Compute change statistic\nprintln(\"Change from adding (1,2): \", change_stat(edges_term, net, 1, 2))\n\n# Compute all terms at once via TermSet\nts = TermSet(terms)\nstats = compute_all(ts, net)\nprintln(\"All statistics: \", stats)","title":"Computing Statistics Manually"},{"category":"section","location":"guide/terms/#Summary-Statistics","page":"Model Terms","text":"# Named summary of network statistics (the page's 3-node directed example:\n# on a DIRECTED network the gwesp label carries R's type, gwesp.OTP.fixed.0.5)\nstats = summary_stats(net, terms)\nprintln(stats)\n# (edges = 1.0, var\"gwesp.OTP.fixed.0.5\" = 0.0, var\"nodematch.gender\" = 0.0, var\"nodecov.age\" = 55.0, var\"absdiff.age\" = 5.0)","title":"Summary Statistics"},{"category":"section","location":"guide/terms/#Change-Statistics-for-All-Terms","page":"Model Terms","text":"# How all statistics change when adding edge (1, 2)\nts = TermSet(terms)\ndeltas = change_stat_all(ts, net, 1, 2)\nprintln(\"Change statistics: \", deltas)","title":"Change Statistics for All Terms"},{"category":"section","location":"guide/terms/#Choosing-Terms","page":"Model Terms","text":"","title":"Choosing Terms"},{"category":"section","location":"guide/terms/#By-Research-Question","page":"Model Terms","text":"Question Terms\nWhat is the baseline density? Edges\nIs there reciprocity? Mutual\nDoes the network cluster? Triangle, GWESP\nAre two-paths prevalent (tied or not)? GWDSP\nIs the degree distribution uneven? Kstar, GWDegree (undirected); OStar, IStar, GWODegree, GWIDegree (directed)\nAre specific degrees over-represented (e.g. isolates)? Degree, IDegree, ODegree\nIs there attribute homophily? NodeMatch, AbsDiff\nDo attributes affect sociality? NodeFactor, NodeCov\nIs there a full mixing structure between groups? NodeMix\nDo spatial/dyadic factors matter? EdgeCov","title":"By Research Question"},{"category":"section","location":"guide/terms/#Best-Practices","page":"Model Terms","text":"Always include Edges: It controls baseline density and is needed in every model\nPrefer geometrically weighted terms: Use GWESP over Triangle and GWDegree over Kstar to avoid degeneracy\nStart simple: Build up from Edges() to more complex specifications\nCheck for multicollinearity: Highly correlated terms can inflate standard errors\nUse change statistics for validation: Verify change_stat is consistent with compute on small networks\nConsider network size: Some terms (Triangle, Kstar) become problematic at larger network sizes","title":"Best Practices"},{"category":"section","location":"guide/simulation/#Network-Simulation","page":"Network Simulation","text":"ERGM.jl provides tools to simulate networks from fitted ERGM models using Markov Chain Monte Carlo (MCMC). Simulated networks are essential for assessing model fit, making predictions, and understanding the structural implications of estimated parameters.","title":"Network Simulation"},{"category":"section","location":"guide/simulation/#Why-Simulate?","page":"Network Simulation","text":"Network simulation serves several purposes:\n\nGoodness of fit: Compare observed network properties to properties of networks generated by the model\nModel exploration: Understand what kinds of networks a given model specification produces\nPrediction: Generate plausible networks given estimated parameters\nHypothesis testing: Use simulation-based tests for network hypotheses","title":"Why Simulate?"},{"category":"section","location":"guide/simulation/#Simulating-from-a-Fitted-Model","page":"Network Simulation","text":"The simplest approach is to simulate from an ERGMResult:\n\nusing Networks, ERGM\nusing Random\n\nRandom.seed!(42)\n\n# Example network: Florentine marriage ties with a categorical attribute\nnet = load_dataset(:florentine_marriage)\nset_vertex_attribute!(net, :gender,\n    Dict(v => (isodd(v) ? \"F\" : \"M\") for v in 1:nv(net)))\n\n# Fit a model\nterms = [Edges(), Triangle(), NodeMatch(:gender)]\nresult = ergm(net, terms; method=:mple)\n\n# Simulate 100 networks from the fitted model\nsim_nets = simulate_ergm(result; n_sim=100)\n\nprintln(\"Generated $(length(sim_nets)) networks\")\nprintln(\"Each with $(nv(sim_nets[1])) nodes\")","title":"Simulating from a Fitted Model"},{"category":"section","location":"guide/simulation/#Parameters","page":"Network Simulation","text":"Parameter Description Default\nn_sim Number of networks to simulate 1\nburnin MCMC burn-in steps (per chain) 20 × n_dyads\ninterval Steps between saved samples max(100, n_dyads ÷ 10)\nrng Random number generator all draws flow from Random.default_rng()\nn_chains Independent chains run in parallel min(n_sim, 4)","title":"Parameters"},{"category":"section","location":"guide/simulation/#Reproducibility-and-Threading","page":"Network Simulation","text":"Every sampling function takes an rng::AbstractRNG keyword, and all random draws flow from it — two calls with the same rng state return identical networks. The n_sim draws are split over n_chains independent chains, run in parallel when Julia has multiple threads; each chain's RNG is seeded deterministically from the caller's rng, so results do not depend on the thread count:\n\nsims_a = simulate_ergm(result; n_sim=20, rng=Xoshiro(1))\nsims_b = simulate_ergm(result; n_sim=20, rng=Xoshiro(1))\n# sims_a and sims_b are identical, at any JULIA_NUM_THREADS","title":"Reproducibility and Threading"},{"category":"section","location":"guide/simulation/#Choosing-Burn-in-and-Interval","page":"Network Simulation","text":"The burn-in ensures the MCMC chain has reached stationarity before sampling. The interval (thinning) reduces autocorrelation between samples.\n\nEvery sampler in ERGM.jl — simulate_ergm, sample_networks, mh_sample, gof, mcmle and the MPLE bootstrap — resolves an omitted burnin/interval through one dyad-scaled rule (ERGM._mcmc_defaults): a chain that proposes one dyad per step needs a number of steps proportional to the number of dyads to move every dyad a bounded number of times.\n\nNetwork Dyads Default burn-in (20 × n_dyads) Default interval (max(100, n_dyads ÷ 10))\n16 nodes, undirected 120 2,400 100\n50 nodes, undirected 1,225 24,500 122\n200 nodes, undirected 19,900 398,000 1,990\n500 nodes, directed 249,500 4,990,000 24,950\n\nERGM._mcmc_defaults(ERGMModel(ERGMFormula(terms), net))   # (burnin = 2400, interval = 100)\n\n# Override explicitly when the chain mixes slowly (check `mcmc_diagnostics`)\nsim_nets = simulate_ergm(result;\n    n_sim = 100,\n    burnin = 50000,\n    interval = 5000\n)","title":"Choosing Burn-in and Interval"},{"category":"section","location":"guide/simulation/#Simulating-from-a-Model-Specification","page":"Network Simulation","text":"You can also simulate directly from an ERGM model and coefficient vector, without a fitted result:\n\n# Define model\nformula = ERGMFormula(terms)\nmodel = ERGMModel(formula, net)\n\n# Specify coefficients\nθ = [-2.0, 0.5, 0.8]  # edges, triangle, nodematch\n\n# Sample networks\nsim_nets = sample_networks(model, θ; n_sim=50)","title":"Simulating from a Model Specification"},{"category":"section","location":"guide/simulation/#Low-Level-Single-Chain-Sampling","page":"Network Simulation","text":"mh_sample runs one parameterized Metropolis–Hastings chain and returns the sampled sufficient statistics (and, optionally, the sampled networks). It is the primitive that the estimation routines and downstream packages (e.g. ERGMEgo.jl) build on:\n\nusing Statistics\n\nout = mh_sample(model, θ; n_samples=200, burnin=5000, interval=100,\n                rng=Xoshiro(7))\nsize(out.stats)          # (200, 3): one row of g(y) per sample\nmean(out.stats, dims=1)  # E[g] estimate at θ\n\nwith_nets = mh_sample(model, θ; n_samples=10, return_networks=true)\nwith_nets.networks       # the 10 sampled networks themselves","title":"Low-Level Single-Chain Sampling"},{"category":"section","location":"guide/simulation/#Starting-Network","page":"Network Simulation","text":"By default, sample_networks initializes from a random network matching the observed density. You can provide a custom starting network:\n\n# Start from the observed network\nsim_nets = sample_networks(model, θ;\n    n_sim = 50,\n    start_net = net\n)\n\n# Start from an empty network\nempty_net = network(nv(net); directed=is_directed(net))\nsim_nets = sample_networks(model, θ;\n    n_sim = 50,\n    start_net = empty_net\n)","title":"Starting Network"},{"category":"section","location":"guide/simulation/#The-MCMC-Algorithm","page":"Network Simulation","text":"Each simulation step:\n\nPropose: Randomly select a dyad (ij)\nCompute: Calculate change statistics delta(y)_ij for all terms\nAccept/Reject: Toggle the edge with probability:\n\nalpha = minleft(1 expleft(theta^top delta(y)_ijright)right)\n\nIf the edge currently exists, the acceptance probability uses the negative of the change statistics (removing vs. adding).","title":"The MCMC Algorithm"},{"category":"section","location":"guide/simulation/#The-kernel:-mh_toggle!","page":"Network Simulation","text":"Steps 1–3, the burn-in and the thinning are one exported function, mh_toggle!, that knows nothing about networks: the proposal, the change statistics and the state mutation are supplied as callables. ERGM.jl's own samplers are a thin adapter over it (a move is a free dyad (i, j), change! fills change_stat_all! and reports whether the tie is present, apply! toggles it), and the ERGM variants adopt the same kernel with their own moves — TERGM's constrained formation/dissolution toggles, ERGMMulti's (layer, i, j), ERGMRank's rank swaps. The kernel allocates nothing per step and draws from rng in a fixed order, so a chain is bit-identical for a given seed. A state need not be a network at all:\n\nusing Statistics\nstate = Ref(false)                      # one \"tie\", statistic g(y) = y\ndraws = Float64[]\nmh_toggle!(Xoshiro(1), [log(3.0)], [0.0],\n           rng -> 1,                                        # propose: the only move\n           (delta, move) -> (delta[1] = 1.0; state[]),      # change!: Δ = 1, removal if present\n           (move, removal) -> (state[] = !removal),         # apply!\n           k -> push!(draws, state[]);                      # on_sample\n           burnin=100, interval=1, n_samples=20_000)\nround(mean(draws), digits=2)            # ≈ 0.75 = P(y = 1) at θ = log 3","title":"The kernel: mh_toggle!"},{"category":"section","location":"guide/simulation/#Random-Initialization","page":"Network Simulation","text":"When no starting network is provided, a random network is created with edge probability matching the observed network density:\n\n# The density of the observed network\ndensity = ne(net) / (nv(net) * (nv(net) - 1) / 2)  # undirected","title":"Random Initialization"},{"category":"section","location":"guide/simulation/#Missing-(Unobserved)-Dyads","page":"Network Simulation","text":"A simulated network has a value at every dyad, so the samplers refuse a network with dyads masked as missing (set_missing_dyad!) by default and offer one opt-in, missing=:condition_on_face (warned): masked dyads are then never proposed for toggling — every sampled network keeps the stored face value at those dyads, and random starting networks (start_net=nothing) preserve them instead of randomizing — so the sampler draws from the model conditional on the face values. missing=:mle is an estimation policy of mcmle and is refused here with a message saying so.\n\nmh_sample additionally takes toggleable=:all (every dyad, masked ones included — the unconditional model, the free chain of the missing-data MCMLE) and toggleable=:masked (only the masked dyads, observed dyads held fixed — the constrained chain whose mean is E[g(Y) | Y_obs]); neither reads an unobserved value as an observation, so neither takes an opt-in. If the chain has no dyad to toggle, sampling throws an ArgumentError.","title":"Missing (Unobserved) Dyads"},{"category":"section","location":"guide/simulation/#Analyzing-Simulated-Networks","page":"Network Simulation","text":"","title":"Analyzing Simulated Networks"},{"category":"section","location":"guide/simulation/#Comparing-Observed-vs.-Simulated-Properties","page":"Network Simulation","text":"# Edge count comparison\nobs_edges = ne(net)\nsim_edges = [ne(s) for s in sim_nets]\n\nprintln(\"Observed edges: \", obs_edges)\nprintln(\"Simulated edges: \",\n    round(mean(sim_edges), digits=1), \" ± \",\n    round(std(sim_edges), digits=1))","title":"Comparing Observed vs. Simulated Properties"},{"category":"section","location":"guide/simulation/#Checking-Model-Statistics","page":"Network Simulation","text":"# Compute model statistics for each simulated network\nts = TermSet(terms)\n\nfor (i, snet) in enumerate(sim_nets[1:5])\n    stats = compute_all(ts, snet)\n    println(\"Simulation $i: \", round.(stats, digits=2))\nend\n\n# Compare to observed\nobs_stats = compute_all(ts, net)\nprintln(\"Observed:      \", round.(obs_stats, digits=2))","title":"Checking Model Statistics"},{"category":"section","location":"guide/simulation/#Network-Level-Summaries","page":"Network Simulation","text":"using Statistics\n\n# Density\nsim_densities = [ne(s) / (nv(s) * (nv(s) - 1) / 2) for s in sim_nets]\nprintln(\"Simulated density: \",\n    round(mean(sim_densities), digits=4), \" ± \",\n    round(std(sim_densities), digits=4))\n\n# Clustering coefficient (via Graphs.jl)\nusing Graphs\nsim_clustering = [global_clustering_coefficient(s.graph) for s in sim_nets]\nprintln(\"Simulated clustering: \",\n    round(mean(sim_clustering), digits=4), \" ± \",\n    round(std(sim_clustering), digits=4))","title":"Network-Level Summaries"},{"category":"section","location":"guide/simulation/#Exploring-Parameter-Effects","page":"Network Simulation","text":"Use simulation to understand how parameters affect network structure:\n\n# Vary the triangle coefficient\nformula = ERGMFormula([Edges(), Triangle()])\nmodel = ERGMModel(formula, net)\n\nfor tri_coef in [0.0, 0.5, 1.0, 1.5]\n    θ = [-2.0, tri_coef]\n    sims = sample_networks(model, θ; n_sim=20)\n    avg_triangles = mean(compute(Triangle(), s) for s in sims)\n    avg_edges = mean(ne(s) for s in sims)\n    println(\"θ_triangle = $tri_coef → \",\n        \"edges: $(round(avg_edges, digits=1)), \",\n        \"triangles: $(round(avg_triangles, digits=1))\")\nend","title":"Exploring Parameter Effects"},{"category":"section","location":"guide/simulation/#Computational-Notes","page":"Network Simulation","text":"Simulation time scales with burnin + n_sim × interval MCMC steps per chain; independent chains run in parallel when Julia has multiple threads (n_chains keyword)\nEach step involves computing change statistics for all terms\nFor large networks, consider reducing n_sim or increasing interval\nSimulated networks share the same node count and directedness as the observed network\nVertex and network attributes (and the observed network's settings) are inherited by simulated networks, so attribute-based terms evaluate against the same covariates","title":"Computational Notes"},{"category":"section","location":"guide/simulation/#Best-Practices","page":"Network Simulation","text":"Set random seeds: Pass an explicit rng (e.g. rng=Xoshiro(42)) for reproducible simulations\nAdequate burn-in: Ensure the chain has reached stationarity\nSufficient thinning: Reduce autocorrelation between samples\nCompare statistics: Always compare model statistics between observed and simulated networks\nMultiple simulations: Use at least 50–100 simulated networks for stable estimates\nCheck mixing: If simulated networks are very similar, increase the interval","title":"Best Practices"},{"category":"section","location":"api/estimation/#Estimation-API-Reference","page":"Estimation","text":"This page documents the functions for model fitting, simulation, and diagnostics.","title":"Estimation API Reference"},{"category":"section","location":"api/estimation/#Model-Fitting","page":"Estimation","text":"","title":"Model Fitting"},{"category":"section","location":"api/estimation/#ergm-/-fit_ergm","page":"Estimation","text":"ergm is a const alias of fit_ergm (ergm === fit_ergm): one method table, one keyword vocabulary.","title":"ergm / fit_ergm"},{"category":"section","location":"api/estimation/#mple","page":"Estimation","text":"","title":"mple"},{"category":"section","location":"api/estimation/#mcmle","page":"Estimation","text":"","title":"mcmle"},{"category":"section","location":"api/estimation/#mcmc_se","page":"Estimation","text":"","title":"mcmc_se"},{"category":"section","location":"api/estimation/#mcmc_convergence","page":"Estimation","text":"The MCMLE convergence tests as one public (not exported) function, so a variant that solves its own moment equations uses the same rule.","title":"mcmc_convergence"},{"category":"section","location":"api/estimation/#newton*fit-/-logistic*derivatives","page":"Estimation","text":"The shared optimizer and logistic kernel are Networks.jl's (public there) and re-exported by ERGM.jl, so using ERGM provides them unchanged and ERGM.newton_fit === Networks.newton_fit. They are documented on Networks.jl's inference page; ERGM's MPLE is newton_fit applied to the binomial-row form of logistic_derivatives:\n\nusing ERGM\n# Edges-only MPLE of the Florentine marriage network in closed form: 120\n# dyads, 20 ties, so θ̂ = logit(20/120)\nd = logistic_derivatives(ones(1, 1), [120.0], [20.0])\nfit = newton_fit(d, [0.0])\nfit.θ[1] ≈ log(20 / 100)     # true\nfit.converged                # true","title":"newtonfit / logisticderivatives"},{"category":"section","location":"api/estimation/#Simulation","page":"Estimation","text":"","title":"Simulation"},{"category":"section","location":"api/estimation/#mh_toggle!","page":"Estimation","text":"The Metropolis toggle kernel every ERGM-family sampler is built on: the accept/reject arithmetic, burn-in and thinning, with the proposal, the change statistics and the state mutation supplied as callables.","title":"mh_toggle!"},{"category":"section","location":"api/estimation/#mh_sample","page":"Estimation","text":"","title":"mh_sample"},{"category":"section","location":"api/estimation/#Sampler-defaults","page":"Estimation","text":"Every sampler resolves an omitted burnin/interval through one dyad-scaled rule (public, not exported):","title":"Sampler defaults"},{"category":"section","location":"api/estimation/#simulate_ergm","page":"Estimation","text":"","title":"simulate_ergm"},{"category":"section","location":"api/estimation/#sample_networks","page":"Estimation","text":"","title":"sample_networks"},{"category":"section","location":"api/estimation/#Utilities","page":"Estimation","text":"","title":"Utilities"},{"category":"section","location":"api/estimation/#is*dyad*dependent-/-has*dyad*dependent","page":"Estimation","text":"","title":"isdyaddependent / hasdyaddependent"},{"category":"section","location":"api/estimation/#Result-Metadata","page":"Estimation","text":"ERGM.jl implements the ecosystem's result-metadata protocol, so what a fit actually did is programmatically inspectable rather than buried in a show method. Networks.fit_metadata(result) collects these accessors.\n\nThe key one is is_exact: an MPLE fit of a dyad-independent formula is the exact MLE, while the same estimator on a formula containing any dyad-dependent term (Triangle, GWESP, Mutual, ...) is an approximation with anticonservative standard errors. That distinction is exactly what a user needs and cannot otherwise see.\n\nThe missing-data trait a term opts into (see the ecosystem missing-data contract):","title":"Result Metadata"},{"category":"section","location":"api/estimation/#Diagnostics","page":"Estimation","text":"","title":"Diagnostics"},{"category":"section","location":"api/estimation/#gof","page":"Estimation","text":"","title":"gof"},{"category":"section","location":"api/estimation/#mcmc_diagnostics","page":"Estimation","text":"","title":"mcmc_diagnostics"},{"category":"function","location":"api/estimation/#ERGM.fit_ergm","page":"Estimation","text":"fit_ergm(net, terms; method=:mple, kwargs...) -> ERGMResult\nergm(net, terms; method=:mple, kwargs...) -> ERGMResult\n\nFit an ERGM to the observed network net with the model terms — R ergm's ergm(net ~ edges + triangle + ...). ergm is a const alias of fit_ergm (ergm === fit_ergm): the statnet name and the ecosystem's fit_<model> name are one function.\n\nBuilds ERGMModel(ERGMFormula(terms), net) (validating every term against the network — a missing attribute, a directed-only term on an undirected network, or a two-mode network throws an ArgumentError before any fitting) and dispatches on method:\n\nmethod=:mple (default) — maximum pseudo-likelihood, mple. Exact for dyad-independent formulas; fast but approximate under dyadic dependence (see the caveat show prints).\nmethod=:mcmle — Monte-Carlo maximum likelihood, mcmle.\n\nEvery other keyword is forwarded unchanged to the chosen estimator:\n\nkeyword estimator meaning\nverbose both progress output\nse, n_boot, boot_burnin, boot_interval mple :hessian or :bootstrap standard errors and the bootstrap's controls (replicates without a finite MPLE are excluded, warned once)\nmaxiter, tol mple Newton iteration cap / tolerance of the logistic fit\nn_samples, burnin, interval, maxiter mcmle MCMC sample size per iteration, dyad-scaled burn-in / thinning, iteration cap\nconv_threshold, hotelling_alpha, gamma0, max_step_norm mcmle convergence tests and Hummel step control\nbridge_rungs, bridge_samples mcmle path-sampling log-likelihood controls\nmissing mcmle :error (default), :mle (missing-data maximum likelihood) or :condition_on_face for masked dyads\nobs_burnin, obs_interval mcmle controls of the constrained chain under missing=:mle\nrng both the AbstractRNG every Monte-Carlo draw flows from\n\nArguments\n\nnet::Network: the observed (one-mode) network\nterms: a single term, a vector of terms, or a vector mixing terms with vectors of terms — [Edges(), Degree(0:2)] is spliced into one term per degree, as statnet's degree(0:2). Anything else is an ArgumentError naming the offending element; swapping the two positional arguments (fit_ergm(terms, net)) is an ArgumentError too, not a MethodError.\nmethod::Symbol=:mple: :mple or :mcmle\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = ergm(net, [Edges(), NodeCov(:wealth)])            # MPLE (exact here)\nround.(coef(fit); digits=3)                              # [-2.595, 0.011]\nfit_dep = fit_ergm(net, [Edges(), GWESP(0.5)]; method=:mcmle,\n                   n_samples=500, rng=Xoshiro(1))\nfit_dep.converged                                        # true\n\n\n\n\n\n","title":"ERGM.fit_ergm"},{"category":"function","location":"api/estimation/#ERGM.ergm","page":"Estimation","text":"ergm(net, terms; method=:mple, kwargs...) -> ERGMResult\n\nThe statnet-style name of fit_ergm — a const alias, so ergm === fit_ergm and the two names share one method table, one docstring of keywords and one set of error messages. See fit_ergm for the full keyword vocabulary.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nergm(net, [Edges(), NodeCov(:wealth)]) === nothing   # false — a fitted ERGMResult\nergm === fit_ergm                                     # true\n\n\n\n\n\n","title":"ERGM.ergm"},{"category":"function","location":"api/estimation/#ERGM.mple","page":"Estimation","text":"mple(model::ERGMModel; verbose=false, se=:hessian, n_boot=100,\n     boot_burnin=nothing, boot_interval=nothing, maxiter=100, tol=1e-8,\n     rng=Random.default_rng()) -> ERGMResult\n\nFit an ERGM using Maximum Pseudo-Likelihood Estimation: a logistic regression of the dyad indicators on the change statistics, solved by Newton–Raphson on the ecosystem's shared kernel (Networks.logistic_derivatives + Networks.newton_fit) to a tolerance of tol on the pseudo-log-likelihood. On the Florentine marriage network the dyad-independent fit agrees with R ergm to 1e-12 on the coefficients and 5e-8 on the standard errors (provenanced fixture, asserted at 1e-6).\n\nArguments\n\nmodel::ERGMModel: The ERGM model specification\nverbose::Bool=false: Print progress information\nmaxiter::Int=100, tol::Float64=1e-8: Newton iteration cap and convergence tolerance (see Networks.newton_fit). A fit that exhausts maxiter is returned with converged = false and a warning\nse::Symbol=:hessian: How to compute standard errors:\n:hessian — inverse of the observed pseudo-likelihood information. Caution: for models with dyad-dependent terms these are typically anticonservative (too small), because the pseudo-likelihood treats dependent dyads as independent observations; the resulting p-values are then too optimistic. For dyad-independent models the pseudo-likelihood is the true likelihood and these SEs are correct.\n:bootstrap — parametric bootstrap: simulate n_boot networks from the model at the MPLE estimate (via the MCMC sampler), refit the MPLE on each, and use the empirical covariance of the refitted coefficients. Honest under dyad dependence (though the MPLE point estimate itself may still be biased; consider method=:mcmle). A replicate on which the MPLE does not exist — a simulated network with no triangle under Triangle(), no shared-partner edge under GWESP, or a separated design — is excluded from the covariance (its refit is -Inf/NaN, which would make every standard error NaN): ONE warning names how many of n_boot were excluded, fit.boot_replicates holds every refit (the excluded ones as NaN rows) and approximations(fit) records it. At least two finite refits are required (ArgumentError otherwise). A point estimate that itself carries a ±Inf coefficient (see below) cannot be simulated from, so se=:bootstrap is refused for it.\nn_boot::Int=100: Number of bootstrap replicates (only for se=:bootstrap)\nboot_burnin, boot_interval: MCMC controls for the bootstrap simulations; default to the dyad-scaled mcmle defaults (20 * n_dyads and max(100, n_dyads ÷ 10))\nrng::AbstractRNG=Random.default_rng(): Source of the bootstrap simulation randomness (reproducible seeding)\n\nReturns\n\nERGMResult: Fitted model results (se_type records :hessian or :bootstrap)\n\nNote: the reported log-likelihood is the maximized pseudo-log-likelihood; AIC/BIC derived from it are only heuristics for dependence models. dof(fit) counts the finite coefficients and the BIC sample size is the number of dyads they were estimated on (R's logLik df and nobs attributes), so after a drop (below) AIC/BIC are R's numbers too.\n\nPerfect separation that _boundary_columns cannot see — a combination of statistics that predicts every tie — leaves the pseudo-likelihood without a maximum. R warns \"The MPLE does not exist!\"; mple detects the asymptote (a perfectly predicted dyad at a fitted probability within 1e-8 of 0/1 while Newton is still moving) and returns the fit with converged == false, a warning, and the caveat in show and approximations; is_exact(fit) is then false.\n\nA statistic at the boundary of its attainable range — a NodeMatch with no within-group tie, a Triangle on a triangle-free network, … — has no finite MPLE. As R ergm does (its default drop=TRUE), mple warns \"observed statistic(s) … are at their smallest attainable values. Their coefficients will be fixed at -Inf\", returns that coefficient as -Inf (+Inf at the largest value) with standard error 0 and p-value 0, and fits the remaining coefficients on the dyads the dropped statistic does not touch — the exact limit of the pseudo-likelihood, and R's numbers. Networks.approximations(fit) lists the fixed coefficients; show prints a note under the table. mcmle refuses such a model instead (see there).\n\nDyads masked as missing (Networks.set_missing_dyad!) are excluded from the pseudo-likelihood: an unobserved tie status is not a response, so those dyads contribute no logistic-regression row and nobs decreases accordingly. Their face values still condition the change statistics of the observed dyads. This is the standard available-case pseudo-likelihood — a principled treatment of missing data — so mple declares Networks.supports_missing(mple) == true and needs no missing keyword; the fit records missing_method = :available_case (:none when nothing is masked).\n\nCaveat for se=:bootstrap on a masked network: the bootstrap simulates from the fitted model, and a simulated network has a value at every dyad, so the MH sampler conditions the masked dyads on their face value. The point estimate is available-case, but the bootstrap SEs around it are not; a warning is emitted. For maximum likelihood under missing data use mcmle(model; missing=:mle).\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nfit = mple(ERGMModel(ERGMFormula([Edges(), NodeCov(:wealth)]), net))\nfit.converged            # true\nround.(coef(fit); digits=3)   # [-2.595, 0.011]  (R ergm: -2.594929, 0.010546)\n\n\n\n\n\n","title":"ERGM.mple"},{"category":"function","location":"api/estimation/#ERGM.mcmle","page":"Estimation","text":"mcmle(model::ERGMModel; n_samples::Int=1000, burnin=nothing,\n      interval=nothing, maxiter::Int=20, n_chains::Int=1,\n      conv_threshold::Float64=0.1, hotelling_alpha::Float64=0.05,\n      gamma0::Float64=0.1, max_step_norm::Float64=5.0,\n      init=nothing, missing::Symbol=:error,\n      obs_burnin=nothing, obs_interval=nothing,\n      rng=Random.default_rng(), bridge_rungs::Int=16,\n      bridge_samples=nothing, verbose::Bool=false) -> ERGMResult\n\nFit an ERGM using Monte Carlo Maximum Likelihood Estimation.\n\nStarting from the MPLE estimates (or init), each iteration samples networks at the current coefficients and takes a Hummel-style partial Newton step toward the pseudo-target γ·g(y_obs) + (1−γ)·ḡ. The step length γ ∈ (0, 1] starts at gamma0 and adapts upward (at most doubling per iteration) while the observed statistics lie outside the sampled statistic cloud; it reaches γ = 1 once the cloud covers them. Each Newton step is capped at Euclidean norm max_step_norm.\n\nConvergence is declared only at full step length (γ = 1) and when the sampled statistics are statistically indistinguishable from the observed ones: every per-statistic convergence t-ratio (g_obs − ḡ)/sd(g) must be below conv_threshold, and a Hotelling T² test of the mean difference (using the sampled covariance and an autocorrelation-adjusted effective sample size) must be non-significant at level hotelling_alpha. The tests are mcmc_convergence, which is public so the variants (ERGMEgo's moment matching) can use the same machinery.\n\nArguments\n\nmodel::ERGMModel: The ERGM model specification\nn_samples::Int=1000: Number of MCMC samples per iteration (in total, over all chains)\nburnin::Int: Burn-in steps per chain. Defaults to 20 * n_dyads (_mcmc_defaults, the one dyad-scaled rule shared with every sampler), so mixing scales with network size\ninterval::Int: Thinning interval. Defaults to max(100, n_dyads ÷ 10)\nmaxiter::Int=20: Maximum MCMLE iterations (the ecosystem's iteration-cap keyword; max_iter is accepted as a deprecated spelling, with a one-time warning, until the next release)\nn_chains::Int=1: Independent MH chains per iteration (and for the final sample). The n_samples draws are split over the chains, each burned in separately from the observed network and seeded deterministically from rng exactly as sample_networks does, then concatenated; the effective sample size behind the Hotelling test is the sum of the per-chain Geyer ESSs. With several Julia threads the chains run in parallel — the result is identical at any thread count, which is why n_chains never defaults to Threads.nthreads(). n_chains=1 is bit-for-bit the single-chain sampler.\nconv_threshold::Float64=0.1: Threshold for the per-statistic convergence t-ratios\nhotelling_alpha::Float64=0.05: Significance level of the Hotelling T² convergence test\ngamma0::Float64=0.1: Initial Hummel step length\nmax_step_norm::Float64=5.0: Cap on the Euclidean norm of each Newton step\ninit::Vector{Float64}: Starting coefficients (statnet's control.ergm(init=)). Default: the MPLE. Pass coef(previous_fit) to continue an unconverged fit from where it stopped.\nmissing::Symbol=:error: Treatment of dyads masked as missing (Networks.set_missing_dyad!): :error, :mle or :condition_on_face — see the missing-data section below\nobs_burnin::Int, obs_interval::Int: Burn-in and thinning of the constrained chain under missing=:mle (statnet's obs.MCMC.burnin / obs.MCMC.interval). Default to the dyad-scaled rule applied to the number of masked dyads — the only dyads that chain moves\nrng::AbstractRNG=Random.default_rng(): Source of all random draws; runs with the same rng state are exactly reproducible\nbridge_rungs::Int=16: Number of path-sampling segments used for the final log-likelihood estimate (see _bridge_loglik). bridge_rungs=0 skips the estimate: loglik, aic and bic are NaN, show prints \"not estimated\", and approximations(fit) records it. The bridge draws bridge_rungs + 1 further MCMC samples of bridge_samples draws each after the fit is finished — on a single thread it was ~70 % of the total mcmle time in the panel's measurement — and it consumes randomness only after the final sample, so coefficients and standard errors are bit-identical with and without it.\nbridge_samples::Int: MCMC samples per bridge rung (default n_samples)\nverbose::Bool=false: Print progress\n\nThe tol keyword accepted by earlier versions is deprecated and ignored; convergence is now assessed with the statistical tests described above.\n\nNon-convergence is loud\n\nA fit that exhausts maxiter without passing both tests is returned with converged == false and a warning quoting the last max t-ratio, the Hotelling p-value and the step length; Networks.approximations(fit) lists the non-convergence too, show prints the caveat under Converged: false, and fit.mcmc_convergence carries the t-ratios, Hotelling p, effective sample size, iteration count and step length recomputed on the final sample at the returned coefficients. Continue such a fit with mcmle(model; init=coef(fit), ...).\n\nStandard errors\n\nvcov(fit) is the inverse Fisher information estimated from the final MCMC sample, V = Σ̂⁻¹, plus the Monte-Carlo component of the estimate (Hunter & Handcock 2006, §3.3): the estimating equation ḡ(θ) = g_obs is itself Monte Carlo, with Var(ḡ) = Σ_mc — the Geyer initial-sequence asymptotic covariance of the sampled statistics, per-statistic on the diagonal with the lag-0 correlations off-diagonal, divided by the number of draws — so vcov = V + V·Σ_mc·V. The Fisher part alone is fit.vcov_fisher, the Monte-Carlo standard error sqrt(diag(V·Σ_mc·V)) is mcmc_se(fit), and show prints R's summary.ergm \"MCMC %\" column: round(100 · (se − se_fisher) / se) per coefficient — the share of the standard error (not of its variance) that the Monte-Carlo term adds, R's 100 * (tot.se - mod.se) / tot.se. At the default n_samples=1000 it rounds to 0 on a well-mixing chain; a large share says the sample, not the data, is limiting the precision — raise n_samples or n_chains.\n\nBoundary statistics\n\nA statistic at the boundary of its attainable range (a NodeMatch with no within-group tie, …) has no finite MLE; R ergm fixes the coefficient at -Inf and drops the term. mcmle refuses such a model with an ArgumentError quoting R's sentence: a -Inf coefficient has no Newton update and the sampler could not honour it. Remove the term, or fit by mple, which reports the ∓Inf coefficient and R's remaining estimates.\n\nMissing data\n\nA network with dyads masked as missing (set_missing_dyad!) is rejected by default: missing=:error throws the shared Networks.require_observed error, whose last bullets name the two policies mcmle accepts (missing_policies(mcmle) == (:error, :condition_on_face, :mle)).\n\nmissing=:mle — missing-data maximum likelihood (Handcock & Gile 2010; what R ergm() does with NA ties). The likelihood is Z_obs(θ) / Z(θ), the masked dyads integrated out. Each iteration runs two chains: the free chain toggles every dyad (masked ones included — the unconditional model) and gives ḡ = Ê[g(Y)] and Σ_free; the constrained chain toggles only the masked dyads, the observed dyads fixed at their observed values, and gives ĝ_obs = Ê[g(Y) | Y_obs] and Σ_obs. ĝ_obs replaces the observed statistics as the Newton target (θ += γ Σ_free⁻¹ (ĝ_obs − ḡ)), the convergence tests compare the two chains (the difference's variance is Σ_free/n_eff_free + Σ_obs/n_eff_obs), the Fisher information is Σ_free − Σ_obs (Eq. 9 there; standard errors are NaN, with a warning, when it is not positive definite), and both chains contribute Monte-Carlo error to the standard errors. The log-likelihood log Z_obs(θ) − log Z(θ) is two path-sampling bridges (a toggleable=:all one from the exact dyad-independent normalizer over all dyads, a toggleable=:masked one from the exact normalizer over the masked dyads with the observed dyads fixed). The fit records missing_method = :mle; nobs is the number of observed dyads. Under dyad independence the estimate coincides with the available-case MPLE (the exact MLE of the observed dyads).\nmissing=:condition_on_face — hold each masked dyad fixed at its stored face value: never toggled, and scored as recorded in the observed sufficient statistics. That is the model conditional on the face values — a different estimand from the missing-data MLE, right only when the stored values are true by construction (structural zeros, ties fixed by design). Explicit and warned; the fit records missing_method = :condition_on_face.\n\nmple (which declares Networks.supports_missing(mple) == true) drops the masked dyads from the pseudo-likelihood instead. The provenanced flomarriage_missing_ergm.toml fixture pins missing=:mle against R ergm on a masked flomarriage.\n\nThe reported log-likelihood (and hence AIC/BIC) is estimated by path sampling along a bridge_rungs-segment ladder from a dyad-independent reference distribution to the fitted coefficients — the standard ergm-style bridge estimator — rather than a one-jump importance-sampling estimate, which has unusably high variance when θ̂ is far from the reference.\n\nReturns\n\nERGMResult: Fitted model results. mcmc_samples holds the statistics sampled at the final coefficients (all chains concatenated; the free chain under missing=:mle; suitable for mcmc_diagnostics); samples from earlier iterations are discarded since they target different coefficient values.\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nmodel = ERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), net)\nfit = mcmle(model; n_samples=500, rng=Xoshiro(1))\nfit.converged                     # true\nse_method(fit)                    # :fisher — inverse Fisher information (+ MC term) from the MCMC sample\nmaximum(mcmc_se(fit) ./ stderror(fit)) < 0.2   # true: the MC share is small\nfast = mcmle(model; n_samples=500, rng=Xoshiro(1), bridge_rungs=0)\ncoef(fast) == coef(fit)           # true: the bridge only runs after the fit\nisnan(loglikelihood(fast))        # true\n\n# Missing-data MLE on a copy with two unobserved dyads\nmasked = copy(net); set_missing_dyad!(masked, 3, 4); set_missing_dyad!(masked, 1, 9)\nfit_na = mcmle(ERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), masked);\n               n_samples=500, missing=:mle, rng=Xoshiro(2))\nmissing_method(fit_na)            # :mle\nnobs(fit_na)                      # 118\n\n\n\n\n\n","title":"ERGM.mcmle"},{"category":"function","location":"api/estimation/#ERGM.mcmc_se","page":"Estimation","text":"mcmc_se(result::ERGMResult) -> Vector{Float64}\n\nThe Monte-Carlo component of each coefficient's standard error: for an MCMLE fit, sqrt(diag(V·Σ_mc·V)) where V = vcov_fisher is the inverse Fisher information and Σ_mc the Geyer initial-sequence covariance of the sampled statistics' mean (Hunter & Handcock 2006 §3.3) — the uncertainty that comes from the finite MCMC sample rather than from the data. stderror(result) already includes it (se² = se_fisher² + mcmc_se²); show prints R's summary.ergm \"MCMC %\" column, which is the share of the standard error (not of its variance) that the Monte-Carlo term adds: round(100 · (se − se_fisher) / se) with se_fisher = sqrt.(diag(result.vcov_fisher)) — R's 100 * (tot.se - mod.se) / tot.se. A large share says the sample, not the data, limits the precision: raise n_samples/n_chains. Zeros for an MPLE fit (no MCMC sample).\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = fit_ergm(net, [Edges(), GWESP(0.5)]; method=:mcmle, n_samples=1000, rng=Xoshiro(1))\nall(mcmc_se(fit) .< stderror(fit))            # true\nall(mcmc_se(fit_ergm(net, [Edges()])) .== 0)  # true: MPLE has no MC term\n\n\n\n\n\n","title":"ERGM.mcmc_se"},{"category":"function","location":"api/estimation/#ERGM.mcmc_convergence","page":"Estimation","text":"mcmc_convergence(samples::AbstractMatrix, targets::AbstractVector;\n                 conv_threshold=0.1, hotelling_alpha=0.05,\n                 chain_lengths=[size(samples, 1)],\n                 target_samples=nothing, target_chain_lengths=nothing)\n    -> (converged, t_ratios, hotelling_p, n_eff)\n\nThe MCMLE convergence tests, as one public function (panel 2026-09, item 24): given an n × p matrix of statistics sampled at the current coefficients and the p target (observed) statistics, compute\n\nt_ratios: the per-statistic convergence t-ratios |target_j − mean_j| / sd_j (Inf for a statistic with zero sampled variance — a collapsed sampler cannot confirm convergence);\nn_eff: the effective sample size — the smallest over statistics of the Geyer initial-sequence ESS, summed over chains when chain_lengths says the rows are several independent chains concatenated;\nhotelling_p: the p-value of the Hotelling T² test that the sampled mean equals the targets, using the sampled covariance and n_eff (0.0 when the covariance is singular or n_eff ≤ p, where the test is undefined);\nconverged: maximum(t_ratios) < conv_threshold && hotelling_p > hotelling_alpha.\n\nWhen the targets are themselves a Monte-Carlo mean — the constrained chain of the missing-data MCMLE — pass its draws as target_samples (with target_chain_lengths): targets must then equal their column means, the per-draw sd behind the t-ratios becomes sqrt(var_j + var_target_j), and the Hotelling test uses the variance of the difference of the two means, Σ/n_eff + Σ_target/n_eff_target.\n\nmcmle calls this at every full-step-length iteration and once more on the final sample (recorded as fit.mcmc_convergence). ERGMEgo's moment matching and any other equation-solving estimator can use it instead of an ad-hoc relative-change rule.\n\nExample\n\nusing ERGM, Random, Statistics\nrng = Xoshiro(1)\ndraws = randn(rng, 2000, 2) .+ [1.0 5.0]           # a stationary sample around (1, 5)\nok = ERGM.mcmc_convergence(draws, [1.0, 5.0])\nok.converged                                        # true\noff = ERGM.mcmc_convergence(draws, [3.0, 5.0])      # 2 sd away on statistic 1\noff.converged, off.t_ratios[1] > 1.5                # (false, true)\ntwo = ERGM.mcmc_convergence(draws, [1.0, 5.0]; chain_lengths=[1000, 1000])\ntwo.n_eff > 1000                                    # true: ESS adds over chains\n\n\n\n\n\n","title":"ERGM.mcmc_convergence"},{"category":"function","location":"api/estimation/#ERGM.mh_toggle!","page":"Estimation","text":"mh_toggle!(rng::AbstractRNG, θ::AbstractVector, delta::Vector{Float64},\n           propose, change!, apply!, on_sample;\n           burnin::Int, interval::Int, n_samples::Int) -> Int\n\nThe Metropolis toggle kernel every ERGM-family sampler is built on (panel 2026-09, item 28). It owns exactly three things — the accept/reject arithmetic, the burn-in and the thinning — and knows nothing about networks: what a move is, how it changes the sufficient statistics and how it is applied to the state are supplied as callables, so the same loop samples binary networks (ERGM.jl's _mh_run!), formation/dissolution networks (TERGM), multilayer networks (ERGMMulti) and rankings (ERGMRank).\n\nFor burnin + n_samples × interval steps the kernel does\n\nmove    = propose(rng)                 # a proposal, drawn from `rng`\nremoval = change!(delta, move)         # fills Δ, says whether the move removes\naccept  = log(rand(rng)) < (removal ? -1 : 1) * θ'Δ\naccept && apply!(move, removal)        # mutate the state\n\nand after burn-in calls on_sample(k) at every interval-th step, k running from 1 to n_samples. Returns the number of samples recorded.\n\nCallables\n\npropose(rng) -> move: draw the next proposal, consuming only rng. For ERGM it is a free dyad (i, j) — never a masked one, never a loop, i < j on undirected networks. Any immutable value can be a move (a tuple, a struct); the kernel only passes it on.\nchange!(delta, move) -> Bool: fill delta with the add-direction change statistics g(y⁺) − g(y⁻) of the move (the state-independent convention every change_stat method follows) and return true when the move is a removal (the tie is currently present), so that the kernel negates the log-ratio. A sampler whose moves have no removal direction — ERGMRank's rank swaps, whose delta is already the signed difference — returns false always.\napply!(move, removal::Bool): mutate the state for an accepted move (toggle the tie, and keep any running statistics current).\non_sample(k): record sample number k (write a row of a statistics matrix, push a copy of the network, ...).\n\nKeywords\n\nburnin::Int: steps discarded before the first sample (≥ 0).\ninterval::Int: steps between recorded samples (≥ 1).\nn_samples::Int: number of samples to record (≥ 0).\n\nContract\n\nθ and delta must have the same length; delta is the caller's workspace and is overwritten at every step. The kernel draws from rng in a fixed order — the proposal first (through propose), then one uniform for the acceptance test — so for the same rng state, propose and change! the sampled sequence is bit-identical to ERGM.jl's pre-0.2 hand-written loop (pinned by the \"MH kernel\" testset). It allocates nothing per step when the callables do not (@allocated is pinned to grow by 0 bytes between burnin=10_000 and burnin=20_000): keep the move a stack value and the closures free of reassigned captured variables.\n\nAdopting the kernel in a variant\n\nTERGM _sample_constrained — moves are dyads drawn from the free list (non-edges of the previous network for formation, its edges for dissolution), the change statistics are the temporal _tchange, no samples are recorded (the state after steps toggles is the draw):\n\npropose = rng -> free[rand(rng, 1:length(free))]\nchange! = (delta, move) -> begin\n    i, j = move\n    for (k, t) in enumerate(terms); delta[k] = _tchange(t, net, i, j, prev); end\n    has_edge(net, i, j)\nend\napply! = (move, removal) -> begin\n    i, j = move\n    removal ? rem_edge!(net, i, j) : add_edge!(net, i, j)\n    nothing\nend\nmh_toggle!(rng, θ, delta, propose, change!, apply!, _ -> nothing;\n           burnin=steps, interval=1, n_samples=0)\n\n(A sketch in TERGM's own names — free, net, prev, _tchange — not a standalone program, hence the jl fence; the runnable example is below.)\n\nERGMMulti — a move is (layer, i, j); change! calls change_stat_layer(t, current, l, i, j) and returns has_edge(current.layers[l], i, j); apply! toggles that layer's edge; on_sample pushes a copy of the multilayer network. This is real code now — ERGMMulti._multi_mh_run, pinned bit-identical to a hand-written mh_toggle! call in ERGMMulti's tests.\n\nERGMRank — a move is a swap (ego, j, k); change! fills delta with _swap_delta(terms, current, ego, j, k) and returns false (a swap has no removal direction); apply! is swap_ranks!(current, ego, j, k); on_sample pushes copy(current).\n\nERGMCount does not adopt it: its sampler is a Gibbs sweep that redraws each dyad's count from its full conditional, not a Metropolis toggle.\n\nExample\n\nA two-state toy chain — a single \"tie\" with statistic g = y, sampled at θ = log(3), so P(y = 1) = 3/4:\n\nusing ERGM, Random, Statistics\nstate = Ref(false)\ndraws = Float64[]\nmh_toggle!(Xoshiro(1), [log(3.0)], [0.0],\n           rng -> 1,                              # the only move\n           (delta, move) -> (delta[1] = 1.0; state[]),\n           (move, removal) -> (state[] = !removal),\n           k -> push!(draws, state[]);\n           burnin=100, interval=1, n_samples=20_000)\nabs(mean(draws) - 0.75) < 0.02    # true\n\n\n\n\n\n","title":"ERGM.mh_toggle!"},{"category":"function","location":"api/estimation/#ERGM.mh_sample","page":"Estimation","text":"mh_sample(model::ERGMModel, θ::Vector{Float64};\n          n_samples::Int=100, burnin=nothing, interval=nothing,\n          rng::AbstractRNG=Random.default_rng(),\n          start_net::Union{Nothing,Network}=nothing,\n          return_networks::Bool=false, missing::Symbol=:error,\n          toggleable::Symbol=:free)\n    -> (stats::Matrix{Float64}, networks::Union{Nothing,Vector{<:Network}})\n\nRun one Metropolis–Hastings edge-toggle chain of the ERGM exp(θ'g(y)) defined by model's terms, and return the sampled sufficient statistics (and optionally the sampled networks).\n\nThis is the public single-chain sampling primitive that the estimation routines (and downstream packages such as ERGMEgo.jl) build on. Each MH step proposes toggling a uniformly random dyad and accepts with probability min(1, exp(±θ'Δ)), where Δ is the add-direction change statistic vector of the dyad.\n\nWhich dyads the chain may toggle is toggleable:\n\n:free (default) — every dyad that is not masked as missing (Networks.set_missing_dyad!). A masked dyad is then frozen at its stored face value (edge present/absent as recorded), never proposed, so the chain conditions on it. Reinterpreting an unobserved tie as an observed one is never silent, so with :free a masked network is rejected unless the caller opts in with missing=:condition_on_face. Throws ArgumentError if every dyad is masked.\n:all — every dyad, masked ones included: the chain samples the unconditional model exp(θ'g(y)) over the whole dyad set, as the free chain of the missing-data MCMLE (mcmle with missing=:mle) does. No unobserved value is read as an observation (the face values only seed the starting state, which burn-in washes out), so no missing= opt-in is needed; missing must be left at :error.\n:masked — only the masked dyads, every observed dyad held fixed at its observed value: the constrained chain of the missing-data MCMLE, whose mean estimates E[g(Y) | Y_obs] (Handcock & Gile 2010). Requires n_missing_dyads(model.network) > 0; missing must be left at :error.\n\nArguments\n\nmodel::ERGMModel: Model specification (terms + network template). The chain runs on a copy; model.network is never mutated.\nθ::Vector{Float64}: Natural-parameter vector, one entry per term.\n\nKeywords\n\nn_samples::Int=100: Number of recorded samples.\nburnin::Int: MH steps discarded before the first sample. Defaults to 20 × n_dyads (the shared dyad-scaled rule, _mcmc_defaults), where n_dyads is the number of dyads the chain may toggle.\ninterval::Int: MH steps between recorded samples (thinning). Defaults to max(100, n_dyads ÷ 10).\nrng::AbstractRNG=Random.default_rng(): Source of all random draws; same rng state ⇒ identical output.\nstart_net=nothing: Starting network. Defaults to a copy of the observed model.network (attributes are preserved either way).\nreturn_networks::Bool=false: Also collect a copy of the network at every sampling point.\nmissing::Symbol=:error: :error rejects a network with masked dyads (under toggleable=:free); :condition_on_face explicitly holds them at their face value.\ntoggleable::Symbol=:free: :free, :all or :masked — see above.\n\nReturns\n\nA NamedTuple (stats, networks):\n\nstats: n_samples × p matrix; row k holds g(y⁽ᵏ⁾).\nnetworks: Vector of the sampled networks if return_networks=true, otherwise nothing.\n\nExample\n\nusing ERGM, Random, Statistics\nnet = load_dataset(:florentine_marriage)\nmodel = ERGMModel(ERGMFormula([Edges(), Triangle()]), net)\nout = mh_sample(model, [-1.5, 0.3]; n_samples=500, rng=Xoshiro(1))\nsize(out.stats)            # (500, 2)\nmean(out.stats, dims=1)    # E[g] estimate at θ\n\n# The two chains of the missing-data MCMLE on a masked copy\nmasked = copy(net); set_missing_dyad!(masked, 3, 4); set_missing_dyad!(masked, 1, 9)\nmm = ERGMModel(ERGMFormula([Edges(), Triangle()]), masked)\nfree = mh_sample(mm, [-1.5, 0.3]; n_samples=200, toggleable=:all, rng=Xoshiro(2))\ncons = mh_sample(mm, [-1.5, 0.3]; n_samples=200, toggleable=:masked, rng=Xoshiro(3),\n                 return_networks=true)\nall(has_edge(s, 2, 6) for s in cons.networks)   # true: observed ties never move\n\n\n\n\n\n","title":"ERGM.mh_sample"},{"category":"function","location":"api/estimation/#ERGM._mcmc_defaults","page":"Estimation","text":"_mcmc_defaults(model::ERGMModel) -> (burnin, interval)\n_mcmc_defaults(n_dyads::Int) -> (burnin, interval)\n\nTHE dyad-scaled rule behind every sampler default in ERGM.jl (panel 2026-09, item 24e): burnin = 20 × n_dyads toggles and interval = max(100, n_dyads ÷ 10) toggles between recorded draws, where n_dyads is the number of free (unmasked) dyads (ERGM._n_dyads). A Metropolis chain that proposes one dyad per step needs a number of steps proportional to the number of dyads to move every dyad a bounded number of times, so a fixed budget (the pre-0.2 10000/1000) was both far too small for a 500-node network and wastefully large for a 16-node one.\n\nmcmle, the MPLE parametric bootstrap, mh_sample, sample_networks, simulate_ergm and gof all resolve a burnin=nothing/interval=nothing keyword through this one function, so the budgets cannot drift apart.\n\nExample\n\nusing ERGM\nmodel = ERGMModel(ERGMFormula([Edges()]), load_dataset(:florentine_marriage))\nERGM._mcmc_defaults(model)     # (burnin = 2400, interval = 100): 120 dyads\nERGM._mcmc_defaults(124750)    # (burnin = 2495000, interval = 12475): n = 500\n\n\n\n\n\n","title":"ERGM._mcmc_defaults"},{"category":"function","location":"api/estimation/#ERGM.simulate_ergm","page":"Estimation","text":"simulate_ergm(result::ERGMResult; n_sim::Int=1, burnin=nothing,\n              interval=nothing, rng::AbstractRNG=Random.default_rng(),\n              n_chains::Int=min(n_sim, 4),\n              missing::Symbol=:error) -> Vector{Network}\n\nSimulate networks from a fitted ERGM (at result.coefficients).\n\nIf the fitted network has dyads masked as missing, the simulation cannot reinterpret them: the sampler would silently freeze each unobserved tie at its stored face value and report it as a simulated tie. That requires the explicit missing=:condition_on_face opt-in (a warning is emitted); the default missing=:error refuses. This holds even for an MPLE fit, whose point estimate excluded the masked dyads: simulation is a separate act.\n\nArguments\n\nresult::ERGMResult: Fitted ERGM result\nn_sim::Int=1: Number of networks to simulate\nburnin::Int: MCMC burn-in steps (per chain). Defaults to 20 × n_dyads (the shared dyad-scaled rule, _mcmc_defaults)\ninterval::Int: Steps between samples. Defaults to max(100, n_dyads ÷ 10)\nrng::AbstractRNG: Source of all random draws (reproducible seeding)\nn_chains::Int: Number of independent chains (see sample_networks)\nmissing::Symbol=:error: Missing-dyad policy (:error or :condition_on_face)\n\nReturns\n\nVector of simulated Network objects\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = fit_ergm(net, [Edges(), NodeCov(:wealth)])\nsims = simulate_ergm(fit; n_sim=10, rng=Xoshiro(1))\nlength(sims)                       # 10\nall(nv(s) == 16 for s in sims)     # true\n\n\n\n\n\n","title":"ERGM.simulate_ergm"},{"category":"function","location":"api/estimation/#ERGM.sample_networks","page":"Estimation","text":"sample_networks(model::ERGMModel, θ::Vector{Float64};\n                n_sim::Int=1, burnin=nothing, interval=nothing,\n                start_net::Union{Nothing,Network}=nothing,\n                rng::AbstractRNG=Random.default_rng(),\n                n_chains::Int=min(n_sim, 4),\n                missing::Symbol=:error) -> Vector{Network{T,D}}\n\nSample networks from an ERGM specification. The returned vector is concretely typed (Vector{Network{T,D}}, the model's own network type).\n\nThe n_sim draws are split over n_chains independent MH chains run in parallel (Threads.@spawn), each burned in separately and seeded deterministically from rng — so for a fixed rng state and n_chains the result is identical regardless of the number of threads. n_chains deliberately does not default to Threads.nthreads(), precisely to keep results thread-count-independent.\n\nArguments\n\nmodel::ERGMModel: ERGM model specification\nθ::Vector{Float64}: Model coefficients\nn_sim::Int=1: Number of networks to sample\nburnin::Int: MCMC burn-in steps (per chain). Defaults to 20 × n_dyads (the shared dyad-scaled rule, _mcmc_defaults)\ninterval::Int: Steps between samples. Defaults to max(100, n_dyads ÷ 10)\nstart_net: Starting network for every chain (default: an independent Bernoulli(density) random network per chain, with the observed network's attributes)\nrng::AbstractRNG: Source of all random draws\nn_chains::Int: Number of independent chains\nmissing::Symbol=:error: Missing-dyad policy. :error refuses a network with masked dyads; :condition_on_face explicitly freezes each masked dyad at its stored face value in every chain (_random_network starting states preserve them too).\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nmodel = ERGMModel(ERGMFormula([Edges(), Triangle()]), net)\nsims = sample_networks(model, [-1.6, 0.2]; n_sim=8, n_chains=2, rng=Xoshiro(1))\nlength(sims)                        # 8\nsims isa Vector{Network{Int,false}} # true\n\n\n\n\n\n","title":"ERGM.sample_networks"},{"category":"function","location":"api/estimation/#ERGM.is_dyad_dependent","page":"Estimation","text":"is_dyad_dependent(term::AbstractERGMTerm) -> Bool\n\nWhether the term's change statistic depends on the state of other dyads (true for e.g. Mutual, Triangle, Kstar, TwoPath, GWESP, GWDegree) or only on exogenous covariates of the dyad itself (false for Edges and all nodal/dyadic covariate terms).\n\nFor dyad-independent models the pseudo-likelihood is the likelihood, so MPLE is exact; for models with any dyad-dependent term MPLE point estimates can be biased and its inverse-Hessian standard errors are typically anticonservative (see mple).\n\nThe fallback for term types this package does not know about is true (the conservative answer). Packages defining their own dyad-independent terms should add a method returning false.\n\nExample\n\nusing ERGM\nis_dyad_dependent(Edges())          # false\nis_dyad_dependent(NodeCov(:x))      # false\nis_dyad_dependent(GWESP(0.5))       # true\n\n\n\n\n\n","title":"ERGM.is_dyad_dependent"},{"category":"function","location":"api/estimation/#ERGM.has_dyad_dependent","page":"Estimation","text":"has_dyad_dependent(model::ERGMModel) -> Bool\n\nWhether the model's formula contains any dyad-dependent term (see is_dyad_dependent; unknown term types count as dependent).\n\nThis is THE predicate that decides whether a pseudo-likelihood fit is an approximation: for a dyad-independent formula the MPLE is the MLE and the inverse-Hessian standard errors are the exact ML ones. It is defined once and used by both show(::ERGMResult) (which prints the prose caveat) and is_exact(::ERGMResult) (which reports the same fact to a machine), so the two can never disagree. The ERGM variants (ERGMCount, ERGMMulti, TERGM) add methods for their own model types rather than defining same-named privates.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nhas_dyad_dependent(ERGMModel(ERGMFormula([Edges(), NodeCov(:wealth)]), net))  # false\nhas_dyad_dependent(ERGMModel(ERGMFormula([Edges(), Triangle()]), net))        # true\n\n\n\n\n\n","title":"ERGM.has_dyad_dependent"},{"category":"method","location":"api/estimation/#Networks.objective-Tuple{ERGMResult}","page":"Estimation","text":"objective(result::ERGMResult) -> Symbol\n\n:pseudolikelihood for an MPLE fit (dyadwise conditionals multiplied as if independent) and :mc_likelihood for MCMLE (a Monte-Carlo approximation to the likelihood). Part of the shared result-metadata protocol.\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges()])\nobjective(fit)      # :pseudolikelihood\n\n\n\n\n\n","title":"Networks.objective"},{"category":"method","location":"api/estimation/#Networks.is_exact-Tuple{ERGMResult}","page":"Estimation","text":"is_exact(result::ERGMResult) -> Bool\n\ntrue only for a converged MPLE fit of a dyad-independent formula: there the dyadwise conditionals are the model's conditionals, so the pseudo-likelihood is the likelihood and the MPLE is the exact MLE. The very same estimator applied to a formula containing any dyad-dependent term (Triangle, GWESP, Mutual, ...) reports false, and so does a fit whose pseudo-likelihood has no finite maximum — perfect separation (converged == false: the coefficients are a point on an asymptote, not an estimate) or a coefficient fixed at ±Inf by a boundary statistic (R's drop): an extended-value estimate is not the exact MLE. MCMLE is always false — it maximizes a Monte-Carlo approximation to the likelihood.\n\nExample\n\nusing ERGM\nnet = load_dataset(:florentine_marriage)\nis_exact(fit_ergm(net, [Edges(), NodeCov(:wealth)]))   # true — MPLE is the MLE here\nis_exact(fit_ergm(net, [Edges(), Triangle()]))         # false — dyad-dependent\n\n\n\n\n\n","title":"Networks.is_exact"},{"category":"method","location":"api/estimation/#Networks.se_method-Tuple{ERGMResult}","page":"Estimation","text":"se_method(result::ERGMResult) -> Symbol\n\n:hessian (inverse observed information of the objective), :bootstrap (parametric bootstrap), or :fisher — the se_type == :mcmc case, where the covariance is the inverse Fisher information estimated from the MCMC sample plus the Monte-Carlo component of the estimate (V + V·Σ_mc·V, Hunter & Handcock 2006 §3.3; the pure Fisher part is result.vcov_fisher, the added standard error is mcmc_se).\n\nExample\n\nusing ERGM\nfit = fit_ergm(load_dataset(:florentine_marriage), [Edges()])\nse_method(fit)      # :hessian\n\n\n\n\n\n","title":"Networks.se_method"},{"category":"method","location":"api/estimation/#Networks.supports_missing-Tuple{AbstractERGMTerm}","page":"Estimation","text":"supports_missing(term::AbstractERGMTerm) -> Bool\n\nWhether the term's statistic honours the missing-dyad mask: that is, whether compute(term, net) is invariant to the face value of any dyad marked with Networks.set_missing_dyad!.\n\nThis is the term-level method of the ecosystem missing-data trait (Networks.supports_missing; see src/missing.jl for how ERGM's estimators declare it). It is false for every built-in term, and that is the honest answer: compute(Edges(), net) counts a masked dyad's stored edge at face value. The estimators are where ERGM's principled treatment lives — MPLE drops masked dyads from the pseudo-likelihood — so a false declaration here is not a defect, merely a statement of fact.\n\nA term declares true only if it consults the mask itself. ERGMUserterms.validate_term tests the declaration by masking a dyad, flipping its face value, and requiring the statistic not to move.\n\nExample\n\nusing ERGM\nsupports_missing(Edges())             # false — counts a masked tie at face value\nsupports_missing(mple)                # true  — the estimator drops masked dyads\nstruct ObservedEdges <: AbstractERGMTerm end\nfunction ERGM.compute(::ObservedEdges, net)\n    total = 0.0\n    for e in edges(net)\n        is_missing_dyad(net, e.src, e.dst) && continue\n        total += 1.0\n    end\n    return total\nend\nERGM.supports_missing(::ObservedEdges) = true\nnet = load_dataset(:florentine_marriage); set_missing_dyad!(net, 1, 9)\ncompute(ObservedEdges(), net)         # 19.0\nsupports_missing(ObservedEdges())     # true\n\n\n\n\n\n","title":"Networks.supports_missing"},{"category":"function","location":"api/estimation/#Networks.gof","page":"Estimation","text":"gof(result::ERGMResult; n_sim::Int=100,\n    stats::Vector{Symbol}=[:degree, :esp, :distance],\n    burnin=nothing, interval=nothing,\n    rng::AbstractRNG=Random.default_rng(),\n    n_chains::Int=min(n_sim, 4), esp_type::Symbol=:OTP,\n    missing::Symbol=:error) -> GOFResult\n\nGoodness-of-fit assessment for a fitted ERGM.\n\nCompares observed network statistics to distributions from simulated networks. The simulations are split over n_chains independent MCMC chains run in parallel and seeded deterministically from rng, so results are reproducible and independent of the thread count (see sample_networks).\n\nFor directed networks the :degree statistic is split into separate in- and out-degree distributions, reported under the keys :idegree and :odegree (as R ergm's GOF does); :idegree/:odegree may also be requested individually. The :esp distribution for directed networks uses the esp_type shared-partner definition (default :OTP, statnet's directed default — the same types as GWESP, including :union for the either-direction count).\n\nArguments\n\nresult::ERGMResult: Fitted ERGM result\nn_sim::Int=100: Number of networks to simulate\nstats::Vector{Symbol}: Statistics to evaluate (:degree, :idegree, :odegree, :esp, :distance); any other symbol is an ArgumentError raised before a single network is simulated\nburnin::Int, interval::Int: MCMC controls for the simulations. Default to the dyad-scaled rule every sampler shares (_mcmc_defaults): 20 × n_dyads and max(100, n_dyads ÷ 10)\nrng::AbstractRNG: Source of all random draws\nn_chains::Int: Number of independent simulation chains\nesp_type::Symbol=:OTP: Directed shared-partner type for :esp (ignored for undirected networks)\nmissing::Symbol=:error: Missing-dyad policy. GOF reads the observed network's degree/ESP/geodesic distributions at face value and simulates from the model, so a masked dyad would be silently reinterpreted as an observed tie on both sides of the comparison. :error (the default) refuses; :condition_on_face is the explicit, warned opt-in.\n\nReturns\n\nA Networks.GOFResult with one GOFStatistic panel per requested statistic (named \"degree\", \"idegree\", \"odegree\", \"esp\", \"distance\"). Per-level p-values are two-sided Monte-Carlo p-values computed with the shared (1 + k)/(N + 1) estimator (Networks.mc_pvalue), so they are never exactly zero. show renders the observed value, simulation envelope, and p-value per level.\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = fit_ergm(net, [Edges(), NodeCov(:wealth)])\ng = gof(fit; n_sim=20, stats=[:degree, :esp], rng=Xoshiro(1))\nn_simulations(g)                         # 20\n[s.name for s in g.statistics]           # [\"degree\", \"esp\"]\n\n\n\n\n\n","title":"Networks.gof"},{"category":"function","location":"api/estimation/#ERGM.mcmc_diagnostics","page":"Estimation","text":"mcmc_diagnostics(result::ERGMResult) -> MCMCDiagnostics\n\nMCMC diagnostics for MCMLE results: per statistic, the lag-1 autocorrelation, two effective sample sizes and the Geweke stationarity test, as an MCMCDiagnostics whose show prints the table R's mcmc.diagnostics prints.\n\nThrows an ArgumentError if the fit has no MCMC samples (i.e. it was estimated by MPLE): there is no chain to diagnose. Refit with method=:mcmle to obtain a result this function accepts.\n\nWith n_chains > 1 every quantity is computed within each chain and combined — the lag-1 autocorrelation length-weighted, both effective sample sizes summed (the Geyer column is then exactly the MCMLE's own fit.mcmc_convergence.n_eff estimator), the Geweke test reported for the chain of largest |z| — never across the seams of the concatenated sample, where the first 10 % of chain 1 would otherwise be compared with the tail of the last chain.\n\nReturns\n\nAn MCMCDiagnostics with fields term_names, autocorrelation, effective_sample_size (lag-1 estimate, Σ_c L_c(1−ρ_c)/(1+ρ_c), the most optimistic column), ess_geyer (Geyer initial-sequence ESS, all lags, summed over chains — preferred), geweke_z/geweke_p (first 10 % vs last 50 % of a chain; small p flags a chain that had not reached stationarity), n_samples and chain_lengths.\n\nExample\n\nusing ERGM, Random\nnet = load_dataset(:florentine_marriage)\nfit = mcmle(ERGMModel(ERGMFormula([Edges(), GWESP(0.5)]), net);\n            n_samples=300, bridge_rungs=0, rng=Xoshiro(1))\nd = mcmc_diagnostics(fit)\nd.term_names                       # [\"edges\", \"gwesp.fixed.0.5\"]\nall(d.ess_geyer .> 20)             # true on a mixing chain\nall(d.geweke_p .> 0.001)           # true\nprintln(d)                         # one row per term: AC, ESS (lag-1), ESS (Geyer), Geweke z, p\n\n\n\n\n\n","title":"ERGM.mcmc_diagnostics"},{"category":"section","location":"guide/estimation/#Model-Estimation","page":"Model Estimation","text":"ERGM.jl provides two estimation methods: Maximum Pseudo-Likelihood Estimation (MPLE) for fast approximation and Monte Carlo Maximum Likelihood Estimation (MCMLE) for accurate inference. Both methods return an ERGMResult object containing coefficients, standard errors, and fit statistics.","title":"Model Estimation"},{"category":"section","location":"guide/estimation/#Overview","page":"Model Estimation","text":"The estimation process differs by method:\n\nMPLE:\n\nBuild a (compressed) design matrix of change statistics for all observed dyads\nFit the logistic regression by Newton–Raphson on the ecosystem's shared kernel (Networks.logistic_derivatives + Networks.newton_fit)\nCompute standard errors from the observed information matrix at the solution\n\nMCMLE:\n\nInitialize with MPLE estimates\nIterate: sample networks via MCMC, update parameters via Newton-Raphson\nConverge when observed statistics match the expected statistics under the model","title":"Overview"},{"category":"section","location":"guide/estimation/#Maximum-Pseudo-Likelihood-Estimation-(MPLE)","page":"Model Estimation","text":"MPLE treats each potential edge as an independent observation and fits a logistic regression model using change statistics as features:\n\ntextlogitleft(P(Y_ij = 1 mid Y_-ij)right) = theta^top delta(y)_ij\n\nusing Networks, ERGM\nusing Random\n\nRandom.seed!(42)\n\n# Example network: Florentine marriage ties with a categorical attribute\nnet = load_dataset(:florentine_marriage)\nset_vertex_attribute!(net, :gender,\n    Dict(v => (isodd(v) ? \"F\" : \"M\") for v in 1:nv(net)))\nterms = [Edges(), GWESP(0.5), NodeMatch(:gender)]\n\nresult = ergm(net, terms; method=:mple)\n\nOr equivalently:\n\nformula = ERGMFormula(terms)\nmodel = ERGMModel(formula, net)\nresult = mple(model)","title":"Maximum Pseudo-Likelihood Estimation (MPLE)"},{"category":"section","location":"guide/estimation/#How-MPLE-Works","page":"Model Estimation","text":"For a network with n nodes:\n\nNetwork Type Number of Dyads\nDirected n(n-1)\nUndirected n(n-1)2\n\nFor each dyad (ij):\n\nResponse: y_ij = 1 if edge exists, 0 otherwise\nFeatures: Change statistics delta(y)_ij for each model term\n\nDyads with identical change-statistic rows are collapsed into one binomial row (n_tot dyads, n_one of them ties), and the logistic regression is solved by Newton–Raphson with step halving on Networks.jl's shared logistic_derivatives/newton_fit pair, to a tolerance of 1e-8 on the pseudo-log-likelihood (maxiter=100; an unconverged fit warns and records converged = false). On the Florentine marriage network the dyad-independent fit agrees with R ergm to 1e-12 on the coefficients and 5e-8 on the standard errors; the provenanced fixture asserts 1e-6.","title":"How MPLE Works"},{"category":"section","location":"guide/estimation/#MPLE-Strengths-and-Limitations","page":"Model Estimation","text":"Aspect Detail\nSpeed Very fast — single optimization\nConsistency Consistent for dyadic independence models\nBias Can be biased for models with strong dependencies\nStandard errors Anticonservative (too small) for dependent models\nUse case Exploration, initial estimates, independent-dyad models","title":"MPLE Strengths and Limitations"},{"category":"section","location":"guide/estimation/#Honest-MPLE-Uncertainty","page":"Model Estimation","text":"For dyad-independent models (only Edges plus nodal/dyadic covariate terms) the pseudo-likelihood is the likelihood, and the default inverse-Hessian standard errors are correct.\n\nFor models with dyad-dependent terms (Triangle, GWESP, Mutual, ...) the pseudo-likelihood treats dependent dyads as independent observations, so the inverse-Hessian standard errors are typically anticonservative and the p-values too optimistic. show(result) prints a caveat in this case (mirroring statnet's warning). Two remedies:\n\n# Parametric-bootstrap standard errors: simulate n_boot networks at the\n# MPLE, refit the MPLE on each, use the empirical covariance\nresult_boot = ergm(net, terms; method=:mple, se=:bootstrap, n_boot=100,\n                   rng=Xoshiro(1))\nresult_boot.se_type   # :bootstrap\n\n# Or refit with full MCMC maximum likelihood\nresult_mcmle = ergm(net, terms; method=:mcmle, rng=Xoshiro(2))\n\nThe bootstrap call above prints, for this model and seed:\n\n┌ Warning: mple: se=:bootstrap — 9 of the 100 bootstrap refits had no finite MPLE (the simulated network put a statistic at the boundary of its attainable range — e.g. no triangle, no shared partner — or perfectly separated the ties) and were excluded; the standard errors are the empirical covariance of the 91 finite refits. This is about the simulated replicates, not about the observed network. …\nERGM Results\n============\nMethod: mple\nLog-likelihood: -53.8523\nAIC: 113.7, BIC: 122.07\nConverged: true\n\nCoefficients:\n                  Estimate  Std.Error  z value  Pr(>|z|)\nedges              -1.7661     0.4774  -3.6993    0.0002 ***\ngwesp.fixed.0.5     0.0973     0.2906   0.3349    0.7377\nnodematch.gender    0.1061     0.5782   0.1836    0.8544\n---\nSignif. codes: 0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nNote: 9 of the 100 parametric-bootstrap refits had no finite MPLE (a statistic at the boundary of its attainable range, or a separated design, in the simulated network) and were excluded from the standard errors, which are the empirical covariance of the remaining 91 refits (fit.boot_replicates)\n\nNote: this model contains dyad-dependent terms and was fit by maximum\npseudolikelihood (MPLE). Standard errors are parametric-bootstrap\nestimates; the MPLE point estimates may still be biased. Consider\nrefitting with method=:mcmle.\n\nA simulated 16-node network with no shared-partner edge has no finite MPLE for gwesp.fixed.0.5 (its coefficient is -Inf under R's drop semantics), and such a replicate would make every standard error NaN if it entered the covariance. Replicates without a finite MPLE are therefore excluded, said once (the warning is about the simulated replicates, not about the data), recorded in approximations(result_boot) and kept as NaN rows of result_boot.boot_replicates; at least two finite refits are required. A point estimate that itself carries a ±Inf coefficient cannot be simulated from, so se=:bootstrap refuses it with an ArgumentError.\n\nNote the bootstrap fixes the standard errors, not the MPLE point estimates, which can themselves be biased under strong dependence — method=:mcmle addresses both. Which terms count as dyad-dependent is queryable with is_dyad_dependent.","title":"Honest MPLE Uncertainty"},{"category":"section","location":"guide/estimation/#Monte-Carlo-Maximum-Likelihood-Estimation-(MCMLE)","page":"Model Estimation","text":"MCMLE uses MCMC sampling to approximate the normalizing constant ratio, providing more accurate estimates for models with dependence:\n\nresult = ergm(net, terms;\n    method = :mcmle,\n    n_samples = 1000,\n    maxiter = 20,\n    rng = Xoshiro(42),\n    verbose = true\n)\nresult.converged\n\nburnin and interval default to the dyad-scaled rule every sampler in ERGM.jl shares (20 × n_dyads and max(100, n_dyads ÷ 10)); pass them explicitly to override. Two further controls change the cost, not the estimate:\n\n# Split each iteration's 1000 draws over 4 independent chains (run in\n# parallel when Julia has threads; the result is identical at any thread\n# count because the chains are seeded from `rng`)\nresult4 = ergm(net, terms; method=:mcmle, n_samples=1000, n_chains=4, rng=Xoshiro(42))\n\n# Skip the path-sampling log-likelihood (~70 % of a single-threaded fit's\n# time on the panel's measurement): coefficients and standard errors are\n# bit-identical, `loglikelihood`/`aic`/`bic` are NaN and `show` says so\nfast = ergm(net, terms; method=:mcmle, n_samples=1000, rng=Xoshiro(42), bridge_rungs=0)\ncoef(fast) == coef(ergm(net, terms; method=:mcmle, n_samples=1000, rng=Xoshiro(42)))\nisnan(loglikelihood(fast))","title":"Monte Carlo Maximum Likelihood Estimation (MCMLE)"},{"category":"section","location":"guide/estimation/#How-MCMLE-Works","page":"Model Estimation","text":"The algorithm iterates:\n\nInitialize: Start with MPLE estimates theta^(0)\nSample: Generate networks from the current model P_theta^(t) via Metropolis-Hastings MCMC\nCheck convergence: at full Hummel step length, stop when every per-statistic convergence t-ratio (g_textobs - barg)textsd(g) is below conv_threshold and a Hotelling T^2 test of the mean difference is non-significant at hotelling_alpha\nUpdate: partial Newton-Raphson step toward the Hummel pseudo-target: theta^(t+1) = theta^(t) + gammaSigma^-1(g(textobs) - barg(textsim)), with step length gamma adapting toward 1 as the sampled statistic cloud covers the observed statistics\nRepeat until convergence or maxiter reached — an unconverged fit is returned with converged == false, a warning quoting the last max t-ratio, Hotelling p-value and step length, an entry in Networks.approximations(result), a caveat line under Converged: false in show, and the numbers themselves in result.mcmc_convergence (see Non-convergence below)\n\nThe convergence tests are one public function, ERGM.mcmc_convergence(samples, targets; conv_threshold, hotelling_alpha, chain_lengths), so a variant that solves its own moment equations (ERGMEgo) uses the same rule.\n\nThe reported log-likelihood (and AIC/BIC) is estimated by path sampling: a bridge_rungs-segment ladder from a dyad-independent reference distribution (whose normalizer is exact) to hattheta, with the expected statistics at each rung estimated by MCMC and integrated by the trapezoid rule — the standard ergm-style bridge estimator. For fully dyad-independent models the exact log-likelihood is returned.","title":"How MCMLE Works"},{"category":"section","location":"guide/estimation/#MCMC-Sampling","page":"Model Estimation","text":"Each MCMC step:\n\nPropose: randomly select a dyad (ij)\nCompute: change statistics delta(y)_ij for all terms\nAccept/reject: toggle edge with probability min(1 exp(theta^top delta))\n\nThe sampler uses burn-in to reach stationarity and thinning to reduce autocorrelation. The loop itself is the exported mh_toggle! kernel (see the simulation guide).","title":"MCMC Sampling"},{"category":"section","location":"guide/estimation/#Standard-errors:-Fisher-information-plus-Monte-Carlo-error","page":"Model Estimation","text":"vcov(result) for an MCMLE fit is the inverse Fisher information estimated from the final MCMC sample, V = hatSigma^-1, plus the Monte-Carlo component of the estimate (Hunter & Handcock 2006, §3.3): the estimating equation bar g(theta) = g_textobs is itself a Monte-Carlo average with operatornameVar(bar g) = Sigma_mc (the Geyer initial-sequence covariance of the sampled statistics' mean — per-statistic Geyer variances on the diagonal, lag-0 correlations off it, summed over chains), so\n\noperatornamevcov = V + VSigma_mcV \n\nresult.vcov_fisher is V alone, mcmc_se(result) is sqrtoperatornamediag(VSigma_mcV), and show prints R's summary.ergm \"MCMC %\" column, which R defines as the share of the standard error (not of its variance) that the Monte-Carlo term adds:\n\ntextMCMC  = operatornameroundBig(100fractextse - textse_textFishertextseBig)\nqquad textse_textFisher = sqrtoperatornamediag(V)\n\n(ergm:::summary.ergm: 100 * (tot.se - mod.se) / tot.se). On the Florentine edges + gwesp model at n_samples=4096 it rounds to 0 (as R reports); at n_samples=100 it is 1–3 %. The variance share 100textmcmc_se^2textse^2 is a different, larger number — 3–5 % at n_samples=100 — and is not what R prints. A large MCMC % says the sample, not the data, limits the precision: raise n_samples or n_chains.\n\nusing LinearAlgebra\nresult_mcmle = ergm(net, terms; method=:mcmle, n_samples=1000, rng=Xoshiro(1))\nmcmc_se(result_mcmle)                                     # the MC part of each SE\nse_fisher = sqrt.(diag(result_mcmle.vcov_fisher))\nround.(Int, 100 .* (stderror(result_mcmle) .- se_fisher) ./ stderror(result_mcmle))   # R's \"MCMC %\"","title":"Standard errors: Fisher information plus Monte-Carlo error"},{"category":"section","location":"guide/estimation/#MCMLE-Parameters","page":"Model Estimation","text":"Parameter Description Default Guidance\nn_samples MCMC samples per iteration (over all chains) 1000 More = better approximation, smaller MCMC %\nn_chains Independent chains per iteration 1 Parallel when Julia has threads; identical result at any thread count\nburnin Steps before sampling (per chain) 20 × n_dyads Increase if poor mixing\ninterval Steps between samples max(100, n_dyads ÷ 10) Increase to reduce autocorrelation\nmaxiter Maximum NR iterations 20 Increase for slow convergence (max_iter is a deprecated spelling, warned once)\nmissing Masked-dyad policy :error :mle (missing-data ML) or :condition_on_face (see below)\nobs_burnin, obs_interval Constrained-chain controls under missing=:mle dyad-scaled over the masked dyads R's obs.MCMC.burnin / obs.MCMC.interval\ninit Starting coefficients the MPLE coef(fit) to continue an unconverged fit\nconv_threshold Max allowed convergence t-ratio 0.1 Smaller = stricter convergence\nhotelling_alpha Level of the Hotelling T² convergence test 0.05 —\nrng RNG all draws flow from Random.default_rng() Pass Xoshiro(seed) for reproducibility\nbridge_rungs Path-sampling segments for the log-likelihood 16 More = less bias in AIC/BIC; 0 skips it (loglik/AIC/BIC NaN)\nbridge_samples MCMC samples per bridge rung n_samples —\n\n(The tol keyword from earlier versions is deprecated and ignored.)","title":"MCMLE Parameters"},{"category":"section","location":"guide/estimation/#MCMLE-Strengths-and-Limitations","page":"Model Estimation","text":"Aspect Detail\nAccuracy Consistent and asymptotically efficient\nStandard errors Correctly accounts for dependencies\nSpeed Slower — requires MCMC at each iteration\nInitialization Benefits from good MPLE starting values\nUse case Final results, dependent models","title":"MCMLE Strengths and Limitations"},{"category":"section","location":"guide/estimation/#Missing-(Unobserved)-Dyads","page":"Model Estimation","text":"Networks.jl can mark dyads whose tie status is unobserved (statnet-style NA ties) with set_missing_dyad!(net, i, j) — distinct from \"no tie\". The estimation routines treat the mask as follows:\n\nMPLE excludes masked dyads. An unobserved tie status is not a response, so masked dyads contribute no row to the logistic-regression design; nobs(result) shrinks by the number of masked dyads. The masked dyads' face values (edge present/absent as stored) still enter the change statistics of the observed dyads, which are computed conditional on the rest of the network. This is the available-case pseudo-likelihood, a principled treatment: Networks.supports_missing(mple) == true, and mple takes no missing keyword.\nMCMLE refuses a masked network by default (missing=:error throws the shared ArgumentError, whose last bullets name the two policies it accepts; missing_policies(mcmle) == (:error, :condition_on_face, :mle); the generic :face is not accepted) and offers two treatments:\nmissing=:mle — missing-data maximum likelihood, what R ergm() does with NA ties (Handcock & Gile 2010). The likelihood of the observed dyads is Z_obs(θ)/Z(θ), the masked dyads integrated out. Each MCMLE iteration runs two chains: the free chain toggles every dyad (masked ones included) and estimates E[g(Y)]; the constrained chain toggles only the masked dyads, the observed dyads held at their observed values, and estimates E[g(Y) | Y_obs], which replaces the observed statistics as the Newton target. The Fisher information is Var[g] − Var[g | Y_obs] (standard errors are NaN, with a warning, if that difference is not positive definite), both chains contribute Monte-Carlo error to the standard errors, and the log-likelihood is log Z_obs(θ) − log Z(θ) from two path-sampling bridges. The constrained chain's burn-in/thinning are obs_burnin/obs_interval (defaults: the dyad-scaled rule applied to the number of masked dyads). nobs is the number of observed dyads; the fit records missing_method = :mle. Use it whenever the masked dyads are genuinely unobserved. Under dyad independence it coincides with the available-case MPLE.\nmissing=:condition_on_face — hold each masked dyad fixed at its stored face value: never toggled, and counted in the observed statistics as recorded. That is the model conditional on the face values — a different estimand, right only when the stored values are true by construction (a structural zero, a tie fixed by design). It is warned, and the fit records missing_method = :condition_on_face.\nOn the provenanced masked-flomarriage fixture (four NA dyads, two of them ties) the two estimands differ by 0.11 on both coefficients of edges + gwesp(0.5) — twenty times R's seed-to-seed spread — which is why neither is the default.\n\nnet_na = copy(net)\nset_missing_dyad!(net_na, 3, 4)          # dyad 3–4 was not measured\nfit_na = ergm(net_na, terms; method=:mple) # excluded from the pseudo-likelihood\nnobs(fit_na)                             # one fewer observation\nfit_na.missing_method                    # :available_case\n\n# Missing-data maximum likelihood: the masked dyad is integrated out\nfit_mle = ergm(net_na, [Edges(), GWESP(0.5)]; method=:mcmle,\n               missing=:mle, n_samples=500, rng=Xoshiro(1))\nfit_mle.missing_method                   # :mle\nnobs(fit_mle)                            # one fewer observed dyad\n\n# Conditioning on the face value must be asked for in writing, and is warned\nfit_cf = ergm(net_na, [Edges(), NodeMatch(:gender)]; method=:mcmle,\n              missing=:condition_on_face, n_samples=200, rng=Xoshiro(1))\nfit_cf.missing_method                    # :condition_on_face\n\nSimulation and GOF (simulate_ergm, sample_networks, mh_sample, gof) accept only :condition_on_face: a simulated network has a value at every dyad, so \"simulation under missing data\" is not defined, and missing=:mle is refused with a message saying so. mh_sample(...; toggleable=:all) and toggleable=:masked expose the two MCMLE chains directly.","title":"Missing (Unobserved) Dyads"},{"category":"section","location":"guide/estimation/#Comparing-Methods","page":"Model Estimation","text":"# Quick exploration\nresult_mple = ergm(net, terms; method=:mple)\n\n# Final analysis (seeded: every Monte-Carlo draw flows from `rng`)\nresult_mcmle = ergm(net, terms; method=:mcmle, rng=Xoshiro(42), verbose=true)\n\n# Compare coefficients\nprintln(\"MPLE:  \", round.(coef(result_mple), digits=3))\nprintln(\"MCMLE: \", round.(coef(result_mcmle), digits=3))\n\nFor models with weak dependencies (e.g., only Edges and NodeMatch), MPLE and MCMLE typically agree closely. For models with strong dependencies (e.g., Triangle, GWESP), MCMLE is preferred.","title":"Comparing Methods"},{"category":"section","location":"guide/estimation/#Understanding-Results","page":"Model Estimation","text":"The ERGMResult object contains:\n\nField Type Description\ncoefficients Vector{Float64} Estimated coefficients\nstd_errors Vector{Float64} Standard errors\nz_values Vector{Float64} Z-statistics (coef/SE)\np_values Vector{Float64} Two-sided p-values\nloglik Float64 (Pseudo-)log-likelihood\naic Float64 Akaike Information Criterion (dof = the finite coefficients, as R's logLik df)\nbic Float64 Bayesian Information Criterion (sample size = the dyads the finite coefficients were estimated on — all of them, or after a drop the dyads the dropped term does not touch, R's logLik nobs)\nmethod Symbol :mple or :mcmle\nconverged Bool Convergence status\nmcmc_samples Matrix{Float64} or nothing MCMC samples (MCMLE only)\nse_type Symbol :hessian, :bootstrap, or :mcmc — how the SEs were obtained\nmissing_method Symbol :none, :available_case (MPLE dropped masked dyads), :mle (integrated out by the constrained chain) or :condition_on_face\nvcov_fisher Matrix{Float64} MCMLE: the inverse Fisher information alone (vcov adds the MC term); MPLE: vcov\nmcmc_se Vector{Float64} Monte-Carlo component of each SE (mcmc_se(result)); zeros for MPLE\nmcmc_convergence MCMLEConvergence or nothing MCMLE: (iterations, step_length, t_ratios, hotelling_p, n_eff) on the final sample","title":"Understanding Results"},{"category":"section","location":"guide/estimation/#Accessor-Functions","page":"Model Estimation","text":"The full ecosystem StatsAPI surface is defined on ERGMResult (Networks.check_statsapi(result; strict=true) passes):\n\ncoef(result)          # Coefficient vector\nstderror(result)      # Standard errors vector\nvcov(result)          # Variance-covariance matrix\nconfint(result)       # Normal-theory limits, one row per coefficient (level=0.95)\ncoeftable(result)     # Networks.CoefficientTable — exactly the table `show` prints\nloglikelihood(result); aic(result); bic(result); nobs(result); dof(result)\nmcmc_se(result)       # Monte-Carlo part of the SEs (zeros for an MPLE fit)","title":"Accessor Functions"},{"category":"section","location":"guide/estimation/#Displaying-Results","page":"Model Estimation","text":"println(result)\n\nshow renders coeftable(result) — the shared ecosystem table with an Estimate, Std.Error, z value and Pr(>|z|) column, p-values floored at <1e-16 and small ones printed in scientific notation — followed by the caveats that apply. For the guide's running MPLE fit of [Edges(), GWESP(0.5), NodeMatch(:gender)] on the Florentine data:\n\nERGM Results\n============\nMethod: mple\nLog-likelihood: -53.8523\nAIC: 113.7, BIC: 122.07\nConverged: true\n\nCoefficients:\n                  Estimate  Std.Error  z value  Pr(>|z|)\nedges              -1.7661     0.3750  -4.7096   2.5e-06 ***\ngwesp.fixed.0.5     0.0973     0.1697   0.5736    0.5662\nnodematch.gender    0.1061     0.5006   0.2120    0.8321\n---\nSignif. codes: 0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nWarning: this model contains dyad-dependent terms and was fit by\nmaximum pseudolikelihood (MPLE). The standard errors are based on the\nnaive pseudolikelihood and are suspect (typically anticonservative);\nthe p-values should not be trusted. Refit with method=:mcmle, or use\nse=:bootstrap for parametric-bootstrap standard errors.\n\nAn MCMLE fit of the same model prints R's \"MCMC %\" line after the table instead of the pseudo-likelihood warning:\n\nprintln(ergm(net, terms; method=:mcmle, n_samples=1000, rng=Xoshiro(1)))\n\nERGM Results\n============\nMethod: mcmle\nLog-likelihood: -53.9462\nAIC: 113.89, BIC: 122.25\nConverged: true\n\nCoefficients:\n                  Estimate  Std.Error  z value  Pr(>|z|)\nedges              -1.7762     0.4301  -4.1302   3.6e-05 ***\ngwesp.fixed.0.5     0.1004     0.2619   0.3835    0.7014\nnodematch.gender    0.1486     0.4783   0.3107    0.7560\n---\nSignif. codes: 0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nMCMC % of the standard error (100·(se − se_fisher)/se): edges 0, gwesp.fixed.0.5 0, nodematch.gender 0\n\nA fit that exhausts maxiter prints Converged: false with the non-convergence caveat directly under it (the same sentence the warning carries and Networks.approximations(result) lists) — see Convergence Issues below for the text and what to do.","title":"Displaying Results"},{"category":"section","location":"guide/estimation/#Interpreting-Coefficients","page":"Model Estimation","text":"","title":"Interpreting Coefficients"},{"category":"section","location":"guide/estimation/#Log-Odds-Ratios","page":"Model Estimation","text":"Coefficients are log-odds ratios for the conditional probability of an edge:\n\ntextlogit(P(Y_ij = 1 mid Y_-ij)) = theta^top delta(y)_ij","title":"Log-Odds Ratios"},{"category":"section","location":"guide/estimation/#Example-Interpretations","page":"Model Estimation","text":"Term Coefficient exp(θ) Interpretation\nEdges -2.3 0.10 Low baseline density\nTriangle 0.9 2.46 Each shared partner increases odds by 146%\nNodeMatch 0.5 1.65 Same-attribute ties are 65% more likely\nNodeCov 0.1 1.11 One-unit increase in attribute raises odds by 11%\nGWESP(0.5) 1.2 3.32 Strong geometrically weighted triadic closure","title":"Example Interpretations"},{"category":"section","location":"guide/estimation/#Confidence-Intervals","page":"Model Estimation","text":"ci = confint(result)            # 95% normal-theory limits: column 1 lower, column 2 upper\nci90 = confint(result; level=0.9)\n\n# Odds ratio confidence intervals\nor_ci = exp.(ci)\n\nThe limits are built from the standard errors the fit reports, so for an MPLE fit of a dyad-dependent model they inherit the anticonservative pseudo-likelihood SEs (use se=:bootstrap or method=:mcmle).","title":"Confidence Intervals"},{"category":"section","location":"guide/estimation/#Model-Comparison","page":"Model Estimation","text":"","title":"Model Comparison"},{"category":"section","location":"guide/estimation/#AIC-and-BIC","page":"Model Estimation","text":"# Fit multiple models\nterms1 = [Edges()]\nterms2 = [Edges(), Triangle()]\nterms3 = [Edges(), GWESP(0.5), NodeMatch(:gender)]\n\nr1 = ergm(net, terms1)\nr2 = ergm(net, terms2)\nr3 = ergm(net, terms3)\n\nprintln(\"Model 1 — AIC: $(r1.aic), BIC: $(r1.bic)\")\nprintln(\"Model 2 — AIC: $(r2.aic), BIC: $(r2.bic)\")\nprintln(\"Model 3 — AIC: $(r3.aic), BIC: $(r3.bic)\")\n\n# Lower AIC/BIC = better fit (with complexity penalty)","title":"AIC and BIC"},{"category":"section","location":"guide/estimation/#Log-Likelihood-Comparison","page":"Model Estimation","text":"println(\"Model 1 LL: \", r1.loglik)\nprintln(\"Model 2 LL: \", r2.loglik)\nprintln(\"Model 3 LL: \", r3.loglik)\n\n# Higher log-likelihood = better fit","title":"Log-Likelihood Comparison"},{"category":"section","location":"guide/estimation/#Convergence-Issues","page":"Model Estimation","text":"","title":"Convergence Issues"},{"category":"section","location":"guide/estimation/#Non-convergence","page":"Model Estimation","text":"An MCMLE fit that exhausts maxiter is never quiet. It is returned with converged == false and\n\na warning at fit time: MCMLE did not converge in maxiter=20 iterations (last max t-ratio 0.25, Hotelling p 0.007, step length γ 1.0): the estimates are the last iterate and the standard errors are unreliable; increase maxiter/n_samples/burnin, check the model for degeneracy (mcmc_diagnostics), or refit from these coefficients (mcmle(model; init=coef(fit)));\nthe same sentence under Converged: false when the result is shown;\nan entry in Networks.approximations(result) (so fit_metadata(result) sees it);\nthe numbers in result.mcmc_convergence: the t-ratios, Hotelling p-value and effective sample size recomputed on the final sample at the returned coefficients, plus the iteration count and the last step length.\n\nif !result.converged\n    c = result.mcmc_convergence\n    println(\"max t-ratio \", maximum(c.t_ratios), \", Hotelling p \", c.hotelling_p)\n    # continue from where it stopped, with a bigger budget\n    result = mcmle(ERGMModel(ERGMFormula(terms), net);\n                   init=coef(result), n_samples=4000, maxiter=40, rng=Xoshiro(2))\nend","title":"Non-convergence"},{"category":"section","location":"guide/estimation/#Common-Causes-and-Solutions","page":"Model Estimation","text":"Issue Symptom Solution\nModel degeneracy Very large coefficients, non-convergence Use GWESP/GWDegree instead of Triangle/Kstar\nNear-degeneracy Slow convergence, unstable estimates Simplify model, use geometrically weighted terms\nA statistic at the boundary of its attainable range (a NodeMatch with no within-group tie; a NodeCov whose positive-change dyads are all empty and negative-change dyads all tied, …) mple warns \"observed statistic(s) … are at their smallest attainable values. Their coefficients will be fixed at -Inf\", returns -Inf (SE 0) for that term and R's estimates, dof, AIC and BIC of the rest; mcmle refuses with the same sentence, before computing anything Remove the term (as R's drop=TRUE does), or keep the mple fit and read the -Inf as \"never\"\nPerfect separation by a combination of statistics (no single one at its boundary, but together they predict every tie) mple warns \"the MPLE does not exist (perfect separation)\" (R: \"The MPLE does not exist!\"), returns the last Newton iterate with converged == false, is_exact == false and the caveat in show/approximations; mcmle refuses to start from it Remove or coarsen a term, or pass init= to mcmle\nMulticollinearity Large standard errors Remove correlated terms","title":"Common Causes and Solutions"},{"category":"section","location":"guide/estimation/#Handling-Degeneracy","page":"Model Estimation","text":"Degeneracy is the most common issue with ERGMs. It occurs when the model places nearly all probability on either the empty or the complete network.\n\n# Degenerate specification (avoid!)\nterms_bad = [Edges(), Triangle()]\n\n# Better specification\nterms_good = [Edges(), GWESP(0.5)]\n\n# Even better — add degree control\nterms_best = [Edges(), GWESP(0.5), GWDegree(0.5)]","title":"Handling Degeneracy"},{"category":"section","location":"guide/estimation/#Advanced-Topics","page":"Model Estimation","text":"","title":"Advanced Topics"},{"category":"section","location":"guide/estimation/#Computing-Summary-Statistics","page":"Model Estimation","text":"# Compute observed statistics without fitting\nstats = summary_stats(net, terms)\nprintln(stats)","title":"Computing Summary Statistics"},{"category":"section","location":"guide/estimation/#Two-Stage-Fitting","page":"Model Estimation","text":"For more control over the estimation process:\n\n# Stage 1: Build the model\nformula = ERGMFormula(terms)\nmodel = ERGMModel(formula, net)\n\n# Stage 2: Fit with your chosen method\nresult = mple(model; verbose=true)\n# or\nresult = mcmle(model; n_samples=2000, verbose=true)","title":"Two-Stage Fitting"},{"category":"section","location":"guide/estimation/#Best-Practices","page":"Model Estimation","text":"Start with MPLE: Use MPLE for initial exploration, switch to MCMLE for final results\nCheck convergence: Always verify result.converged == true\nAvoid degeneracy: Prefer geometrically weighted terms for larger networks\nCompare AIC/BIC: Use information criteria for model selection\nValidate with GOF: Always run goodness-of-fit diagnostics after estimation\nUse verbose mode: Monitor MCMLE progress to diagnose convergence issues\nSufficient network size: ERGMs require networks with at least ~20 nodes for reliable estimation\nSet random seeds: Pass an explicit rng (e.g. rng=Xoshiro(42)) to mcmle/ergm — all Monte Carlo draws flow from it, so runs with the same seed are exactly reproducible\nHonest uncertainty: For dyad-dependent MPLE fits, use se=:bootstrap or refit with method=:mcmle before interpreting p-values","title":"Best Practices"},{"category":"section","location":"#ERGM.jl","page":"Home","text":"Fit probability models for a binary network observed on a fixed set of actors. ERGM.jl combines structural statistics and actor or dyad covariates, estimates their coefficients, and simulates networks for model checking.\n\nFirst analysis Learn the model or data Reference and detail\nFit and check a first model Choose an estimator Browse model terms\n\nnote: Supported scope\nEstimation supports loop-free, one-mode binary networks. Curved and bipartite ERGMs are not implemented. MPLE is a conditional approximation for dyad-dependent terms; use MCMC-MLE and inspect its convergence diagnostics when that is the intended estimator. Missing dyads require the estimator-specific policies described in the estimation guide.","title":"ERGM.jl"},{"category":"section","location":"#Installation","page":"Home","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Start with a dyad-independent baseline for the bundled Florentine marriage network:\n\nusing Networks, ERGM, Random\n\nnet = load_dataset(:florentine_marriage)\nfit = fit_ergm(net, [Edges(), NodeCov(:wealth)]; method=:mple)\ndisplay(fit)\ndraws = simulate_ergm(fit; n_sim=3, rng=Xoshiro(42))\nprintln(network_density.(draws))\n\nHere the wealth term associates the sum of two families’ wealth with their conditional log-odds of a marriage tie. Dyad independence makes this MPLE objective a likelihood. Three draws illustrate simulation; they are too few for a goodness-of-fit assessment. Dependence terms such as triangles change both the model and the estimation problem.","title":"Quick Start"},{"category":"section","location":"#Choosing-Terms","page":"Home","text":"Use Case Recommended Terms\nBaseline density Edges\nReciprocity (directed) Mutual\nTriadic closure Triangle, GWESP\nTwo-path prevalence GWDSP\nDegree distribution Kstar, GWDegree (undirected); OStar, IStar, GWIDegree, GWODegree (directed)\nSpecific degree counts (isolates, ...) Degree, IDegree, ODegree\nAttribute homophily NodeMatch, AbsDiff\nGroup mixing structure NodeMix\nAttribute main effects NodeCov, NodeFactor\nDyadic covariates EdgeCov","title":"Choosing Terms"},{"category":"section","location":"#Documentation","page":"Home","text":"Pages = [\n    \"getting_started.md\",\n    \"guide/terms.md\",\n    \"guide/estimation.md\",\n    \"guide/simulation.md\",\n    \"guide/diagnostics.md\",\n    \"api/types.md\",\n    \"api/terms.md\",\n    \"api/estimation.md\",\n]\nDepth = 2","title":"Documentation"},{"category":"section","location":"#Theoretical-Background","page":"Home","text":"","title":"Theoretical Background"},{"category":"section","location":"#The-ERGM-Framework","page":"Home","text":"An ERGM specifies the probability of a network y on n nodes as:\n\nP_theta(Y = y) = fracexpleft(theta^top g(y)right)c(theta) quad c(theta) = sum_y in mathcalY expleft(theta^top g(y)right)\n\nThe normalizing constant c(theta) sums over all possible networks, making exact computation intractable for all but very small networks. This motivates the two estimation approaches:\n\nMPLE approximates the likelihood by treating dyads as conditionally independent, reducing the problem to logistic regression\nMCMLE uses MCMC sampling to approximate the ratio of normalizing constants, iterating via Newton-Raphson until convergence","title":"The ERGM Framework"},{"category":"section","location":"#Change-Statistics","page":"Home","text":"The change statistic delta_g(y)_ij measures how statistic g changes when edge (ij) is toggled:\n\ndelta_g(y)_ij = g(y^+_ij) - g(y^-_ij)\n\nChange statistics are central to both estimation (MPLE uses them as features) and simulation (MCMC acceptance probabilities depend on them).","title":"Change Statistics"},{"category":"section","location":"#References","page":"Home","text":"Hunter, D.R., Handcock, M.S., Butts, C.T., Goodreau, S.M., Morris, M. (2008). ergm: A Package to Fit, Simulate and Diagnose Exponential-Family Models for Networks. Journal of Statistical Software, 24(3), 1-29.\nRobins, G., Pattison, P., Kalish, Y., Lusher, D. (2007). An introduction to exponential random graph (p) models for social networks. *Social Networks, 29(2), 173-191.\nSnijders, T.A.B. (2002). Markov chain Monte Carlo estimation of exponential random graph models. Journal of Social Structure, 3(2), 1-40.\nStrauss, D., Ikeda, M. (1990). Pseudolikelihood estimation for social networks. Journal of the American Statistical Association, 85(409), 204-212.\nFrank, O., Strauss, D. (1986). Markov graphs. Journal of the American Statistical Association, 81(395), 832-842.","title":"References"},{"category":"section","location":"#Citation","page":"Home","text":"If you use ERGM.jl in your work, please cite it using the entry in CITATION.bib:\n\n@misc{SNWJERGMJL,\n  author = {{Statistical Network Analysis with Julia}},\n  title = {ERGM.jl: Exponential Random Graph Models for Julia},\n  year = {2026},\n  url = {https://github.com/statistical-network-analysis-with-Julia/ERGM.jl},\n  note = {Homepage: https://statistical-network-analysis-with-Julia.github.io/ERGM.jl; GitHub: https://github.com/statistical-network-analysis-with-Julia}\n}","title":"Citation"},{"category":"section","location":"#Module","page":"Home","text":"","title":"Module"},{"category":"module","location":"#ERGM","page":"Home","text":"ERGM.jl - Exponential Random Graph Models for Julia\n\nA Julia package for fitting, simulating, and diagnosing Exponential-Family Random Graph Models (ERGMs).\n\nPort of the R ergm package from the StatNet collection.\n\n\n\n\n\n","title":"ERGM"}]
}
